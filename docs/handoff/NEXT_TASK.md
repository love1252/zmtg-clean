# 下一任务

## 当前任务

执行第十四阶段 API 版本治理迁移矩阵动作应用，并创建草稿 PR。

## 审核重点

1. 是否只修改 55 条已批准记录。
2. 是否只修改 `recommended_action` 字段。
3. 是否将 55 条记录从 `KEEP_REVIEW` 改为 `API_VERSION_REVIEW`。
4. 是否确认修改前 `API_VERSION_REVIEW` 为 91 条。
5. 是否确认修改后 `API_VERSION_REVIEW` 为 146 条。
6. 是否保持风险、阶段、状态、目标路径、人工审核要求和 notes 不变。
7. 是否保持两条运行时边界记录逐字段不变。
8. 是否保持迁移矩阵行数和路径集合不变。
9. 本轮不得修改或移动任何 API 文件。
10. 本轮不得修改 Schema、Migration、依赖或锁文件。

## 后续候选

第十四阶段合并后，规划 API 版本治理辅助标记方案，用于两条已经确认运行时边界、但不能覆盖主 `recommended_action` 的路由记录。辅助标记方案完成前，不修改 API 路由实现。
