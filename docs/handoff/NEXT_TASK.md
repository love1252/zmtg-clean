# 下一任务

## 当前任务

审核第八阶段运行时 Demo 与休眠领域 Mock 边界，并创建草稿 PR。

## 审核重点

1. 11 个原运行时 Demo 候选是否完整纳入边界清单。
2. 是否依据当前调用证据确认 10 个运行时边界。
3. 是否将 1 个过时运行时标记重分类为休眠领域 Mock。
4. 是否正确区分 API、认证、领域与服务职责。
5. 是否区分 Framework 路由、运行时依赖、显式注入和仅测试调用。
6. 是否明确 11 个候选本阶段均不移动。
7. 迁移矩阵是否分别记录 `runtime_boundary_confirmed` 和 `dormant_boundary_confirmed`。
8. 本轮不得修改或移动任何运行时、测试、Demo 脚本、Seed 或依赖配置文件。

## 后续候选

第八阶段合并后，第九阶段审核最后 3 个候选：1 个 Demo 脚本和 2 个 Seed 安全边界文件。第九阶段仍不执行 Seed、Migration、数据库连接或真实数据写入。
