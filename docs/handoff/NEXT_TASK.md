# 下一任务

## 当前任务

审核第九阶段 Demo 脚本、数据库 Seed 入口与 Seed 安全守卫，并创建草稿 PR。

## 审核重点

1. 最后 3 个第五阶段候选是否完整纳入边界清单。
2. 是否确认 Demo CLI 默认只执行 dry-run。
3. 是否确认写入或清理前必须先通过安全守卫。
4. 是否识别 Demo CLI 与核心 Seed Guard 之间的策略差异。
5. 是否确认数据库 Seed 入口在创建 Client 前执行核心守卫。
6. 是否确认核心守卫只允许 loopback、本地目标和固定人工确认。
7. 是否确认第五阶段 `audit_completed` 候选归零。
8. 本轮不得执行 Seed、Migration、数据库连接或真实数据写入。
9. 本轮不得修改或移动脚本、Seed、守卫、测试、Schema 或依赖配置。

## 后续候选

第九阶段合并后，第十阶段先进行目录重构阶段性闭环审计，核对各阶段状态、遗留风险和迁移矩阵计数；在闭环确认前不直接启动 API 路径迁移或源码移动。
