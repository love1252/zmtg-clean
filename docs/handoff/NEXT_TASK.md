# 下一任务

## 当前任务

审核第五阶段 Demo、Mock、Fixture、Seed 调用关系清单，并创建草稿 PR。

## 审核重点

1. 第二阶段列出的 44 个候选是否全部纳入清单。
2. 每个候选的文件归属、引用范围和运行时可达性是否有记录。
3. 所有候选的 `move_now` 是否均为 `no`。
4. Seed、认证和运行时 API 是否被标记为高风险边界。
5. 迁移矩阵是否只更新审计状态，没有宣称已完成文件移动。
6. 本轮不得修改源码、脚本、依赖配置、Schema 或 Migration。

## 后续候选

第五阶段合并后，第六阶段优先处理文档历史和纯测试候选的归属说明；继续不移动 Seed、认证和运行时 Demo 文件。
