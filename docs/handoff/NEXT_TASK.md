# 智美天工唯一下一任务

## 唯一下一任务

```text
填写并提交 BASE-B5 仓库外权威业务依据输入表
```

## 当前基线

- evidence submission contract／input template：ready／ready；
- selected branch：`B5_KEEP_BLOCKED`；
- authority evidence submitted／admitted：`0／0`；
- remediation、数据库、Reader、Capability 和 BASE-02 完成状态均未授权。

## 输入文件

执行本轮脚本后，桌面会生成 `base02-b5-authority-evidence-input-20260806.md`。只填写低敏、可追溯的来源引用。

## 禁止范围

- 不填写 PII、原始 tenant／institution 双键、数据库行、连接参数、密码、Token、API Key 或凭证；
- 不提交未脱敏数据库导出；
- 不连接数据库，不执行 DDL、DML、Migration、Seed 或 FK VALIDATE；
- 不创建 Scope，不开放 Reader 或业务 Capability；
- 不把填写模板写成证据已准入或 BASE-B5 已完成。
