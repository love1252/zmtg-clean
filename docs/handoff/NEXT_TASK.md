# 下一任务

## 当前任务

执行第十五阶段 API 版本治理辅助标记方案规划，并创建草稿 PR。

## 审核重点

1. 是否只覆盖两条已确认运行时边界的路由记录。
2. 是否保持主 `recommended_action` 不变。
3. 是否保持主 `status` 不变。
4. 是否拒绝复合动作字符串和直接覆盖主动作。
5. 是否说明仅写入 notes 的局限。
6. 是否评估增加迁移矩阵新列的影响。
7. 是否推荐独立 sidecar 辅助标记注册表。
8. 是否将 `current_path` 作为唯一关联键。
9. 本轮不得修改迁移矩阵。
10. 本轮不得修改或移动任何 API 文件。

## 后续候选

第十五阶段合并后，在单独授权下创建辅助标记注册表，并仅写入两条 `api_version_governance=review_required` 记录。主迁移矩阵和 API 路由实现继续保持不变。
