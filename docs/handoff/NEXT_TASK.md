# 下一任务

## 当前任务

执行第三十一阶段：最终目录重构闭环审计。

## 前置结论

- 第三十阶段已闭环；
- R06 Demo Seed Guard：`resolved`；
- R07 DemoAuthRoutes 测试治理：`resolved`；
- R08 迁移矩阵：`explained_governed_backlog`；
- 第三十一阶段不存在未解释高风险前置阻断；
- 原迁移矩阵共 1509 条，其中 pending 1455 条，继续作为最终审计输入。

## 首轮执行模式

第三十一阶段先执行 `audit_only`：

- 不直接移动正式业务源码；
- 不批量改写迁移矩阵；
- 不在同一分支混入修复；
- 若发现真实阻断，必须形成独立风险编号、精确允许文件和单独分支。

## 必须审计

1. 重新盘点全仓目录和文件集合；
2. 核对迁移矩阵动作、状态和当前文件是否一致；
3. 检查旧路径、重复实现、孤立文件和失效交接入口；
4. 检查错误导入、跨模块反向依赖和循环依赖；
5. 核对 R01-R08 的闭环、保护或延期证据；
6. 更新最终架构说明、交接入口和发布历史；
7. 执行完整验证并形成最终关闭结论。

## 初始允许范围

首轮仅允许新增或修改第三十一阶段审计证据与交接文档。

任何生产源码、测试、脚本、配置或矩阵原文件修改，均需在审计发现后单独授权。

## 强制保护边界

- 不修改 Schema 或 Migration；
- 不修改 package.json 或锁文件；
- 不连接数据库；
- 不执行 Seed 或 Migration；
- 不读取 `.env.local`、`DATABASE_URL` 或真实凭证；
- 不调用真实 HIS、企业微信、短信、webhook 或部署；
- 不删除 `.gitnexus`；
- 不批量移动生产业务源码；
- 不改变权限、租户隔离或错误行为。

## 验证要求

- `git diff --check`
- `pnpm typecheck`
- `pnpm lint`
- 代表性测试
- `pnpm build`
- main／origin/main 一致性检查
- working tree clean

## 最终退出条件

1. 所有路线图领域均已闭环或明确延期；
2. 所有保留文件均有明确职责；
3. 所有移动均有证据和回退记录；
4. 迁移矩阵不存在未解释状态；
5. 旧路径、重复实现、孤立文件、错误导入和循环依赖均有结论；
6. Schema、Migration 和数据库保护边界未被越权修改；
7. Ready 和合并仍需分别授权。
