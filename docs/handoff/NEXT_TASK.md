# 下一任务

## 当前任务

执行第十一阶段 API 路径版本化治理候选范围规划，并创建草稿 PR。

## 审核重点

1. 91 个 `API_VERSION_REVIEW` 候选是否完整纳入清单。
2. 是否明确 91 个候选不等于全仓 API 路由。
3. 是否同时记录候选范围和全仓 `route.ts` 数量。
4. 是否明确候选内版本化路由为 0，不代表全仓版本化路由为 0。
5. 是否区分候选内版本化、非版本化和非路由支持文件。
6. 是否记录仓库内运行时、测试和脚本调用方证据。
7. 是否明确字面量调用方扫描只能作为下限。
8. 是否记录兼容期、弃用信号、观测和回退准入条件。
9. 本轮不得修改迁移矩阵。
10. 本轮不得新增、删除、重命名或移动任何 API 文件。

## 后续候选

第十一阶段合并后，先补齐当前未纳入 `API_VERSION_REVIEW` 的版本化路由分类，并建立全仓版本化／非版本化路由族对照。在完整范围明确前，不选择 API 文件进行移动或删除。
