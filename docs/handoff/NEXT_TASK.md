# 下一任务

## 当前任务

执行第二十五阶段：机构端阶段闭环。

## 审计范围

只读复核第二十二至第二十四阶段，包括：

1. 第二十二阶段机构端职责、依赖和领域所有权基线；
2. 第二十三阶段预约纯领域单文件试点；
3. 第二十四阶段套餐额度只读服务单文件试点；
4. 两个试点的回退路径、调用方和测试覆盖；
5. 机构端剩余文件的后续治理分类。

## 必须输出

1. 第二十二至第二十四阶段闭环审计文档；
2. 两个已完成试点的结果和契约对比；
3. 正式业务源码累计移动统计；
4. 机构端剩余项分类清单：
   - 可迁移；
   - 保持当前位置；
   - 保护边界；
   - 延期处理；
5. 后续非阻断 backlog；
6. 第二十六阶段开放平台职责与依赖图启动条件。

## 默认边界

- 本阶段 audit-only；
- 不移动第三个机构端源码文件；
- 不修改 API；
- 不修改 `file-migration-matrix.csv`；
- 不修改 Schema、Migration、package 或锁文件；
- 不连接真实数据库、HIS 或企业微信；
- 不改变权限、租户隔离、错误响应或真实渠道行为；
- API 后续批次继续冻结。

## 验证

1. `git diff --check`
2. 交接文档一致性检查
3. 两个试点路径、blob、调用方和测试证据可追溯
4. 机构端剩余分类总数与第二十二阶段基线一致
5. 无源码、API、迁移矩阵或运行时配置修改
6. `pnpm typecheck`
7. `pnpm lint`
8. 机构端代表性测试
9. `pnpm build`

## 退出条件

- 第二十二至第二十四阶段无未解释差异；
- 两个试点均具备明确回退路径；
- 机构端剩余项全部获得治理分类；
- 不要求一次移动全部机构端文件；
- 第二十六阶段只能在本阶段合并后单独启动。
