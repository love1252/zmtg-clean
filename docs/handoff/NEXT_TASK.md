# 下一任务

## 当前任务

执行第十二阶段全仓 API 路由分类补全审计，并创建草稿 PR。

## 审核重点

1. 全仓 145 个 `route.ts` 是否完整纳入逐路由清单。
2. 是否确认版本化路由 56 个、非版本化路由 89 个。
3. 是否确认第十一阶段已覆盖 88 个候选内路由。
4. 是否确认候选范围之外仍有 57 个分类缺口。
5. 是否确认缺口包含 56 个版本化路由和 1 个非版本化路由。
6. 是否建立全仓版本化／非版本化路由族对照。
7. 是否记录仓库内运行时、测试和脚本字面量调用证据。
8. 是否明确字面量调用证据只能作为调用方下限。
9. 本轮只生成迁移矩阵分类建议，不得修改迁移矩阵。
10. 本轮不得新增、删除、重命名或移动任何 API 文件。

## 后续候选

第十二阶段合并后，先审核 57 条迁移矩阵分类建议。若全仓存在版本化／非版本化重叠族，则最多选择一个单一路由族形成兼容契约；在调用方、行为等价、权限、租户隔离、弃用期、观测和回退方案明确前，不移动或删除 API 文件。
