# 下一任务

## 当前任务

审核第三阶段脚本入口与实现文件分层结果，并创建草稿 PR。

## 审核重点

1. `package.json` 中的原有命令入口是否保持不变。
2. 根目录兼容入口是否能正确加载下沉后的实现。
3. 测试服务器部署脚本的 `--help` 是否可以正常运行。
4. `run-next.mjs` 和 `run-vitest.mjs` 是否仍可正常使用运行时解析器。
5. TypeScript、ESLint、定向测试和生产构建是否通过。
6. 本轮不得执行真实部署、数据库迁移或 Seed。

## 后续候选

第三阶段合并后，再审核其余根目录脚本是否需要继续下沉，或进入低风险 Demo、Mock、Fixture 分类整理。
