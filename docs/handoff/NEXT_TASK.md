# 下一任务

## 当前状态

第十七至第三十一阶段文件目录重构任务已完成。

- 第三十一阶段：`closed`
- 目录重构总任务：`completed`
- 正式业务源码累计移动：3
- 初始自动发现项：17
- 经复核非阻断项：17
- 未解释阻断：0
- 最终证据：`docs/refactor/phase-31-final-directory-closeout-audit.md`

## 后续工作方式

目录重构完成不等于必须迁移全部代码。

后续只在出现明确业务需求或具体技术债时，按独立任务处理：

- 可迁移候选不得自动搬动；
- 保持当前位置的文件继续作为稳定实现；
- 保护边界必须经过运行时、安全、数据库或依赖专项授权；
- 延期项必须先拆分风险和调用方；
- 不再创建新的目录重构编号阶段。

## 已解释的最终审计发现

- 7 个 `.DS_Store`：初始本地快照中的系统元数据噪声；
- 4 个根目录脚本：稳定兼容入口；
- 1 个品牌资源文件：明确保留的休眠 typed 注册表；
- 5 次旧路径引用：3 个已完成迁移文件的历史来源记录。

上述项目均不要求 runtime 修复。

## 强制保护边界

- 不批量移动生产业务源码；
- 不修改 Schema 或 Migration；
- 不修改 package.json 或锁文件；
- 不连接数据库；
- 不执行 Seed 或 Migration；
- 不读取 `.env.local`、`DATABASE_URL` 或真实凭证；
- 不调用真实 HIS、企业微信、短信、webhook 或部署；
- 不删除 `.gitnexus`；
- 不改变权限、租户隔离或错误行为。

## Git 状态退出条件

- `main == origin/main`
- working tree clean
- Ready 和合并仍需分别授权
