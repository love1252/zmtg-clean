# 下一任务

## 当前任务

审核第七阶段模块 Mock 运行时与测试调用边界，并创建草稿 PR。

## 审核重点

1. 5 个模块 Mock 候选是否完整纳入边界清单。
2. 是否正确区分运行时值依赖、运行时类型依赖和测试值依赖。
3. 是否识别 Mock 文件之间的组合依赖。
4. 是否明确 5 个候选均不能直接迁入测试目录。
5. 迁移矩阵是否记录为 `boundary_confirmed`，而不是迁移完成。
6. 本轮不得修改或移动任何运行时、测试、Seed 或依赖配置文件。

## 后续候选

第七阶段合并后，第八阶段审核 11 个运行时 Demo 边界候选，按 API、认证、领域与服务职责分组；继续只形成边界说明，不直接移动运行时文件。
