# 2026-06-03 开发日志

## Phase 22 Plan Mode：HIS 标准治疗事件 mapper v1

- 分支：`docs/phase22-his-treatment-event-mapper-plan`
- 目标：规划如何把未来 HIS / 机构系统中的治疗事件先转换成智美天工内部可识别的标准治疗事件结构，为后续治疗摘要、路径模板建议、随访任务和运营分析提供稳定输入。
- 完成：
  - 新增 Phase 22 设计文档，明确当前只是 Plan Mode，不是功能实现。
  - 新增 Phase 22 计划文档，给出后续 PR 拆分建议。
  - 明确 Phase 22 承接 Phase 17 标准治疗事件 domain-only 契约，不重新宣称真实 HIS 接入完成。
  - 明确建议字段：`externalEventId`、`externalSource`、`tenantId`、`customerExternalId`、`appointmentExternalId`、`treatmentDate`、`treatmentProject`、`treatmentCategory`、`treatmentStage`、`recoveryStage`、`riskLevel`、`nextCareAction`、`tags`、`rawSourceType` 和 `mappingWarnings`。
  - 明确 mapper v1 与治疗摘要、路径模板、随访建议、来源任务和运营分析的关系。
- 边界：
  - 不写代码。
  - 不改测试。
  - 不新增 API。
  - 不改现有 API。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不接真实 HIS、机构系统或企微。
  - 不接 AI / RAG / Agent。
  - 不做 AI 解析。
  - 不做自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要。
  - 不自动创建随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
- 验证：
  - `git diff --check`
  - `git diff --cached --check`

## Phase 22 PR 2：标准事件 mapper 契约差异评估

- 分支：`docs/phase22-standard-event-contract-gap-review`
- 目标：对比 Phase 17 `StandardTreatmentEvent` 契约和 Phase 22 HIS mapper 建议字段，明确后续是否需要调整字段命名、增加字段或保持兼容策略。
- 完成：
  - 新增 `docs/superpowers/plans/2026-06-03-phase22-standard-event-contract-gap-review.md`。
  - 列出现有 Phase 17 标准事件字段和 mapper 输入白名单。
  - 对比 Phase 22 建议字段：`externalEventId`、`externalSource`、`tenantId`、`customerExternalId`、`appointmentExternalId`、`treatmentDate`、`treatmentProject`、`treatmentCategory`、`treatmentStage`、`recoveryStage`、`riskLevel`、`nextCareAction`、`tags`、`rawSourceType` 和 `mappingWarnings`。
  - 明确 v1 优先保留 Phase 17 `sourceSystem`、`sourceEventId`、`sourceCustomerId` 和 `appointmentRef` 内部命名，不重复造 `external*` 核心 DTO 字段。
  - 明确 `external*` 命名如需兼容，只作为 adapter 输入层别名或文档映射，不进入内部核心 DTO。
  - 明确后续优先只补缺字段：`recoveryStage`、`rawSourceType` 和 `mappingWarnings`。
- 边界：
  - 不写代码。
  - 不改测试。
  - 不新增 API。
  - 不改现有 API。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不接真实 HIS、机构系统或企微。
  - 不接 AI / RAG / Agent。
  - 不做 AI 解析。
  - 不做自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要。
  - 不自动创建随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
- 验证：
  - `git diff --check`
  - `git diff --cached --check`

## Phase 22 PR 3A：标准事件缺口字段 domain-only 契约

- 分支：`codex/phase22-standard-event-contract-fields`
- 目标：在现有 `StandardTreatmentEvent` / mapper 契约中最小补齐 Phase 22 已确认的缺口字段：`recoveryStage`、`rawSourceType` 和 `mappingWarnings`。
- 完成：
  - 在 `StandardTreatmentEvent` 中新增 `recoveryStage`、`rawSourceType` 和 `mappingWarnings`。
  - 在 mapper 输入白名单中只新增上述三个字段，不新增 `externalEventId`、`externalSource`、`customerExternalId` 或 `appointmentExternalId` 核心 DTO 字段。
  - `recoveryStage` 复用安全短文本边界，缺省输出 `null`。
  - `rawSourceType` 只允许粗粒度安全集合：`treatment_record`、`appointment`、`order`、`course_progress`、`manual_review`、`other`。
  - `mappingWarnings` 只允许安全告警代码，去重、限制数量和长度，不作为客户可见文案。
  - 补充 `StandardTreatmentEventMapper.test.ts`，覆盖合法 / 缺省 / 非法字段、context 可信边界、`external*` 拒绝和禁止字段边界。
- 边界：
  - 不新增 API route。
  - 不改现有 API。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不改 UI。
  - 不接真实 HIS、机构系统或企微。
  - 不接 AI / RAG / Agent。
  - 不做 AI 解析。
  - 不做自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要。
  - 不自动创建随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
- 验证：
  - `git diff --check`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/StandardTreatmentEventMapper.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests`
  - `./node_modules/.bin/tsc --noEmit`

## Phase 22 PR 3B：mapper 解析器与安全测试收尾

- 分支：`codex/phase22-standard-event-parser-safety`
- 目标：在 PR 3A 已补齐 `recoveryStage`、`rawSourceType` 和 `mappingWarnings` 的基础上，补强 mapper 解析器安全边界回归测试。
- 完成：
  - 补充 `recoveryStage` 空字符串、手机号、身份证号、病历号原文、raw payload、完整正文、图片 / 文件原文、SQL、stack、token、secret、`DATABASE_URL` 和连接串拒绝测试。
  - 补充 `rawSourceType` 全安全集合、缺省 / 空字符串、非法来源、外部表名、接口路径、请求体、响应体、字段原文和 raw payload 线索拒绝测试。
  - 补充 `mappingWarnings` 全安全代码、缺省、去重、数量限制、长度限制、非字符串、空字符串、未知代码、raw payload、PII、完整正文、SQL、stack、token、secret、`DATABASE_URL` 和连接串拒绝测试。
  - 补充源码扫描测试，确认 mapper 不调用 HIS / 企微 / AI / RAG / Agent / fetch / axios，不写数据库，不创建治疗摘要或随访任务。
  - 新增测试直接通过，未改 `standard-treatment-event.ts` 或 `standard-treatment-event-mapper.ts`。
- 边界：
  - 不新增 API route。
  - 不改现有 API。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不改 UI。
  - 不接真实 HIS、机构系统或企微。
  - 不接 AI / RAG / Agent。
  - 不做 AI 解析。
  - 不做自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要。
  - 不自动创建随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
- 验证：
  - `git diff --check`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/StandardTreatmentEventMapper.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests`
  - `./node_modules/.bin/tsc --noEmit`

## Phase 22 PR 3C：标准治疗事件 mapper 文档 / smoke 收尾

- 分支：`codex/phase22-standard-event-smoke-docs`
- 目标：收口 Phase 22 标准治疗事件 mapper domain-only 最小闭环，确认 Plan Mode、契约差异评估、缺口字段契约、解析器安全测试和文档 / smoke 收尾链路完整。
- 完成：
  - 补充 `StandardTreatmentEventMapper.test.ts` smoke 断言，集中确认 `recoveryStage`、`rawSourceType`、`mappingWarnings` 输出存在。
  - 确认 `sourceSystem`、`sourceEventId`、`sourceCustomerId`、`appointmentRef` 命名继续保留，`externalEventId`、`externalSource`、`customerExternalId`、`appointmentExternalId` 不进入核心 DTO。
  - 确认 `tenantId`、`eventId`、`receivedAt` 仍只来自 mapper context，`mappingWarnings` 仍为白名单安全代码数组。
  - 沿用 PR 3B 源码扫描测试，继续确认 mapper 不调用 HIS / 企微 / AI / RAG / Agent / fetch / axios，不写数据库，不创建治疗摘要或随访任务。
  - README、roadmap、Phase 22 spec / plan 和契约差异评估文档轻量同步 PR 3C 收口状态。
- 边界：
  - 不新增 API route。
  - 不改现有 API。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不改 UI。
  - 不接真实 HIS、机构系统或企微。
  - 不接 AI / RAG / Agent。
  - 不做 AI 解析。
  - 不做自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要。
  - 不自动创建随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
- 验证：
  - `git diff --check`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/StandardTreatmentEventMapper.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests`
  - `./node_modules/.bin/tsc --noEmit`

## 真实 HIS adapter Plan Mode 前置评估

- 分支：`docs/his-adapter-preflight-plan`
- 目标：只规划未来如果要接真实 HIS / 机构系统，在进入实现前必须先评估哪些边界、输入、凭证、安全、租户绑定、幂等、审计和 raw payload 处理策略。
- 完成：
  - 新增真实 HIS adapter 前置评估设计文档。
  - 新增真实 HIS adapter 前置评估计划文档。
  - 明确本 PR 是前置评估，不是 HIS adapter 实现。
  - 明确 HIS adapter 的职责是读取外部系统输入，并转换为 Phase 22 mapper 可接受的安全输入；mapper 的职责是输出标准治疗事件。
  - 明确 adapter 不应该绕过 mapper 直接写治疗摘要、随访任务或运营分析。
  - 规划租户绑定、外部系统连接配置、凭证安全、Webhook / 定时同步 / 手动导入、幂等键、重试策略、错误降级、审计事件、raw payload、字段白名单、敏感字段拒绝、患者身份匹配、人工复核、自动摘要 / 自动任务禁止和自动触达禁止边界。
  - 明确 raw HIS payload 默认不保存；未来如确需保留排障片段，必须单独评估脱敏、保留时间、权限、审计和删除策略。
  - 给出后续 PR A-G 拆分建议：真实 HIS adapter spec / plan、连接配置与凭证边界、Webhook / 同步任务、患者身份匹配、人工复核 / 标准事件预览、adapter domain-only 输入 DTO / parser、真实外部系统接入 PoC。
- 边界：
  - 不写代码。
  - 不改测试。
  - 不新增 API。
  - 不改现有 API。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不接真实 HIS、机构系统或企微。
  - 不接 AI / RAG / Agent。
  - 不做 AI 解析。
  - 不做自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要。
  - 不自动创建随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
- 验证：
  - `git diff --check`
  - `git diff --cached --check`

## 连接配置与凭证边界 Plan Mode

- 分支：`docs/his-connection-credentials-boundary-plan`
- 目标：只规划未来接入真实 HIS / 机构系统前，连接配置和凭证管理应该如何设计边界，包括连接配置字段、凭证类型、凭证展示边界、加密 / 轮换 / 撤销、安全审计、错误降级和权限可见性。
- 完成：
  - 新增连接配置与凭证边界设计文档。
  - 新增连接配置与凭证边界计划文档。
  - 明确本 PR 是连接配置与凭证边界规划，不是连接配置实现、凭证存储实现、测试连接实现或 HIS adapter 实现。
  - 规划连接配置候选字段：`connectionId`、`tenantId` 来源、`connectionName`、`sourceSystem`、`vendorType` / `systemType`、`status`、`createdBy` / `updatedBy`、`createdAt` / `updatedAt`、`lastCheckedAt`、`lastHealthStatus`。
  - 规划一租户多连接、一个外部系统绑定多租户、删除、停用、恢复、撤销边界。
  - 评估 API Key、Basic Auth、OAuth token、签名密钥、mTLS / 专线、IP 白名单、SFTP / 文件导入和厂商专用凭证。
  - 明确凭证明文永不返回前端、永不写审计、永不写日志、永不进入错误信息、永不进入 PR / 文档示例 / demo seed，示例只能使用 `<REDACTED>` 或 `***`。
  - 规划凭证创建、测试连接、启用、暂停、轮换、过期、撤销、删除、连接失败降级和凭证泄露应急。
  - 规划权限与可见性、审计事件、安全元数据、审计禁止项、错误降级和前端展示边界。
  - 明确连接配置与凭证边界是后续真实 HIS adapter 前置条件，后续仍需单独规划连接配置 schema / API、凭证加密与密钥管理、连接健康检查 / 测试连接、Webhook / 同步任务和真实外部系统接入 PoC。
- 边界：
  - 不写代码。
  - 不改测试。
  - 不新增 API。
  - 不改现有 API。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不接真实 HIS、机构系统或企微。
  - 不接 AI / RAG / Agent。
  - 不做 AI 解析。
  - 不做自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存任何真实凭证。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要。
  - 不自动创建随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
  - 不做测试连接实现。
- 验证：
  - `git diff --check`
  - `git diff --cached --check`

## 连接配置 schema / API Plan Mode

- 分支：`docs/his-connection-schema-api-plan`
- 目标：只规划未来 HIS / 机构系统连接配置进入实现前，schema、API、权限、审计、DTO、错误态、凭证引用和租户隔离应该如何设计。
- 完成：
  - 新增连接配置 schema / API v1 设计文档。
  - 新增连接配置 schema / API v1 计划文档。
  - 明确本 PR 是 schema / API Plan Mode，不是 schema 实现、migration、API 实现、凭证存储、测试连接或 HIS adapter。
  - 规划未来连接配置字段：`id`、`tenantId`、`connectionName`、`sourceSystem`、`vendorType`、`systemType`、`status`、`credentialRef`、`healthStatus`、`lastCheckedAt`、`lastErrorCode`、`createdBy`、`updatedBy`、`createdAt`、`updatedAt`、`revokedAt`、`deletedAt`。
  - 明确 `tenantId` 只能来自服务端可信上下文或平台受控选择，`credentialRef` 只能是凭证引用，不是凭证明文。
  - 规划未来机构端 API：`GET /api/institution/his-connections`、`POST /api/institution/his-connections`、`GET /api/institution/his-connections/[connectionId]`、`PATCH /api/institution/his-connections/[connectionId]`、pause、resume、revoke 和 delete 状态操作。
  - 明确平台端管理只能作为受控安全元数据视图规划，不在本 PR 实现。
  - 规划机构管理员、普通机构人员、平台管理员、平台运营和安全角色的可见性边界，跨租户不可见，高危操作必须审计。
  - 规划审计事件：`his_connection:create`、`his_connection:update`、`his_connection:pause`、`his_connection:resume`、`his_connection:revoke`、`his_connection:delete`、`his_connection:test_requested`、`his_connection:test_succeeded`、`his_connection:test_failed`、`his_connection:credential_ref_updated`。
  - 规划 list DTO、detail DTO、create payload、update payload、status transition payload 和稳定错误码。
  - 明确后续 PR A-H 拆分：schema / API Plan Mode、schema / migration 实现 Plan Mode、只读 API、create / update API、状态 API、凭证引用集成、测试连接 Plan Mode、真实 HIS adapter Plan Mode。
- 边界：
  - 不写代码。
  - 不改测试。
  - 不新增 API。
  - 不改现有 API。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不接真实 HIS、机构系统或企微。
  - 不接 AI / RAG / Agent。
  - 不做 AI 解析。
  - 不做自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存任何真实凭证。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要。
  - 不自动创建随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
  - 不做测试连接实现。
- 验证：
  - `git diff --check`
  - `git diff --cached --check`

## 连接配置 schema / migration 实现 Plan Mode

- 分支：`docs/his-connection-schema-migration-plan`
- 目标：只规划未来 HIS / 机构系统连接配置进入 schema / migration 实现前，`his_connections` 表、状态枚举、索引 / 唯一约束、软删除、凭证引用、审计关系、migration 顺序、数据最小化和错误边界应该如何设计。
- 完成：
  - 新增连接配置 schema / migration v1 设计文档。
  - 新增连接配置 schema / migration v1 计划文档。
  - 明确本 PR 是 schema / migration 实现 Plan Mode，不是 schema 实现、migration 实现、API 实现、repository、凭证存储、测试连接或 HIS adapter。
  - 规划未来 `his_connections` 字段：`id`、`tenantId`、`connectionName`、`sourceSystem`、`vendorType`、`systemType`、`status`、`credentialRef`、`healthStatus`、`lastCheckedAt`、`lastErrorCode`、`createdBy`、`updatedBy`、`createdAt`、`updatedAt`、`revokedAt`、`deletedAt`。
  - 明确 `tenantId` 只能来自服务端可信上下文或平台受控选择，`credentialRef` 只能是凭证引用，不是凭证明文、密文或可调用密钥。
  - 明确不保存 token、secret、API key、OAuth token、basic auth、签名密钥、私钥、连接串、raw HIS payload、完整请求体 / 响应体、外部系统错误响应全文、完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文、SQL、stack 或 `DATABASE_URL`。
  - 规划状态枚举：`draft`、`active`、`paused`、`revoked`、`deleted`、`error`；其中 `revoked` 不应普通恢复为 `active`，`deleted` 是软删除 / 归档，`error` 只保存稳定错误码。
  - 规划索引 / 唯一约束候选：`tenantId + id`、`tenantId + status`、`tenantId + sourceSystem`、`tenantId + deletedAt`、`tenantId + credentialRef`、`tenantId + lastCheckedAt`、同租户连接名唯一和软删除唯一处理；明确不做跨租户唯一，不用外部 ID 做主键。
  - 规划审计事件：create、update、pause、resume、revoke、delete、credential ref update、health status change、test requested / succeeded / failed；审计只记录安全元数据。
  - 明确后续 PR A-J 拆分：schema / migration Plan Mode、schema / migration 实现、schema / repository tests、只读 repository、list / detail API、create / update API、pause / resume / revoke API、`credentialRef` 集成 Plan Mode、测试连接 Plan Mode、真实 HIS adapter Plan Mode。
- 边界：
  - 不写代码。
  - 不改测试。
  - 不新增 API。
  - 不改现有 API。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不接真实 HIS、机构系统或企微。
  - 不接 AI / RAG / Agent。
  - 不做 AI 解析。
  - 不做测试连接实现。
  - 不做自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存任何真实凭证。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要。
  - 不自动创建随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
- 验证：
  - `git diff --check`
  - `git diff --cached --check`

## 连接配置 schema / migration 最小实现

- 分支：`codex/his-connection-schema-migration`
- 目标：只落地 HIS / 机构系统连接配置的数据库 schema、Drizzle migration、schema / migration 测试和轻量文档同步。
- 完成：
  - 新增 `his_connection_status` 和 `his_connection_health_status` 数据库枚举。
  - 新增 `his_connections` 安全元数据表，包含 `tenantId`、`connectionName`、`sourceSystem`、`vendorType`、`systemType`、`status`、nullable `credentialRef`、`healthStatus`、`lastCheckedAt`、`lastErrorCode`、`createdBy`、`updatedBy`、`createdAt`、`updatedAt`、`revokedAt` 和 `deletedAt`。
  - 新增租户外键、`tenantId + id` 租户内唯一约束、`tenantId`、`tenantId + status`、`tenantId + sourceSystem`、`tenantId + deletedAt`、`tenantId + credentialRef`、`tenantId + lastCheckedAt` 索引，以及 `deletedAt is null` 的同租户连接名唯一索引。
  - 新增 `drizzle/0006_his_connections.sql` 和 Drizzle meta snapshot。
  - 补充 schema / migration 测试，覆盖字段、状态集合、nullable `credentialRef`、软删除 / 撤销字段、索引约束、敏感字段禁止和 demo seed 不写入连接配置。
  - 轻量同步 README、roadmap 和本 devlog。
- 边界：
  - 不新增 API。
  - 不做 repository。
  - 不改 UI。
  - 不改权限、认证或租户隔离。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不保存 raw HIS payload、真实凭证、token、secret、API key、OAuth token、basic auth、签名密钥、私钥、连接串、完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文、SQL、stack 或 `DATABASE_URL`。
  - 不修改 demo seed 数据。

## 连接配置只读 repository 最小实现

- 分支：`codex/his-connection-read-repository`
- 目标：只落地 HIS / 机构系统连接配置只读 repository，供后续 list / detail API 使用。
- 完成：
  - 新增 `src/modules/institution/server/his-connection-repository.ts`。
  - 新增 `listHisConnectionsByTenant`，按可信 `tenantId` 列出未软删除连接配置。
  - 新增 `getHisConnectionByTenant`，按 `tenantId + connectionId` 读取未软删除连接配置，跨租户或已删除返回 `null`。
  - repository 返回安全 read model，不返回 `credentialRef`，只派生 `credentialConfigured`。
  - 新增 repository 测试，覆盖租户隔离、软删除过滤、凭证引用不返回、敏感字段禁止、只读边界、无外部调用、无治疗摘要 / 随访任务创建和 demo seed 不修改。
  - 轻量同步 README、roadmap 和既有计划文档状态。
- 边界：
  - 不新增 API。
  - 不做写入 repository。
  - 不改 UI。
  - 不改权限、认证或租户隔离。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不保存 raw HIS payload、真实凭证、token、secret、API key、OAuth token、basic auth、签名密钥、私钥、连接串、完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文、SQL、stack 或 `DATABASE_URL`。
  - 不修改 demo seed 数据。
