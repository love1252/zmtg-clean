# 2026-05-29 开发日志

## 视觉底座与工作台入口

- 分支：`codex/visual-foundation`
- PR：[#1 feat: 搭建视觉底座与工作台入口](https://github.com/love1252/zmtg-clean/pull/1)
- 目标：把智美天工 Clean 项目从基础脚手架推进到可预览的产品入口，统一首页、登录页、机构端和平台端视觉。
- 完成：
  - 使用旧项目确认过的官网首页模板替换新项目首页。
  - 复制并适配机构登录页、平台登录页的品牌化视觉。
  - 新增 `/hospital` 机构工作台首屏，承接机构登录后的跳转。
  - 新增 `/open-platform` 平台管理后台首屏，承接平台登录后的跳转。
  - 新增入口页测试，覆盖首页、登录页、机构端、平台端关键文案和导航。
  - 建立 Codex 自主执行工作流文档。
- 修改文件：
  - `src/modules/marketing/components/MarketingHome.tsx`
  - `src/app/(auth)/login/page.tsx`
  - `src/app/(auth)/platform-login/page.tsx`
  - `src/modules/auth/components/LuxuryLoginShell.tsx`
  - `src/app/hospital/page.tsx`
  - `src/app/open-platform/page.tsx`
  - `src/modules/workspace/components/InstitutionWorkspace.tsx`
  - `src/modules/workspace/components/PlatformConsole.tsx`
  - `src/modules/workspace/tests/WorkspaceEntryPages.test.tsx`
  - `docs/operations/codex-autonomous-workflow.md`
- 验证：
  - `pnpm lint`
  - `pnpm test`
  - `pnpm typecheck`
  - `pnpm build`
  - 浏览器预览 `/`、`/login`、`/platform-login`、`/hospital`、`/open-platform`
- 风险：
  - 当前登录、机构端、平台端仍是静态/demo 视觉阶段。
  - `/api/auth/login`、`/api/auth/session`、`/api/auth/logout` 尚未在 clean 项目中实现。
  - 真实租户隔离、权限、审计、数据库模型还未开始迁移。
- 下一步：
  - 保持 PR #1 为 Draft，待用户确认后再转 Ready 或合并。
  - 下一阶段实现干净的 demo auth/session/logout，使登录链路可以完整跑通。

## Demo 认证闭环

- 分支：`codex/auth-demo-session`
- PR：[#2 feat: 接入本地 demo 认证闭环](https://github.com/love1252/zmtg-clean/pull/2)
- 目标：让已完成的机构登录页、平台登录页、机构工作台、平台后台形成本地可跑通的 demo 认证闭环。
- 完成：
  - 新增 `/api/auth/login`，支持机构账号 `admin/admin123` 和平台账号 `platform/admin123`。
  - 新增 `/api/auth/session`，从 HttpOnly demo session cookie 读取当前用户。
  - 新增 `/api/auth/logout`，清理 demo session cookie。
  - 新增 `DemoSessionGate`，机构端只允许 `tenant_admin`，平台端只允许 `platform_admin`。
  - 新增 `LogoutButton`，退出时调用 logout API 后返回对应登录页。
  - 补充 demo auth 路由测试和入口页 session 测试。
  - 更新 README 和本地开发文档中的 demo 账号说明。
- 修改文件：
  - `src/app/api/auth/login/route.ts`
  - `src/app/api/auth/session/route.ts`
  - `src/app/api/auth/logout/route.ts`
  - `src/modules/auth/server/demo-session.ts`
  - `src/modules/auth/components/DemoSessionGate.tsx`
  - `src/modules/auth/components/LogoutButton.tsx`
  - `src/app/hospital/page.tsx`
  - `src/app/open-platform/page.tsx`
  - `src/modules/workspace/components/InstitutionWorkspace.tsx`
  - `src/modules/workspace/components/PlatformConsole.tsx`
  - `src/modules/auth/tests/DemoAuthRoutes.test.ts`
  - `src/modules/workspace/tests/WorkspaceEntryPages.test.tsx`
  - `README.md`
  - `docs/operations/local-development.md`
- 验证：
  - 已运行目标测试：`pnpm test src/modules/auth/tests/DemoAuthRoutes.test.ts src/modules/workspace/tests/WorkspaceEntryPages.test.tsx`
  - 已运行完整验证：`pnpm lint && pnpm test && pnpm typecheck && pnpm build`
  - 浏览器验证：
    - `admin/admin123` 登录后进入 `/hospital`，退出后回到 `/login`。
    - `platform/admin123` 登录后进入 `/open-platform`，退出后回到 `/platform-login`。
- 风险：
  - 这是 demo auth，不是正式认证系统。
  - 生产环境默认禁用 demo auth，除非显式设置 `ZMTG_ENABLE_DEMO_AUTH=true`。
  - 后续真实认证需要替换为数据库用户、服务端 session、密码哈希、审计日志和权限策略。
- 下一步：
  - 保持 PR #2 为 Draft，等待完成代码核对后转 Ready 或合并。
  - 继续规划机构端客户、预约、随访核心模块的迁移顺序。

## 18:42 应用底座整理

- 分支：`codex/app-foundation`
- PR：[#3 docs: 制定应用底座整理计划](https://github.com/love1252/zmtg-clean/pull/3)
- 目标：收拢认证 session 类型和工作台静态配置，降低后续业务模块迁移时的组件耦合。
- 完成：
  - 新增应用底座实施计划文档。
  - 新增认证 session domain，统一 `AuthRole`、`AuthSessionUser`、`AuthSession`、`AuthSessionPayload`。
  - `demo-session` 与 `DemoSessionGate` 改为复用公共 session 类型。
  - 抽离机构端工作台导航、指标、建议和用户分层配置。
  - 抽离平台端导航、指标、健康状态和能力卡配置。
  - 补充 domain 测试，覆盖导航唯一性、active 项唯一和关键业务入口存在。
- 修改文件：
  - `docs/superpowers/plans/2026-05-29-app-foundation.md`
  - `src/modules/auth/domain/session.ts`
  - `src/modules/auth/server/demo-session.ts`
  - `src/modules/auth/components/DemoSessionGate.tsx`
  - `src/modules/auth/tests/AuthSessionDomain.test.ts`
  - `src/modules/workspace/domain/institution-dashboard.ts`
  - `src/modules/workspace/domain/platform-dashboard.ts`
  - `src/modules/workspace/components/InstitutionWorkspace.tsx`
  - `src/modules/workspace/components/PlatformConsole.tsx`
  - `src/modules/workspace/tests/WorkspaceDashboardDomain.test.ts`
- 验证：
  - `./node_modules/.bin/eslint .`：0 error，保留既有 `<img>` warning。
  - `node scripts/run-vitest.mjs run`：6 个测试文件、14 个用例通过。
  - `./node_modules/.bin/tsc --noEmit`：通过。
  - `node scripts/run-next.mjs build --webpack`：通过。
  - 浏览器验证机构端登录进入 `/hospital`、退出回 `/login`。
  - 浏览器验证平台端登录进入 `/open-platform`、退出回 `/platform-login`。
- 风险：
  - 本阶段只整理静态配置和类型，不改变真实业务能力。
  - 当前仍为 demo auth，不能视为正式认证系统。
  - 浏览器自动化中首次读取退出按钮 count 偶发为 0，fresh snapshot 可见且重试点击成功，判断为浏览器可访问树同步问题。
- 下一步：
  - 将 PR #3 推送更新后转 Ready 或合并。
  - 下一阶段拆机构端客户中心、预约中心、智能随访的页面壳和 mock 数据边界。

## 19:47 工作台 UI 重设计

- 分支：`codex/workspace-ui-redesign-spec`
- PR：[#4 docs: 制定机构端与平台端 UI 重设计规格](https://github.com/love1252/zmtg-clean/pull/4)
- 目标：按已确认的 UI 方向，把机构端改为浅奢医疗增长工作台，把平台端改为深色科技运营中枢。
- 完成：
  - 调整机构端导航、指标、AI 建议、客户旅程和行动队列数据。
  - 机构端 `/hospital` 改为品牌化增长工作台，突出“让咨询团队先看到增长机会”。
  - 平台端 `/open-platform` 改为深色科技运营中枢，突出租户、模型、接口和审计的运营总览。
  - 更新工作台 domain 测试和入口页测试，覆盖新版核心文案、导航和能力入口。
  - 修正客户旅程节点类型，避免可选字段导致 TypeScript 报错。
  - 根据截图微调机构端首屏断点和标题换行，避免桌面端挤字。
- 修改文件：
  - `src/modules/workspace/domain/institution-dashboard.ts`
  - `src/modules/workspace/domain/platform-dashboard.ts`
  - `src/modules/workspace/components/InstitutionWorkspace.tsx`
  - `src/modules/workspace/components/PlatformConsole.tsx`
  - `src/modules/workspace/tests/WorkspaceDashboardDomain.test.ts`
  - `src/modules/workspace/tests/WorkspaceEntryPages.test.tsx`
  - `docs/superpowers/plans/2026-05-29-workspace-ui-redesign.md`
  - `docs/superpowers/specs/2026-05-29-workspace-ui-redesign-design.md`
  - `docs/design/workspace-ui-redesign/institution-preview.png`
  - `docs/design/workspace-ui-redesign/platform-preview.png`
- 验证：
  - `./node_modules/.bin/eslint .`：0 error，保留既有 `LuxuryLoginShell.tsx` 的 `<img>` warning。
  - `node scripts/run-vitest.mjs run`：6 个测试文件、14 个用例通过。
  - `./node_modules/.bin/tsc --noEmit`：通过。
  - `node scripts/run-next.mjs build --webpack`：通过。
  - 浏览器验证机构端登录进入 `/hospital`、退出回 `/login`。
  - 浏览器验证平台端登录进入 `/open-platform`、退出回 `/platform-login`。
  - 移动端 390px 检查 `/hospital` 与 `/open-platform` 均无横向溢出。
- 风险：
  - 本阶段仍为静态 demo 工作台，不涉及真实客户数据、权限、API Key、OAuth、Webhook 或数据库迁移。
  - 平台端开放连接能力仅作为导航和视觉入口，后续正式实现必须单独规划安全、签名、审计和租户隔离。
- 下一步：
  - 推送 PR #4，先给用户确认实际页面视觉。
  - 确认后再继续拆机构端客户中心、预约中心、智能随访等二级页面。
