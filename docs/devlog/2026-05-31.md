# 2026-05-31 开发日志

## Phase 5 机构业务页面真实化收尾

- 分支：`chore/phase5-closeout-test-docs`
- 目标：完成 Phase 5 总体验收后的测试和文档收尾，不进入 Phase 6。
- 完成：
  - Phase 5 A/B/C/D 已完成：client/helper 与 view model 地基、客户中心真实 API、预约中心真实 API、智能随访 / 随访任务真实 API。
  - 客户中心、预约中心、智能随访三大业务页面均已通过 `tenant-business-client.ts` 接入现有真实 API。
  - 修正工作台入口测试中的旧断言，将 `WorkspaceEntryPages.test.tsx` 从预约中心旧静态文案校验升级为真实 API 页面切换校验。
  - 工作台入口测试补充客户、预约、随访 API mock，验证切换到三大业务页面后展示 API records。
  - 明确保留的静态内容仅为工作台经营展示、随访旅程说明和静态安全话术提示，不作为三大业务页面的客户、预约、随访任务列表数据源。
- 边界：
  - 不修改业务功能、API route、数据库 schema、migration、权限模型、demo auth 或租户隔离逻辑。
  - 不进入 AI provider、RAG、Agent、企业微信、OAuth、Webhook、支付、平台租户管理、套餐权益或治疗记录。
- 验证：
  - `git diff --check`
  - `node scripts/run-vitest.mjs run`
  - `./node_modules/.bin/tsc --noEmit`
  - `node scripts/run-next.mjs build --webpack`
- 下一步：
  - 合并收尾 PR 后，可进入下一阶段 Plan Mode。

## Phase 6 Plan Mode：运营页面一致性与 workspace 入口真实化

- 分支：`docs/phase6-workspace-entry-consistency-plan`
- 目标：固化 Phase 6 的 spec / plan，并同步 README、roadmap、devlog 状态。
- Plan Mode 结论：
  - Phase 6 推荐聚焦机构工作台首页真实化、workspace 与三大业务页状态一致性、机构端导航边界、页面 smoke 测试和文档收尾。
  - 机构工作台首页应复用现有客户、预约、随访 API 派生关键指标和行动摘要，不新增后端聚合 API。
  - 三大业务页继续保持真实 API records 数据源，不回退到静态 demo 数组。
  - seed/demo 测试数据、随访旅程说明、安全话术说明和静态视觉元素可以保留，但不能被描述成真实同步数据。
- PR 拆分：
  - PR 1：Phase 6 spec/plan 文档与 README/roadmap/devlog 状态更新。
  - PR 2：机构工作台首页接入现有三类真实 API，派生指标、行动摘要、首页 loading/error/empty。
  - PR 3：抽小型共享页面状态组件，统一三大业务页 header/state/boundary 展示。
  - PR 4：导航与 smoke 稳定化，补测试和收尾文档。
- 本次边界：
  - 只做 Phase 6 PR 1 的计划文档和状态同步。
  - 不修改业务代码、页面、测试、API route、数据库 schema / migration、权限、认证或租户隔离。
  - 不进入 Phase 6 PR 2/3/4 的代码执行。
  - 不进入 AI provider、Agent、RAG / 知识库真实功能、企业微信、HIS / CRM / OTA、API Key、OAuth、Webhook、支付、合同、发票、套餐权益 enforcement、平台租户管理真实功能、治疗记录完整正文或客户详情完整时间线。
