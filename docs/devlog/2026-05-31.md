# 2026-05-31 开发日志

## Phase 5 机构业务页面真实化收尾

- 分支：`chore/phase5-closeout-test-docs`
- 目标：完成 Phase 5 总体验收后的测试和文档收尾，不进入 Phase 6。
- 完成：
  - Phase 5 A/B/C/D 已完成：client/helper 与 view model 地基、客户中心真实 API、预约中心真实 API、智能随访 / 随访任务真实 API。
  - 客户中心、预约中心、智能随访三大业务页面均已通过 `tenant-business-client.ts` 接入现有真实 API。
  - 修正工作台入口测试中的旧断言，将 `WorkspaceEntryPages.test.tsx` 从预约中心旧静态文案校验升级为真实 API 页面切换校验。
  - 工作台入口测试补充客户、预约、随访 API mock，验证切换到三大业务页面后展示 API records。
  - 明确保留的静态内容仅为工作台经营展示、随访旅程说明和静态安全话术提示，不作为三大业务页面的客户、预约、随访任务列表数据源。
- 边界：
  - 不修改业务功能、API route、数据库 schema、migration、权限模型、demo auth 或租户隔离逻辑。
  - 不进入 AI provider、RAG、Agent、企业微信、OAuth、Webhook、支付、平台租户管理、套餐权益或治疗记录。
- 验证：
  - `git diff --check`
  - `node scripts/run-vitest.mjs run`
  - `./node_modules/.bin/tsc --noEmit`
  - `node scripts/run-next.mjs build --webpack`
- 下一步：
  - 合并收尾 PR 后，可进入下一阶段 Plan Mode。

## Phase 6 Plan Mode：运营页面一致性与 workspace 入口真实化

- 分支：`docs/phase6-workspace-entry-consistency-plan`
- 目标：固化 Phase 6 的 spec / plan，并同步 README、roadmap、devlog 状态。
- Plan Mode 结论：
  - Phase 6 推荐聚焦机构工作台首页真实化、workspace 与三大业务页状态一致性、机构端导航边界、页面 smoke 测试和文档收尾。
  - 机构工作台首页应复用现有客户、预约、随访 API 派生关键指标和行动摘要，不新增后端聚合 API。
  - 三大业务页继续保持真实 API records 数据源，不回退到静态 demo 数组。
  - seed/demo 测试数据、随访旅程说明、安全话术说明和静态视觉元素可以保留，但不能被描述成真实同步数据。
- PR 拆分：
  - PR 1：Phase 6 spec/plan 文档与 README/roadmap/devlog 状态更新。
  - PR 2：机构工作台首页接入现有三类真实 API，派生指标、行动摘要、首页 loading/error/empty。
  - PR 3：抽小型共享页面状态组件，统一三大业务页 header/state/boundary 展示。
  - PR 4：导航与 smoke 稳定化，补测试和收尾文档。
- 本次边界：
  - 只做 Phase 6 PR 1 的计划文档和状态同步。
  - 不修改业务代码、页面、测试、API route、数据库 schema / migration、权限、认证或租户隔离。
  - 不进入 Phase 6 PR 2/3/4 的代码执行。
  - 不进入 AI provider、Agent、RAG / 知识库真实功能、企业微信、HIS / CRM / OTA、API Key、OAuth、Webhook、支付、合同、发票、套餐权益 enforcement、平台租户管理真实功能、治疗记录完整正文或客户详情完整时间线。

## Phase 6 PR 2：机构工作台首页真实 API

- 分支：`codex/phase6-workspace-dashboard-real-api`
- 目标：让机构工作台首页读取现有客户、预约、随访 GET API，并基于真实 records 派生运营摘要。
- 完成：
  - 首页调用 `/api/institution/customers`、`/api/institution/appointments`、`/api/institution/followups`。
  - 新增 workspace dashboard view model，派生客户总数、高优先级客户、待确认预约、待处理随访、高风险随访和近期人工处理摘要。
  - 首页补充 loading、empty、401 / 403 / 503 错误态。
  - 移除首页旧静态经营数据源和“今日高意向客户 18 位”“AI 已排序”“实时同步”等不可验证文案。
- 边界：
  - 不新增 API route、数据库 schema、migration、权限、认证、租户隔离或 mutation。
  - 不改客户中心、预约中心、智能随访页面业务流程。

## Phase 6 PR 3：共享页面状态组件

- 分支：`codex/phase6-shared-page-states`
- 目标：统一机构端首页、客户中心、预约中心、智能随访的加载态、空态、错误态、权限态、不可用态和占位态。
- 完成：
  - 新增 `InstitutionPageState`，统一 loading / empty / error / forbidden / unavailable / placeholder 展示。
  - 新增 `InstitutionSectionHeader`，统一三大业务页标题、副标题和操作区基础布局。
  - 客户中心、预约中心、智能随访和工作台首页复用共享状态组件。
  - 组件测试覆盖 401 / 403 / 503 稳定中文提示，并验证不透出数据库、SQL、连接串、DATABASE_URL 等敏感信息。
- 边界：
  - 不改 API、数据库、权限、认证、租户隔离。
  - 不新增业务功能，不改客户 / 预约 / 随访业务流程。

## Phase 6 PR 4：导航与 smoke 稳定化收尾

- 分支：`chore/phase6-navigation-smoke-closeout`
- 目标：明确机构端导航已接入 / 后续占位边界，补 workspace entry smoke，并收尾 Phase 6 文档。
- 完成：
  - 导航中标注已接入页面：工作台、客户中心、预约中心、智能随访。
  - 导航中标注后续占位页面：客服工作台、知识库、数据分析。
  - 占位页明确不会在 Phase 6 触发客服、知识库或数据分析真实功能请求。
  - workspace smoke 覆盖登录后进入机构端、首页真实 API mock、桌面 / 移动导航切换三大业务页、占位入口和旧静态误导文案缺失。
  - README、roadmap、Phase 6 spec / plan 更新为 Phase 6 完成状态。
- 静态内容边界：
  - seed/demo 测试数据继续保留。
  - 随访旅程和演示安全话术继续作为静态说明。
  - 工作台背景、视觉卡片和非数据性视觉元素可保留，但不能描述成真实同步数据。
  - 三大业务页面不使用静态 demo 数组作为业务 records 数据源。
- 后续建议：
  - 重新进入 Plan Mode 评估客户详情时间线第一版、审计日志只读查询、治疗记录结构化摘要安全计划、平台租户管理与套餐权益。
  - 知识库 / RAG、AI provider、Agent、企业微信、OAuth、Webhook、API Key、支付和计费继续作为高风险模块后置。

## Phase 7 PR 1：客户详情时间线 v1 spec / plan

- 分支：`docs/phase7-customer-timeline-plan`
- 目标：固化 Phase 7 A-first 路线，优先做客户详情时间线 v1，并将审计日志只读查询后置到 Phase 8。
- 完成：
  - 新增 Phase 7 design spec 和 implementation plan。
  - 明确客户详情时间线 v1 范围：客户脱敏摘要、预约摘要、随访摘要、结构化时间线和安全审计摘要。
  - 明确允许最小 audit `resource_id` enrich，不新增自由 `metadata jsonb`。
  - 明确 Phase 7 不进入完整治疗记录、完整病历正文、咨询对话全文、AI、RAG、Agent、企微、OAuth、Webhook 或支付。

## Phase 7 PR 2：最小 audit resource_id enrich

- 分支：`codex/phase7-audit-resource-id`
- 目标：为审计事件补充最小目标资源关联能力，支持后续客户详情时间线精确关联客户、预约和随访记录。
- 完成：
  - `audit_events` 新增 nullable `resource_id`。
  - 新增 `(tenant_id, resource, resource_id, occurred_at)` 查询索引。
  - 审计领域模型和仓储支持可选 `resourceId`。
  - 客户、预约、随访现有审计写入链路补充安全可确认的目标记录 id。
- 边界：
  - 未新增 `metadata jsonb`。
  - 未存请求体、错误堆栈、SQL、连接串、手机号原文、身份证号、病历号原文、治疗记录正文或咨询对话全文。

## Phase 7 PR 3：客户详情 timeline 后端 API

- 分支：`codex/phase7-customer-timeline-api`
- 目标：新增单客户详情时间线只读 API，为 UI 提供稳定结构化摘要。
- 完成：
  - 新增 `GET /api/institution/customers/[customerId]/timeline`。
  - 服务端从 session 推导 tenant，不接受前端传入 `tenantId`。
  - 使用 `tenantId + customerId` 校验客户归属，跨租户或不存在客户返回稳定 404。
  - 返回客户脱敏摘要、预约摘要、随访摘要、customer audit 摘要和结构化 `timeline`。
- 边界：
  - 未新增 schema / migration。
  - 未改权限、认证或租户隔离。
  - 未返回手机号原文、身份证号、病历号原文、治疗记录正文、咨询对话全文、请求体、SQL、连接串、stack、token 或 secret。

## Phase 7 PR 4：客户详情时间线 UI

- 分支：`codex/phase7-customer-timeline-ui`
- 目标：在客户中心增加“查看详情”入口，并展示客户详情时间线 v1。
- 完成：
  - 客户中心每个客户新增“查看详情”入口。
  - 新增右侧客户详情时间线抽屉。
  - 抽屉调用已合并的 timeline API，展示客户脱敏摘要、预约摘要、随访摘要、结构化时间线和安全审计摘要。
  - 覆盖 loading、empty、401、403、404、503、关闭后列表保留、请求不携带 tenantId 和敏感信息不展示测试。
- 边界：
  - 未新增后端 API。
  - 未新增 schema / migration。
  - 未改权限、认证或租户隔离。
  - 未进入审计日志完整查询页面、AI、RAG、Agent、企微、OAuth、Webhook 或支付。

## Phase 7 PR 5：smoke / 文档收尾

- 分支：`chore/phase7-customer-timeline-closeout`
- 目标：补充客户详情时间线 smoke / entry 测试，更新 README、roadmap、devlog 和 Phase 7 spec / plan，标记 Phase 7 完成。
- 完成：
  - workspace entry smoke 覆盖机构入口进入客户中心、打开客户详情时间线、展示脱敏客户摘要、预约摘要、随访摘要、结构化时间线和安全审计摘要。
  - smoke 确认详情关闭后客户列表仍保留。
  - smoke 确认 timeline 请求不发送 `tenantId`，不发送 mutation。
  - smoke 确认不展示手机号原文、身份证号、病历号原文、完整治疗记录正文、咨询对话全文、SQL、DATABASE_URL、连接串、stack、token 或 secret。
  - README、roadmap、Phase 7 spec / plan 更新为 Phase 7 完成状态。
- Phase 7 最终状态：
  - 客户详情时间线 v1 已完成。
  - audit `resource_id` enrich 已完成。
  - 客户详情 timeline 后端 API 已完成。
  - 客户详情时间线 UI 已完成。
  - 审计日志完整查询页面仍未做。
  - AI / RAG / Agent / 企微 / OAuth / Webhook / 支付仍未做。
- 后续建议：
  - 进入 Phase 8 Plan Mode，单独评估审计日志只读查询基础版。

## Phase 8 PR 1：审计查询 spec / plan 文档

- 分支：`docs/phase8-audit-query-plan`
- 目标：固化 Phase 8 A-first 路线，优先做审计日志只读查询基础版，并后置平台租户管理、治疗记录、AI / RAG / Agent、企微、OAuth、Webhook 和支付。
- 完成：
  - 新增 Phase 8 design spec 和 implementation plan。
  - 明确机构端只能查看当前租户审计事件。
  - 明确平台端采用受控只读范围，`security_auditor` 适合承接跨租户安全事件，当前不重构权限模型。
  - 明确默认不新增 schema / migration，不新增 `metadata jsonb`，不存请求体或隐私正文。

## Phase 8 PR 2：审计查询 domain / repository / query parser / API DTO

- 分支：`codex/phase8-audit-query-domain`
- 目标：为机构端和平台端审计日志只读 API/UI 提供底层查询能力。
- 完成：
  - 新增审计查询条件类型和显式 scope。
  - 新增白名单 query parser，覆盖 `from`、`to`、`resource`、`resourceId`、`action`、`result`、`reason`、`actorId`、`limit` 和 cursor。
  - `audit-event-repository` 新增只读 `listAuditEvents()`，保留原写入逻辑不变。
  - 新增安全 DTO mapper，保留 `resourceId`，不返回请求体、metadata、SQL、stack、token、secret、连接串或业务正文。
- 边界：
  - 未新增 API route。
  - 未新增 UI。
  - 未新增 schema / migration。
  - 未改权限、认证或租户隔离。

## Phase 8 PR 3：机构端审计只读 API 与基础 UI

- 分支：`codex/phase8-institution-audit-events`
- 目标：新增机构端审计日志只读能力，让机构端查看当前租户审计事件。
- 完成：
  - 新增 `GET /api/institution/audit-events`。
  - 服务端从 access context 推导当前机构租户，不接受前端 `tenantId` 切换租户。
  - 机构端新增「审计日志」入口和基础 UI。
  - 支持白名单筛选、分页 DTO、loading、empty、error、403 和 503。
  - UI 不展示 `tenantId`，不展示请求体、metadata、SQL、stack、token、secret、连接串、手机号原文、身份证号、病历号原文、治疗记录正文或咨询对话全文。
- 边界：
  - 未做平台端审计。
  - 未新增 schema / migration。
  - 未改权限、认证或租户隔离模型。

## Phase 8 PR 4：平台端审计 API 与平台 UI

- 分支：`codex/phase8-platform-audit-events`
- 目标：新增平台端审计日志只读能力，让平台端在受控范围内查看审计事件。
- 完成：
  - 新增 `GET /api/open-platform/audit-events`，选择该路径是因为当前平台页面入口和模块命名使用 `/open-platform`。
  - 平台端「权限与审计」新增审计日志只读 UI。
  - 支持平台端 `tenantId` 筛选、审计字段白名单筛选和分页 DTO。
  - `platform_admin` 与现有 `security_auditor` 角色可访问安全 DTO；未新增 `security_auditor` 账号，未重构权限模型。
  - UI 可展示 `tenantId` 作为平台审计归属字段，但不展示请求体、metadata、SQL、stack、token、secret、连接串或业务正文。
- 边界：
  - 未改机构端审计能力。
  - 未新增 schema / migration。
  - 未改权限、认证或租户隔离模型。
  - 未做导出、告警、复杂风控、平台租户管理或套餐权益。

## Phase 8 PR 5：smoke / 文档收尾

- 分支：`chore/phase8-audit-query-closeout`
- 目标：补充审计查询入口 smoke，更新 README、roadmap、devlog 和 Phase 8 spec / plan，标记 Phase 8 完成。
- 完成：
  - workspace entry smoke 强化机构端审计日志入口、筛选请求和 `tenantId` 不展示边界。
  - workspace entry smoke 强化平台端审计日志入口、平台 `tenantId` 筛选和敏感字段不展示边界。
  - audit API smoke 强化 `platform_operator` 无权访问平台审计 API。
  - 机构端和平台端审计 UI 覆盖 401、403、503 稳定错误态。
  - README、roadmap、Phase 8 spec / plan 更新为 Phase 8 完成状态。
- Phase 8 最终状态：
  - 审计查询底层能力已完成。
  - 机构端审计只读 API 与 UI 已完成。
  - 平台端审计 API 与 UI 已完成。
  - 机构端只能查看当前租户审计事件，平台端使用受控只读范围。
  - 两端均不展示请求体、metadata、SQL、stack、token、secret、DATABASE_URL、连接串、手机号原文、身份证号、病历号原文、治疗记录正文、咨询对话全文或业务正文。
  - 未新增 schema / migration。
  - 未改权限、认证或租户隔离模型。
  - 未进入审计导出、告警、复杂风控、平台租户管理、治疗记录、AI / RAG / Agent、企微、OAuth、Webhook、支付或套餐权益 enforcement。
- 后续建议：
  - 进入 Phase 9 Plan Mode，在治疗记录结构化摘要 v1、平台租户管理基础版和审计高级治理之间重新评估优先级。

## Phase 9 PR 1：平台租户管理 spec / plan 文档

- 分支：`docs/phase9-platform-tenant-management-plan`
- 目标：固化 Phase 9 A-first 路线，优先做平台端租户管理基础版，并后置治疗记录结构化摘要、知识库 / RAG、AI provider、Agent、企微、OAuth、Webhook、支付和套餐 enforcement。
- 完成：
  - 新增 Phase 9 design spec 和 implementation plan。
  - 明确平台端租户管理 v1 只展示租户运营元数据。
  - 明确 `platform_admin` 可查看租户列表和基础详情，`platform_operator` 对完整租户详情采用保守边界。
  - 明确需要最小套餐 / 配额 schema、只读 API、平台 UI、smoke 和文档收尾。
- 边界：
  - 只做文档。
  - 未改业务代码、API、数据库、权限、认证或租户隔离。

## Phase 9 PR 2：租户套餐 / 配额数据底座

- 分支：`codex/phase9-tenant-plan-quota-schema`
- 目标：为平台端租户管理基础版提供最小可用的数据底座。
- 完成：
  - 新增租户套餐、租户套餐分配和租户配额快照 schema / migration。
  - demo seed 写入套餐、分配和配额快照演示数据。
  - 新增 open-platform tenant-management domain / DTO 和 repository 查询。
  - DTO 只返回租户运营元数据、套餐 / 配额和聚合用量快照。
  - 测试覆盖 schema、migration、seed、repository、DTO 白名单、无套餐租户和无配额快照租户。
- 边界：
  - 未新增 API route。
  - 未新增 UI。
  - 未改权限、认证或租户隔离。
  - 未做套餐 enforcement、计费、支付、合同、发票或租户状态变更。

## Phase 9 PR 3：平台端租户只读 API

- 分支：`codex/phase9-platform-tenant-read-api`
- 目标：新增平台端租户管理只读 API，供后续平台端租户管理 UI 使用。
- 完成：
  - 新增 `GET /api/open-platform/tenants`。
  - 复用 tenant-management repository / domain。
  - `platform_admin` 可访问，未登录返回 401，非平台角色返回 403，`platform_operator` 按当前 RBAC 保守返回 403。
  - 数据服务异常返回稳定 503。
  - API 输出白名单 DTO，不返回客户 / 预约 / 随访明细、PII、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
- 边界：
  - 只暴露 GET。
  - 未新增 UI。
  - 未改数据库 schema / migration。
  - 未改权限、认证或租户隔离。
  - 未做租户创建 / 修改 / 删除 / 冻结 / 恢复、套餐 enforcement、计费、支付、合同或发票。

## Phase 9 PR 4：平台端租户管理 UI

- 分支：`codex/phase9-platform-tenant-management-ui`
- 目标：平台端「租户管理」入口接入 `GET /api/open-platform/tenants`。
- 完成：
  - 新增平台租户管理 client/helper。
  - 新增 `OpenPlatformTenantManagementPanel`。
  - 平台端“租户管理”导航接入真实租户 API。
  - UI 展示租户名称、状态、套餐名称 / code / 状态、配额上限、当前用量和 `snapshotAt`。
  - 覆盖 loading、empty、403、503、成功态和敏感字段不展示测试。
- 边界：
  - 未新增 API。
  - 未改数据库 schema / migration。
  - 未改权限、认证或租户隔离。
  - 未做租户创建 / 编辑 / 删除 / 冻结 / 恢复、套餐 enforcement、计费、支付、合同或发票。

## Phase 9 PR 5：smoke / 文档收尾

- 分支：`chore/phase9-tenant-management-closeout`
- 目标：强化平台端租户管理 smoke，更新 README、roadmap、devlog 和 Phase 9 spec / plan，标记 Phase 9 完成。
- 完成：
  - workspace entry smoke 覆盖平台端进入“租户管理”、请求 `GET /api/open-platform/tenants`、展示租户名称、状态、套餐、配额和当前用量。
  - smoke 覆盖租户管理 empty、403 和 503 稳定状态。
  - smoke 强化只读请求边界，确认不发送租户创建、编辑、删除、冻结 / 恢复、支付、计费或套餐 enforcement mutation。
  - smoke 确认不展示客户明细、预约明细、随访任务明细、治疗记录、病历正文、咨询对话、手机号原文、身份证号、病历号原文、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
  - README、roadmap、Phase 9 spec / plan 更新为 Phase 9 完成状态。
- Phase 9 最终状态：
  - 租户套餐 / 配额数据底座已完成。
  - 平台端租户只读 API 已完成。
  - 平台端租户管理 UI 已完成。
  - 平台端仅展示租户运营元数据、套餐 / 配额和聚合用量快照。
  - 未做租户创建、编辑、删除、冻结 / 恢复、套餐 enforcement、计费、支付、合同、发票或客户 / 预约 / 随访业务明细下钻。
  - 未进入治疗记录、AI / RAG / Agent、企微、OAuth、Webhook 或 API Key。
- 后续建议：
  - 进入 Phase 10 Plan Mode，重新评估治疗记录结构化摘要 v1、平台租户状态管理和状态变更审计、套餐权益 enforcement、知识库 / RAG 基础准备的优先级。

## Phase 10 PR 1：套餐配额 enforcement spec / plan 文档

- 分支：`docs/phase10-plan-quota-enforcement-light`
- 目标：固化 Phase 10 B-first 路线，优先做平台套餐配额 enforcement 轻量版。
- 完成：
  - 新增 Phase 10 design spec 和 implementation plan。
  - 明确只覆盖新增客户和新增预约两个会增加数量的写入。
  - 明确使用 active plan / quota limit 和按租户业务表 live count 判断，不把 `tenant_quota_snapshots.current*` 当强一致依据。
  - 明确无 active plan、无 quota limit fail closed，拒绝时返回稳定 `409` 并写 denied 审计。
- 边界：
  - 只做文档。
  - 未改业务代码、API、数据库、权限、认证或租户隔离。

## Phase 10 PR 2：quota enforcement 地基

- 分支：`codex/phase10-quota-enforcement-core`
- 目标：新增内部套餐配额 enforcement domain / repository / helper。
- 完成：
  - 新增客户和预约两类 `TenantQuotaResource`。
  - 读取当前租户 active plan / quota limit。
  - 按 `tenantId` 实时统计 `customers` 和 `appointments`。
  - 返回稳定 allowed / denied 结果，reason 包括 `quota_exceeded_customers`、`quota_exceeded_appointments`、`missing_active_plan`、`missing_quota_limit`。
  - 测试覆盖未超额、已达上限、无 active plan、无 quota limit、无 quota snapshot 但可解析 limit、租户隔离、PII / SQL / stack / token / secret 不泄露和不写业务表。
- 边界：
  - 未接入客户 / 预约 API。
  - 未改前端 UI。
  - 未新增 schema / migration。

## Phase 10 PR 3：客户 / 预约创建 API 接入 enforcement

- 分支：`codex/phase10-quota-enforcement-api`
- 目标：把套餐配额 enforcement 接入客户和预约创建 API。
- 完成：
  - `POST /api/institution/customers` 创建前执行客户数量配额校验。
  - `POST /api/institution/appointments` 创建前执行预约数量配额校验。
  - 超额、无 active plan、无 quota limit 时 fail closed，返回稳定中文 `409` 错误。
  - denied 时不写客户 / 预约业务表，并写 denied 审计。
  - API 测试覆盖不接受前端 `tenantId` 绕过配额、错误响应不泄露 SQL / stack / `DATABASE_URL` / token / secret。
  - 更新客户、更新预约、随访状态流转不受数量配额阻断。
- 边界：
  - 未改前端 UI。
  - 未改数据库 schema / migration。
  - 未改权限、认证或租户隔离模型。

## Phase 10 PR 4：前端错误态与 smoke

- 分支：`codex/phase10-quota-enforcement-ui`
- 目标：让客户中心和预约中心在套餐配额拒绝时展示稳定中文提示。
- 完成：
  - tenant business client 将稳定 quota reason 映射为安全中文提示。
  - 客户表单展示客户配额已满、无 active plan 的稳定提示。
  - 预约表单展示预约配额已满、无 quota limit 的稳定提示。
  - 失败后保留表单输入，前端请求 body 不发送 `tenantId`。
  - UI 和 client 测试确认不展示内部 reason 原始值、SQL、stack、`DATABASE_URL`、token、secret 或连接串。
- 边界：
  - 未改后端 enforcement。
  - 未新增 API。
  - 未新增套餐购买、变更、支付、合同或发票入口。

## Phase 10 PR 5：全量验证 / 文档收尾

- 分支：`chore/phase10-quota-enforcement-closeout`
- 目标：补充 Phase 10 smoke，更新 README、roadmap、devlog 和 Phase 10 spec / plan，标记 Phase 10 完成。
- 完成：
  - workspace entry smoke 增强客户创建配额错误态，确认稳定中文提示、表单输入保留、请求 body 不含 `tenantId`，且不展示内部 reason、SQL、stack、`DATABASE_URL`、token 或 secret。
  - workspace entry smoke 增强预约创建配额错误态，确认无 quota limit 稳定中文提示、表单输入保留、请求 body 不含 `tenantId`，且不展示敏感细节。
  - README、roadmap、Phase 10 spec / plan 更新为 Phase 10 完成状态。
  - Phase 10 最终验证覆盖客户创建未超额成功、客户创建超额拒绝、预约创建未超额成功、预约创建超额拒绝、无 active plan、无 quota limit、无 quota snapshot 但按 live count 判断、denied 不写业务表、denied 写审计、审计 reason 稳定、更新和随访状态流转不被阻断、前端稳定错误态和 PII / SQL / stack / token / secret 不泄露。
- Phase 10 最终状态：
  - 套餐 / 配额 enforcement 地基已完成。
  - 客户创建配额 enforcement 已完成。
  - 预约创建配额 enforcement 已完成。
  - 前端稳定错误态已完成。
  - smoke / 文档收尾已完成。
  - 未新增 schema / migration。
  - 未改权限、认证或租户隔离模型。
  - 未做套餐购买、套餐变更、续费、支付、合同、发票、租户冻结 / 恢复或完整套餐商业化后台。
  - 未进入治疗记录、AI / RAG / Agent、企微、OAuth、Webhook 或 API Key。
- 后续建议：
  - 进入 Phase 11 Plan Mode，优先重新评估治疗记录结构化摘要 v1、平台租户状态管理和状态变更审计、知识库 / RAG 基础准备；本阶段不进入 Phase 11 实现。

## Phase 11 PR 1：平台商业化健康 spec / plan 文档

- 分支：`docs/phase11-platform-commercial-health-plan`
- 目标：固化 Phase 11 C-first 路线，优先做平台套餐商业化管理增强 v1。
- 完成：
  - 新增 Phase 11 design spec 和 implementation plan。
  - 明确 Phase 11 v1 只做平台端只读运营辅助，不做完整商业化后台。
  - 明确治疗记录结构化摘要和知识库 / RAG 后置。
  - 明确复用现有 `GET /api/open-platform/tenants` 和 `GET /api/open-platform/audit-events`。
  - 明确 `tenant_quota_snapshots.current*` 是配额快照 / 运营参考，不是计费或 Phase 10 live enforcement 依据。
- 边界：
  - 只做文档。
  - 未改业务代码、页面、测试、API、数据库、权限、认证或租户隔离。

## Phase 11 PR 2：平台商业化健康 view model / client 派生逻辑

- 分支：`codex/phase11-commercial-health-view-model`
- 目标：基于现有平台租户 API 和平台审计 API 派生平台商业化健康指标。
- 完成：
  - 新增平台商业化健康 view model / domain 派生逻辑。
  - client helper 复用现有 tenants / audit-events 只读 API。
  - 派生套餐覆盖率、无 active plan 租户、配额快照风险、配置缺失、quota denied 审计信号。
  - 测试覆盖空态、风险阈值、已达上限、reason / resource 聚合、snapshot 运营参考口径和敏感字段不返回。
- 边界：
  - 未新增 API。
  - 未新增 UI。
  - 未新增 schema / migration。
  - 未改权限、认证、租户隔离或套餐 enforcement。

## Phase 11 PR 3：平台端商业化健康 UI 增强

- 分支：`codex/phase11-commercial-health-ui`
- 目标：在平台端租户管理中展示商业化健康摘要。
- 完成：
  - 平台端「租户管理」展示商业化健康摘要卡片。
  - 展示套餐覆盖率、无 active plan、配额风险项、配置缺失租户和近期 quota denied。
  - 展示配额风险租户、配置缺失租户、quota denied reason / resource 聚合。
  - 文案明确“只读运营辅助”“配额快照”“运营参考”，不把 snapshot current usage 描述为强一致 enforcement 或计费依据。
  - UI 测试覆盖 loading、empty、403、503、套餐覆盖、风险、配置缺失、quota denied 信号和敏感字段不展示。
- 边界：
  - 未新增 API。
  - 未改数据库 schema / migration。
  - 未改权限、认证、租户隔离或套餐 enforcement。
  - 未进入套餐购买 / 变更 / 续费、支付、合同、发票、治疗记录、AI / RAG / Agent、企业微信、OAuth 或 Webhook。

## Phase 11 PR 4：smoke / 文档收尾

- 分支：`chore/phase11-commercial-health-closeout`
- 目标：强化平台商业化健康入口 smoke，更新 README、roadmap、devlog 和 Phase 11 spec / plan，标记 Phase 11 完成。
- 完成：
  - workspace entry smoke 覆盖平台端进入“租户管理”后展示商业化健康摘要。
  - smoke 覆盖套餐覆盖率、配额风险、配置缺失租户、quota denied reason / resource 信号。
  - smoke 确认页面包含“运营参考 / 配额快照”边界，不把 snapshot current usage 描述成强一致 enforcement 依据。
  - smoke 确认不展示客户明细、预约明细、随访任务明细、治疗记录、病历正文、咨询对话、手机号原文、身份证号、病历号原文、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
  - README、roadmap、Phase 11 spec / plan 更新为 Phase 11 完成状态。
- Phase 11 最终状态：
  - 平台商业化健康派生逻辑已完成。
  - 平台商业化健康 UI 展示已完成。
  - smoke / 文档收尾已完成。
  - 未新增 API。
  - 未新增数据库 schema / migration。
  - 未改权限、认证或租户隔离模型。
  - 未改 Phase 10 enforcement。
  - 未做套餐购买、套餐变更、续费、支付、合同、发票、租户冻结 / 恢复、自动升级套餐、自动触达客户或租户。
  - 未进入治疗记录、AI / RAG / Agent、企业微信、OAuth、Webhook 或 API Key。
- 后续建议：
  - 进入 Phase 12 Plan Mode，重新评估治疗记录结构化摘要 v1、平台租户状态管理和状态变更审计、更多资源配额 enforcement、知识库 / RAG 基础准备、审计高级治理；当前不进入 Phase 12 实现。

## Phase 12 PR 1：治疗记录结构化摘要 v1 spec / plan 文档

- 分支：`docs/phase12-treatment-summary-plan`
- 目标：固化 Phase 12 A-first 路线，优先做治疗记录结构化摘要 v1。
- 完成：
  - 新增 Phase 12 design spec 和 implementation plan。
  - 明确 Phase 12 只做治疗结构化摘要，不做完整治疗记录正文、完整病历正文或咨询对话全文。
  - 明确知识库 / RAG 和平台套餐商业化继续增强后置。
  - 明确字段白名单、禁止字段、API 策略、schema / migration 策略、租户隔离、复合外键、DTO 脱敏边界和 PR 拆分。
- 边界：
  - 只做文档。
  - 未改业务代码、页面、测试、API、数据库、权限、认证或租户隔离。

## Phase 12 PR 2：治疗结构化摘要数据底座

- 分支：`codex/phase12-treatment-summary-schema`
- 目标：新增治疗记录结构化摘要的最小数据底座。
- 完成：
  - 新增 `treatment_summaries` schema、Drizzle migration 和 meta。
  - 新增 demo 治疗摘要 seed，内容为结构化摘要，不包含真实敏感信息或病历正文。
  - 新增 treatment summary domain、repository 和 DTO 白名单 mapper。
  - repository 按 `tenantId + customerId` 查询，`appointmentId` 保持同租户约束。
  - 测试覆盖 schema / migration / seed / repository / DTO 白名单、租户隔离、跨租户不返回和敏感字段不保存 / 不返回。
- 边界：
  - 未新增 API。
  - 未新增 UI。
  - 未改权限、认证或租户隔离模型。
  - 未保存完整治疗记录正文、完整病历正文、诊疗原文或咨询对话全文。

## Phase 12 PR 3：治疗摘要接入客户详情 timeline API

- 分支：`codex/phase12-treatment-summary-timeline-api`
- 目标：把治疗结构化摘要接入现有 `GET /api/institution/customers/[customerId]/timeline`。
- 完成：
  - customer timeline response 增加 `treatmentSummaries`。
  - `timeline` 增加 `type: "treatment_summary"` 事件。
  - 治疗事件按时间与客户、预约、随访和审计事件统一排序。
  - route 按当前访问上下文的 `tenantId + customerId` 查询治疗摘要。
  - 测试覆盖未登录 401、无权限 403、跨租户客户 404、数据异常 503、DTO 白名单和敏感字段不返回。
- 边界：
  - 未新增独立治疗 API。
  - 未新增 UI。
  - 未改数据库 schema / migration。
  - 未改权限、认证或租户隔离模型。

## Phase 12 PR 4：客户详情抽屉展示治疗摘要

- 分支：`codex/phase12-treatment-summary-ui`
- 目标：在客户详情抽屉 / 时间线 UI 中展示治疗结构化摘要。
- 完成：
  - 客户详情抽屉新增“治疗结构化摘要”区域。
  - 展示治疗时间、治疗项目、治疗类别、治疗阶段、恢复阶段、风险等级、负责人、摘要、下一步护理建议和标签。
  - 结构化时间线展示 `treatment_summary` 节点，并展示风险和标签。
  - 空治疗摘要时展示稳定空态。
  - UI 测试确认不展示完整治疗记录正文、完整病历正文、咨询对话全文、手机号原文、身份证号、病历号原文、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
- 边界：
  - 使用现有 timeline API。
  - 未新增 API。
  - 未修改 timeline API。
  - 未改数据库 schema / migration。
  - 未新增治疗写入 UI。

## Phase 12 PR 5：smoke / 文档收尾

- 分支：`chore/phase12-treatment-summary-closeout`
- 目标：强化客户详情治疗摘要入口 smoke，更新 README、roadmap、devlog 和 Phase 12 spec / plan，标记 Phase 12 完成。
- 完成：
  - workspace entry smoke 覆盖机构端进入客户中心、打开客户详情抽屉、展示治疗结构化摘要和 `treatment_summary` 时间线节点。
  - smoke 覆盖治疗时间、项目、类别、阶段、恢复阶段、风险等级、负责人、摘要、下一步护理建议和标签。
  - smoke 覆盖无治疗摘要稳定空态。
  - smoke 确认不展示手机号原文、身份证号、病历号原文、完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文、图片 / 文件原文、AI 生成内容、外部系统同步原文、请求体、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
  - README、roadmap、Phase 12 spec / plan 更新为 Phase 12 完成状态。
- Phase 12 最终状态：
  - 治疗结构化摘要数据底座已完成。
  - 治疗摘要接入 customer timeline API 已完成。
  - 客户详情抽屉展示治疗摘要已完成。
  - smoke / 文档收尾已完成。
  - 未新增独立治疗 API。
  - 未新增治疗写入 UI。
  - 未改权限、认证或租户隔离模型。
  - 未进入完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文。
  - 未进入 AI provider、AI 生成治疗建议、Agent、RAG、企微、HIS / CRM / OTA、OAuth、Webhook、支付、合同、发票或外部系统同步。
- 后续建议：
  - 当时建议进入 Phase 13 Plan Mode；Phase 13、Phase 14、Phase 15 和 Phase 16 现已完成，后续建议进入 Phase 17 Plan Mode。

## Phase 13 PR 1：治疗摘要人工录入 v1 spec / plan

- 分支：`docs/phase13-treatment-summary-create-plan`
- 目标：固化 Phase 13 A-first 路线，优先做治疗摘要人工录入 v1。
- 完成：
  - 新增 Phase 13 design spec 和 implementation plan。
  - 明确治疗摘要人工录入承接 Phase 12 数据底座和客户详情 timeline。
  - 明确治疗摘要管理、知识库 / RAG 和平台商业化增强后置。
  - 明确字段白名单、禁止字段、POST API 路径、RBAC 推荐方案、审计设计、appointment 校验、payload parser、医疗隐私拦截、租户隔离和 PR 拆分。
- 边界：
  - 只做文档。
  - 未改业务代码、页面、测试、API、数据库、权限、认证或租户隔离。

## Phase 13 PR 2：治疗摘要写入地基

- 分支：`codex/phase13-treatment-summary-create-core`
- 目标：为治疗摘要人工录入提供 parser、domain、repository create、RBAC 和 audit 决策地基。
- 完成：
  - 新增治疗摘要 create payload parser，限制结构化字段白名单。
  - parser 拒绝 `tenantId`、未知字段、完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文、手机号原文、身份证号、病历号原文、图片 / 文件原文、AI 生成内容、外部系统原文和敏感错误细节。
  - 新增 create domain input 和 repository create 方法，写入使用服务端确认的 `tenantId` / `customerId`。
  - 增加 appointmentId 同租户 / 同客户校验 helper。
  - 新增 `treatment_summary` access resource，`tenant_admin` 具备必要 create / read 权限，未扩大 update / delete。
  - 新增 audit reason `invalid_treatment_summary_reference`，补齐 RBAC、audit domain、audit query parser、repository 和 parser 测试。
- 边界：
  - 未新增 API route。
  - 未新增 UI。
  - 未新增数据库 schema / migration。
  - 未改认证或租户隔离模型。

## Phase 13 PR 3：治疗摘要创建 API

- 分支：`codex/phase13-treatment-summary-create-api`
- 目标：新增机构端治疗摘要人工录入服务端 API。
- 完成：
  - 新增 `POST /api/institution/customers/[customerId]/treatment-summaries`。
  - 服务端从 access context 推导 tenant，不接受前端 `tenantId`。
  - 校验 `treatment_summary:create`、customer 当前租户归属、appointment 当前租户归属和 appointment 同客户归属。
  - 使用 Phase 13 PR 2 payload parser 和 repository create。
  - 成功返回安全 DTO，并写 allowed audit。
  - 覆盖 401 / 403 / 400 / 404 / 409 / 503，拒绝 / not_found / invalid reference / invalid payload 写稳定 audit reason。
  - 错误响应不泄露 SQL、stack、token、secret、`DATABASE_URL` 或连接串。
- 边界：
  - 未新增 UI。
  - 未修改 timeline API。
  - 未新增数据库 schema / migration。
  - 未改权限模型主结构、认证或租户隔离。

## Phase 13 PR 4：客户详情抽屉结构化录入 UI

- 分支：`codex/phase13-treatment-summary-create-ui`
- 目标：在客户详情抽屉新增“添加治疗摘要”结构化录入能力。
- 完成：
  - 客户详情抽屉新增“添加治疗摘要”入口和结构化录入表单。
  - 表单只提交 `treatmentDate`、`treatmentProject`、`treatmentCategory`、`treatmentStage`、`recoveryStage`、`riskLevel`、`ownerUserId`、`summary`、`nextCareAction`、`tags` 和可选 `appointmentId`。
  - 新增 `createTreatmentSummary(customerId, payload)` client helper，只 pick 白名单字段。
  - 提交成功后刷新客户详情 timeline，新摘要显示在治疗摘要区域和 `treatment_summary` 时间线节点。
  - 400 / 401 / 403 / 404 / 409 / 503 错误态展示稳定中文提示，失败后保留输入。
  - UI 和 client 测试确认请求 body 不含 `tenantId`、未知字段、PII、完整正文、图片 / 文件原文、AI 生成内容或外部系统原文。
- 边界：
  - 使用既有 POST API。
  - 未新增 API。
  - 未改数据库 schema / migration。
  - 未改权限、认证或租户隔离。

## Phase 13 PR 5：smoke / 文档收尾

- 分支：`chore/phase13-treatment-summary-create-closeout`
- 目标：补充治疗摘要录入入口 smoke，更新 README、roadmap、devlog 和 Phase 13 spec / plan，标记 Phase 13 完成。
- 完成：
  - workspace entry smoke 覆盖机构端进入客户中心、打开客户详情抽屉、打开“添加治疗摘要”表单、展示结构化字段、提交成功后刷新 timeline。
  - smoke 确认新治疗摘要出现在治疗摘要区域和 `treatment_summary` 时间线节点。
  - smoke 确认失败后表单输入保留，错误态不展示 SQL、stack、token、secret、`DATABASE_URL` 或连接串。
  - smoke 确认请求 body 不含 `tenantId`、`customerId`、未知字段、手机号原文、身份证号、病历号原文、完整治疗记录正文、完整病历正文、咨询对话全文、图片 / 文件原文、AI 生成内容或外部系统原文。
  - README、roadmap、Phase 13 spec / plan 更新为 Phase 13 完成状态。
- Phase 13 最终状态：
  - 治疗摘要写入 payload parser 已完成。
  - 治疗摘要 repository create 已完成。
  - `POST /api/institution/customers/[customerId]/treatment-summaries` 已完成。
  - 客户详情抽屉结构化录入 UI 已完成。
  - smoke / 文档收尾已完成。
  - 未新增数据库 schema / migration。
  - 未改认证或租户隔离模型。
  - 未进入完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文。
  - 未进入图片 / 文件上传、AI provider、AI 生成治疗建议、Agent、RAG、企微、HIS / CRM / OTA、OAuth、Webhook、支付、合同、发票或外部系统同步。
- 后续建议：
  - 当时建议进入 Phase 14 Plan Mode；Phase 14、Phase 15 和 Phase 16 现已完成，后续建议进入 Phase 17 Plan Mode，重新评估治疗摘要编辑 / 作废、follow-up 配额 enforcement、知识库 / RAG 安全基础准备、平台商业化增强、平台租户状态管理和审计高级治理。

## Phase 14 PR 1：治疗摘要管理能力 v1 spec / plan

- 分支：`docs/phase14-treatment-summary-management-plan`
- 目标：固化 Phase 14 A-first 路线，优先做治疗摘要管理能力 v1。
- 完成：
  - 新增 Phase 14 design spec 和 implementation plan。
  - 明确 Phase 14 承接 Phase 12 治疗摘要读路径和 Phase 13 人工录入闭环。
  - 明确治疗摘要管理 v1 范围：只读列表、白名单筛选、分页、安全详情和当前租户范围查询。
  - 明确治疗后护理 / 随访联动、RAG、平台商业化增强、AI、企微和外部系统后置。
  - 明确默认不新增 schema / migration，复用现有 `treatment_summaries`。
- 边界：
  - 只做文档。
  - 未改业务代码、页面、测试、API、数据库、权限、认证或租户隔离。

## Phase 14 PR 2：治疗摘要列表 API

- 分支：`codex/phase14-treatment-summary-list-api`
- 目标：新增机构端治疗摘要只读列表 API。
- 完成：
  - 新增治疗摘要列表 query parser，只接受 `customerId`、`treatmentProject`、`riskLevel`、`from`、`to`、`limit`、`cursor`。
  - 新增 cursor / limit 校验，拒绝 `tenantId`、未知参数、重复参数和敏感筛选内容。
  - 新增治疗摘要列表领域类型、DTO 白名单 mapper 和 repository `listTreatmentSummariesByTenant`。
  - 新增 `GET /api/institution/treatment-summaries`，服务端从 access context 推导 `tenantId`，只查询当前租户。
  - API 返回 `{ records, pageInfo }`，不返回 `tenantId`、客户 / 预约 / 随访明细、完整正文、PII、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
- 边界：
  - 未新增 UI。
  - 未新增数据库 schema / migration。
  - 未改权限、认证或租户隔离。
  - 未改治疗摘要 POST API、customer timeline API 或客户详情抽屉。

## Phase 14 PR 3：机构端治疗摘要管理 UI

- 分支：`codex/phase14-treatment-summary-management-ui`
- 目标：新增机构端治疗摘要管理页面，接入既有 `GET /api/institution/treatment-summaries`。
- 完成：
  - 新增 `listTreatmentSummaries()` client helper，只拼接白名单 query，不发送 `tenantId`。
  - 新增 `TreatmentSummaryManagementShell`，支持治疗摘要列表、基础筛选、加载更多和安全详情查看。
  - 机构端新增「治疗摘要管理」导航入口，并标记为已接入页面。
  - UI 覆盖 loading、empty、403、503、成功列表、筛选提交、加载更多、安全详情打开 / 关闭和敏感字段不展示。
  - workspace smoke 覆盖机构入口进入治疗摘要管理并读取列表。
- 边界：
  - 使用既有 GET API。
  - 未新增 API。
  - 未改数据库 schema / migration。
  - 未改权限、认证或租户隔离。
  - 未进入治疗摘要新增 / 编辑 / 删除、完整正文展示、AI、RAG、企微或外部系统。

## Phase 14 PR 4：smoke / 文档收尾

- 分支：`chore/phase14-treatment-summary-management-closeout`
- 目标：强化治疗摘要管理入口 smoke，更新 README、roadmap、devlog 和 Phase 14 spec / plan，标记 Phase 14 完成。
- 完成：
  - workspace entry smoke 覆盖机构端进入治疗摘要管理、请求 `GET /api/institution/treatment-summaries`、展示治疗摘要列表。
  - smoke 覆盖按客户 ID、治疗项目、风险等级和时间范围筛选，确认只发送白名单 query 且不包含 `tenantId`。
  - smoke 覆盖加载更多和安全详情查看，确认只读请求不发送 POST / PATCH / DELETE。
  - smoke 确认不展示 `tenantId`、客户 / 预约 / 随访明细、完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文、手机号原文、身份证号、病历号原文、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
  - README、roadmap、Phase 14 spec / plan 更新为 Phase 14 完成状态。
- Phase 14 最终状态：
  - Phase 14 spec / plan 已完成。
  - 治疗摘要列表 query parser、repository list、DTO 白名单和 `GET /api/institution/treatment-summaries` 已完成。
  - 机构端治疗摘要管理 UI 已完成，支持列表、筛选、加载更多和安全详情查看。
  - smoke / 文档收尾已完成。
  - 未新增数据库 schema / migration。
  - 未改权限、认证或租户隔离模型。
  - 未进入治疗摘要新增 / 编辑 / 删除、完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文、图片 / 文件上传。
  - 未进入 AI provider、AI 生成治疗建议、Agent、RAG、企微、HIS / CRM / OTA、OAuth、Webhook、支付、合同、发票或外部系统同步。
- 后续建议：
  - Phase 15 和 Phase 16 现已完成；后续建议进入 Phase 17 Plan Mode，重新评估治疗摘要编辑 / 作废、follow-up 配额 enforcement、知识库 / RAG 安全基础准备、平台商业化增强、平台租户状态管理和审计高级治理；当前不进入 Phase 17 实现。

## Phase 15 PR 1：治疗后护理 / 随访联动 v1 spec / plan

- 分支：`docs/phase15-treatment-followup-link-plan`
- 目标：固化 Phase 15 A-first 路线，优先做治疗后护理 / 随访联动 v1。
- 完成：
  - 新增 Phase 15 design spec 和 implementation plan。
  - 明确 Phase 15 基于结构化治疗摘要生成确定性护理 / 随访建议。
  - 明确 v1 支持“机构人员人工确认后创建结构化随访任务”，不自动创建。
  - 明确治疗摘要编辑、作废、知识库 / RAG 和平台商业化增强后置。
- 边界：
  - 只做文档。
  - 未改业务代码、页面、测试、API、数据库、权限、认证或租户隔离。
  - 不进入 AI provider、AI 生成护理建议、Agent、RAG、企微、短信、电话外呼、自动触达客户或外部系统。

## Phase 15 PR 2：确定性护理 / 随访建议规则

- 分支：`codex/phase15-treatment-followup-suggestions`
- 目标：基于治疗摘要结构化字段生成确定性的护理 / 随访任务建议。
- 完成：
  - 新增治疗后护理 / 随访建议 domain、parser / mapper 和 suggestion DTO。
  - 基于 `riskLevel`、`recoveryStage`、`treatmentStage`、`nextCareAction`、`treatmentCategory`、`treatmentProject`、`treatmentDate`、`tags` 生成稳定建议。
  - 新增稳定 `suggestionKey`，不包含 PII、正文或自由文本原文。
  - 测试覆盖高风险、恢复早期、`nextCareAction`、分类规则、信息不足、key 稳定性、敏感字段不输出、不调用 AI / RAG / Agent、不写数据库。
- 边界：
  - 未新增 API route。
  - 未新增 UI。
  - 未创建随访任务。
  - 未改数据库 schema / migration。
  - 未接 AI / RAG / Agent、企微、短信、电话或外部系统。

## Phase 15 PR 3：随访来源关联地基

- 分支：`codex/phase15-followup-source-link-schema`
- 目标：为“治疗摘要建议 -> 人工确认创建随访任务”提供来源追溯和 repository 创建能力。
- 完成：
  - `follow_up_tasks` 新增 `source_treatment_summary_id` 和 `source_suggestion_key` 来源字段。
  - 新增 Drizzle migration / meta，历史任务兼容 nullable 来源字段。
  - 新增 repository create 能力，由服务端传入 `tenantId`、确认 `customerId`、确认来源治疗摘要和 `sourceSuggestionKey`。
  - 来源幂等 / 去重按同一 `tenantId + sourceTreatmentSummaryId + sourceSuggestionKey` 判断，不跨租户。
  - 测试覆盖来源字段、创建保存、重复冲突、不同租户隔离、不同 suggestionKey、终态重建策略、跨租户拒绝和安全 DTO。
- 边界：
  - 未新增 API route。
  - 未新增 UI。
  - 未自动创建随访任务。
  - 未自动触达客户。
  - 未改权限、认证、租户隔离或 Phase 10 quota enforcement。
  - 未保存完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文、AI 生成内容或外部系统原文。

## Phase 15 PR 4：人工确认 API + 治疗摘要管理 UI 联动

- 分支：`codex/phase15-followup-confirm-api-ui`
- 目标：实现“治疗摘要 -> 确定性随访建议 -> 人工确认创建随访任务”闭环。
- 完成：
  - 新增 `GET /api/institution/treatment-summaries/[summaryId]/follow-up-suggestions`，只读返回确定性建议，不写数据库、不创建任务。
  - 新增 `POST /api/institution/treatment-summaries/[summaryId]/follow-up-tasks`，只接受 `suggestionKey`，服务端重新计算建议并人工确认创建结构化随访任务。
  - 新增服务端确认 / 审计封装，成功、重复、非法、not_found 等路径写稳定 audit。
  - 新增 client helper，GET 不发送 `tenantId`，POST 只发送 `suggestionKey`。
  - 治疗摘要管理 UI 增加查看随访建议、人工确认创建、成功提示和重复确认冲突提示。
  - API / UI / workspace smoke 测试覆盖 401 / 403 / 404 / 409 / 503、跨租户、DTO 白名单、来源关联、敏感字段不展示和不自动触达边界。
- 边界：
  - 未自动创建随访任务。
  - 未自动触达客户。
  - 未改数据库 schema / migration。
  - 未改权限、认证、租户隔离或 quota enforcement。
  - 未接 AI / RAG / Agent、企微、短信、电话或外部系统。

## Phase 15 PR 5：workspace smoke / 文档收尾

- 分支：`chore/phase15-treatment-followup-link-closeout`
- 目标：强化治疗摘要随访联动入口 smoke，更新 README、roadmap、devlog 和 Phase 15 spec / plan，标记 Phase 15 完成。
- 完成：
  - workspace entry smoke 覆盖机构端进入治疗摘要管理、打开治疗摘要安全详情、查看随访建议、展示“人工确认”边界、人工确认创建随访任务。
  - workspace entry smoke 覆盖重复确认时展示稳定冲突提示。
  - smoke 确认创建请求只发送 `suggestionKey`，不发送 `tenantId`、`customerId` 或 `suggestedAction`。
  - smoke 确认不展示手机号原文、身份证号、病历号原文、完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文、图片 / 文件原文、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
  - README、roadmap、Phase 15 spec / plan 更新为 Phase 15 完成状态。
- Phase 15 最终状态：
  - 确定性护理 / 随访建议规则已完成。
  - `follow_up_tasks` 来源关联和幂等 / 去重已完成。
  - 人工确认 API 已完成。
  - 治疗摘要管理 UI 联动已完成。
  - workspace smoke / 文档收尾已完成。
  - 未进入 AI provider、AI 生成护理建议、Agent、RAG、企微、短信、电话外呼、自动触达客户、HIS / CRM / OTA、OAuth、Webhook、支付、完整治疗记录正文、完整病历正文、咨询对话全文、图片 / 文件原文或外部系统同步。
- 后续建议：
  - Phase 16 现已完成；后续进入 Phase 17 Plan Mode，重新评估治疗摘要编辑能力 v1、治疗摘要作废能力 v1、follow-up 配额 enforcement、知识库 / RAG 安全基础准备、平台商业化增强、平台租户状态管理和审计高级治理；当前不进入 Phase 17 实现。

## Phase 16 PR 1：随访任务来源治理增强 v1 spec / plan

- 分支：`docs/phase16-followup-source-governance-plan`
- 目标：固化 Phase 16 默认方向，优先做“随访任务创建后的治理增强 v1”。
- 完成：
  - 新增 Phase 16 design spec 和 implementation plan。
  - 明确优先承接 Phase 15 的治疗摘要来源随访任务，补齐创建后的来源查看、来源筛选和重复任务提示。
  - 明确治疗摘要编辑、治疗摘要作废、知识库 / RAG 后置。
  - 明确 Phase 16 v1 默认不新增 schema / migration，复用 Phase 15 已完成的 `follow_up_tasks` 来源字段。
- 边界：
  - 只做文档。
  - 未改业务代码、页面、测试、API、数据库、权限、认证或租户隔离。
  - 未进入自动创建、自动触达、AI / RAG / Agent、企微、短信、电话或外部系统。

## Phase 16 PR 2：后端 follow-up 来源筛选、DTO、安全查询、API 测试

- 分支：`codex/phase16-followup-source-filter-api`
- 目标：扩展现有 `GET /api/institution/followups`，支持治疗摘要来源随访任务的安全查询。
- 完成：
  - 新增 `source=treatment_summary` 和 `sourceTreatmentSummaryId` 查询白名单。
  - 服务端继续从 access context 推导 `tenantId`，拒绝前端传入 `tenantId`、未知参数和未定义 `source`。
  - repository 查询始终绑定当前租户，跨租户或不存在来源返回稳定空结果。
  - follow-up DTO 返回安全来源字段 `source`、`sourceTreatmentSummaryId` 和 `sourceSuggestionKey`。
  - API route、repository 和 client helper 测试覆盖来源筛选、租户隔离、安全 DTO、401 / 403 / 503 和敏感字段不展示。
- 边界：
  - 未新增 UI。
  - 未新增 API route。
  - 未改数据库 schema / migration。
  - 未改权限、认证或租户隔离模型。
  - 未改 Phase 15 人工确认创建逻辑。

## Phase 16 PR 3：机构端随访来源展示和重复任务提示

- 分支：`codex/phase16-followup-source-ui`
- 目标：在机构端前端展示 Phase 16 PR 2 的随访来源治理能力。
- 完成：
  - 智能随访列表展示“来源：治疗摘要”、来源摘要 ID 和建议 key。
  - 智能随访列表增加“全部来源 / 治疗摘要来源”筛选，调用现有 `GET /api/institution/followups?source=treatment_summary`。
  - 治疗摘要详情页加载建议后查询同来源随访任务。
  - 存在同来源活跃任务时展示只读重复提示，并禁用重复创建。
  - institution / workspace 前端测试覆盖来源标签、来源筛选、duplicate hint、不自动创建、不自动触达和敏感字段不展示。
- 边界：
  - 未新增 API。
  - 未改数据库 schema / migration。
  - 未改权限、认证或租户隔离。
  - 未改随访任务创建逻辑。
  - 未进入 AI / RAG / Agent、企微、短信、电话或外部系统。

## Phase 16 PR 4：smoke / 文档收尾

- 分支：`chore/phase16-followup-source-governance-closeout`
- 目标：强化随访来源治理入口 smoke，更新 README、roadmap、devlog 和 Phase 16 spec / plan，标记 Phase 16 完成。
- 完成：
  - workspace entry smoke 覆盖机构端进入智能随访、展示“来源：治疗摘要”、展示来源摘要 ID 和建议 key。
  - smoke 覆盖“全部来源 / 治疗摘要来源”切换，请求 `GET /api/institution/followups?source=treatment_summary` 且不包含 `tenantId`。
  - smoke 覆盖治疗摘要管理页加载建议后识别同来源活跃随访任务，展示只读重复任务提示，并禁用重复创建。
  - smoke 确认不自动创建随访任务、不自动触达客户，不出现“自动发送微信 / 自动短信 / 电话外呼 / 自动触达”等文案。
  - smoke 确认不展示手机号原文、身份证号、病历号原文、完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文、图片 / 文件原文、AI 生成内容、外部系统同步原文、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
  - README、roadmap、Phase 16 spec / plan 更新为 Phase 16 完成状态，并给出 Phase 17 Plan Mode 建议。
- Phase 16 最终状态：
  - follow-up 来源筛选已完成。
  - 安全来源 DTO 已完成。
  - 智能随访来源标签展示已完成。
  - 治疗摘要来源筛选已完成。
  - 治疗摘要管理页重复任务提示已完成。
  - smoke / 文档收尾已完成。
  - 未进入治疗摘要编辑、治疗摘要作废、自动创建随访任务、自动触达客户、企业微信、短信、电话外呼、AI provider、Agent、RAG、HIS / CRM / OTA、OAuth、Webhook、支付、完整治疗记录正文、完整病历正文、咨询对话全文、图片 / 文件原文或外部系统同步。
- 后续建议：
  - 进入 Phase 17 Plan Mode，优先重新评估治疗摘要编辑能力 v1；治疗摘要作废、follow-up 配额 enforcement 和知识库 / RAG 安全基础准备继续单独规划，当前不进入 Phase 17 实现。

## Phase 17 PR 1：HIS 标准治疗事件 spec / plan 文档

- 分支：`docs/phase17-his-treatment-event-standard-plan`
- 目标：固化 Phase 17 默认方向，优先做“HIS 接入标准模型 / 治疗事件标准化 v1”。
- 完成：
  - 新增 Phase 17 design spec 和 implementation plan。
  - 明确标准治疗事件是未来 HIS / 导入 / 外部系统进入智美天工后的内部标准化事件，不是数据库 schema。
  - 明确 HIS 字段必须经过 adapter 映射为智美天工标准治疗事件，再进入治疗摘要 / 路径引擎 / 随访任务 / 经营分析。
  - 明确字段白名单、禁止字段、租户隔离、PII / 医疗隐私边界，以及与 `treatment_summaries`、预约、客户、随访任务和客户时间线的关系。
- 边界：
  - 只做文档。
  - 未改业务代码、页面、测试、API、数据库、权限、认证或租户隔离。
  - 未进入真实 HIS 接入、Webhook、文件导入、外部系统同步、AI / RAG / Agent、企微、个人微信或自动触达。

## Phase 17 PR 2：domain-only 标准治疗事件类型与测试

- 分支：`codex/phase17-his-treatment-event-domain`
- 目标：用 TypeScript 锁定标准治疗事件 domain-only 契约，为后续 HIS adapter、路径引擎、身份匹配和经营智能提供统一数据模型。
- 完成：
  - 新增标准治疗事件 domain 类型。
  - 新增 `sourceSystem` 稳定集合：`his`、`manual`、`import`、`other`。
  - 新增 `treatmentStatus`、`riskLevel` 等稳定集合。
  - 新增 mapper 输入 / 输出契约，输出由服务端可信上下文提供 `tenantId`、`eventId` 和 `receivedAt`。
  - 新增字段白名单和禁止字段边界。
  - institution 测试覆盖合法输入、`sourceSystem`、`sourceEventId`、`customerMatchKey`、`maskedPhone`、治疗项目 / 分类 / 阶段、金额 / 币种、tags 标准化、未知字段拒绝和禁止字段拒绝。
- 边界：
  - 未新增 API route。
  - 未新增数据库 schema / migration。
  - 未新增 repository。
  - 未改 UI、权限、认证或租户隔离。
  - mapper 不接受外部 `tenantId`，不接受 raw payload，不保存外部系统原始数据。
  - 不自动生成或修改 `treatment_summaries`。
  - 未进入真实 HIS、Webhook、文件导入、外部系统同步、AI / RAG / Agent、企微、个人微信、自动触达、业务事件埋点实现或经营智能中心实现。
- 验证：
  - `git diff --check`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests`
  - `./node_modules/.bin/tsc --noEmit`
  - `node scripts/run-next.mjs build --webpack`
  - `node scripts/run-vitest.mjs run`

## Phase 17 PR 3：文档收尾

- 分支：`chore/phase17-his-treatment-event-standard-closeout`
- 目标：更新 README、roadmap、devlog 和 Phase 17 spec / plan，标记 Phase 17 完成。
- 完成：
  - README、roadmap、Phase 17 spec / plan 更新为 Phase 17 完成状态。
  - 明确 Phase 17 已完成 HIS 标准治疗事件 spec / plan、domain-only 类型、`sourceSystem` 稳定集合、mapper 契约、字段白名单、禁止字段边界和 institution 测试。
  - 明确外部 `tenantId` 不可信，mapper 不接受 raw payload，不自动生成或修改 `treatment_summaries`。
  - 明确 Phase 17 未进入真实 HIS、Webhook、文件导入、外部系统同步、API route、数据库 schema / migration、UI、企业微信 / 个人微信、AI / RAG / Agent、业务事件埋点实现或经营智能中心实现。
- Phase 17 最终状态：
  - HIS 接入标准模型 / 标准治疗事件 v1 已完成。
  - domain-only 标准治疗事件类型和 mapper 契约已完成。
  - 字段白名单、禁止字段、租户隔离和 PII / 医疗隐私边界已完成。
  - 现阶段仍不代表真实 HIS 接入完成。
- 后续建议：
  - 当时建议进入 Phase 18 Plan Mode；Phase 18 现已完成治疗摘要编辑能力 v1。
  - HIS 标准治疗事件 mapper 继续增强和业务事件埋点体系 spec 仍可作为 Phase 19 候选方向，进入实现前需单独规划。

## Phase 18 PR 1：治疗摘要编辑能力 v1 spec / plan 文档

- 分支：`docs/phase18-treatment-summary-edit-plan`
- 目标：固化 Phase 18 默认方向，优先做“治疗摘要编辑能力 v1”。
- 完成：
  - 新增 Phase 18 design spec 和 implementation plan。
  - 明确编辑能力只允许白名单结构化字段，不允许完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文、图片 / 文件原文、AI 生成内容或外部系统原文。
  - 明确 `PATCH /api/institution/treatment-summaries/[summaryId]`、payload parser、RBAC、审计、`appointmentId` 归属校验、随访任务关系、schema / migration 决策和 PR 拆分。
- 边界：
  - 只做文档。
  - 未改业务代码、页面、测试、API、数据库、权限、认证或租户隔离。
  - 未进入治疗摘要删除 / 作废、版本历史、diff 展示、AI / RAG / Agent、企微、真实 HIS / CRM / OTA 接入或外部系统同步。

## Phase 18 PR 2：治疗摘要编辑地基

- 分支：`codex/phase18-treatment-summary-edit-core`
- 目标：为后续 PATCH API 提供 parser、RBAC、repository update 和审计决策地基。
- 完成：
  - 新增 `parseUpdateTreatmentSummaryPayload` 和 `UpdateTreatmentSummaryDraft`。
  - 编辑 payload 只接受 `treatmentDate`、`treatmentProject`、`treatmentCategory`、`treatmentStage`、`recoveryStage`、`riskLevel`、`ownerUserId`、`summary`、`nextCareAction`、`tags`、`appointmentId`。
  - parser 拒绝未知字段、外部 `tenantId` / `customerId` / `id` / `createdAt` / `updatedAt`、完整正文、PII、图片 / 文件原文、AI 生成内容、外部系统 payload、SQL、stack、token、secret、`DATABASE_URL` 和连接串。
  - 为 `tenant_admin` 增加最小 `treatment_summary:update` 权限，未开放 `treatment_summary:delete`。
  - 新增 `updateTreatmentSummaryByTenant`，按服务端确认的 `tenantId + summaryId` 更新，`appointmentId` 更新前校验同租户且属于同一 customer。
  - parser、repository、RBAC 和 audit 决策测试已覆盖旧权限不回退和敏感字段边界。
- 边界：
  - 未新增 API route。
  - 未新增 UI。
  - 未改数据库 schema / migration。
  - 未改认证或租户隔离。
  - 未进入治疗摘要删除 / 作废、版本历史、diff 展示、AI / RAG / HIS / 企微或外部系统。

## Phase 18 PR 3：治疗摘要编辑 PATCH API

- 分支：`codex/phase18-treatment-summary-edit-api`
- 目标：新增机构端治疗摘要编辑服务端 API。
- 完成：
  - 新增 `PATCH /api/institution/treatment-summaries/[summaryId]`。
  - 服务端从 access context 推导 `tenantId`，不接受前端传入 `tenantId`。
  - API 使用 Phase 18 PR 2 的 update parser 和 repository update，校验 `treatment_summary:update` 权限。
  - summary 必须属于当前 tenant；传入 `appointmentId` 时必须同租户且属于当前 summary 的 customer。
  - 编辑成功返回安全 DTO，并写 `treatment_summary/update allowed` audit。
  - API 测试覆盖 400 / 401 / 403 / 404 / 409 / 503、跨租户、敏感字段拒绝、错误响应脱敏和审计 reason 稳定。
- 边界：
  - 未新增 UI。
  - 未改数据库 schema / migration。
  - 未改权限模型、认证或租户隔离。
  - 未自动修改既有随访任务，未重新生成随访建议。
  - 未进入删除 / 作废、版本历史、diff 展示、AI / RAG / HIS / 企微或外部系统。

## Phase 18 PR 4：机构端治疗摘要编辑 UI

- 分支：`codex/phase18-treatment-summary-edit-ui`
- 目标：在机构端治疗摘要管理页 / 安全详情中提供受控编辑入口。
- 完成：
  - 治疗摘要安全详情新增“编辑治疗摘要”入口。
  - 新增受控编辑表单，只展示和提交白名单字段。
  - 新增 `updateTreatmentSummary(summaryId, payload)` client helper，调用既有 PATCH API。
  - 请求 body 不包含 `tenantId`、`customerId`、`id`、时间戳字段、未知字段、完整正文、PII、图片 / 文件原文、AI 生成内容、外部系统原文、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
  - 成功后刷新治疗摘要列表和当前详情，并展示“治疗摘要已更新”。
  - 失败后展示稳定中文错误并保留用户输入。
  - 页面明确编辑不会自动修改既有随访任务，也不会重新生成随访建议。
- 边界：
  - 使用既有 PATCH API，未新增 API。
  - 未改数据库 schema / migration。
  - 未改权限、认证或租户隔离。
  - 未进入删除 / 作废、版本历史、diff 展示、AI / RAG / HIS / 企微或外部系统。

## Phase 18 PR 5：smoke / 文档收尾

- 分支：`chore/phase18-treatment-summary-edit-closeout`
- 目标：强化治疗摘要编辑入口 smoke，更新 README、roadmap、devlog 和 Phase 18 spec / plan，标记 Phase 18 完成。
- 完成：
  - workspace entry smoke 覆盖机构端进入治疗摘要管理页面。
  - smoke 覆盖打开治疗摘要安全详情、看到“编辑治疗摘要”入口、打开受控编辑表单。
  - smoke 覆盖合法 payload 调用 `PATCH /api/institution/treatment-summaries/[summaryId]`，请求 body 只包含白名单字段。
  - smoke 确认请求 body 不包含 `tenantId`、`customerId`、`id`、`createdAt`、`updatedAt`、未知字段、完整治疗记录正文、完整病历正文、诊疗原文、咨询全文、手机号原文、身份证号、病历号原文、图片 / 文件原文、AI 生成内容、外部系统原文、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
  - smoke 覆盖编辑成功后刷新治疗摘要列表和当前详情，并展示“治疗摘要已更新”。
  - smoke 覆盖编辑失败后展示稳定中文错误、隐藏敏感错误细节并保留输入。
  - smoke 确认页面文案明确不会自动修改既有随访任务，也不会重新生成随访建议。
  - README、roadmap、Phase 18 spec / plan 更新为 Phase 18 完成状态。
- Phase 18 最终状态：
  - Phase 18 spec / plan 已完成。
  - 编辑 payload parser 已完成。
  - `treatment_summary:update` 最小权限已完成，未开放 delete。
  - repository update 已完成，按服务端确认的 `tenantId + summaryId` 更新。
  - `PATCH /api/institution/treatment-summaries/[summaryId]` 已完成。
  - 机构端治疗摘要编辑 UI 已完成。
  - smoke / 文档收尾已完成。
  - 不自动修改既有随访任务。
  - 不重新生成随访建议。
  - 未新增数据库 schema / migration。
  - 未改认证或租户隔离。
  - 未进入治疗摘要删除、治疗摘要作废、版本历史、diff 展示、完整治疗记录正文、完整病历正文、咨询对话全文、图片 / 文件上传、AI provider、Agent、RAG、企业微信、真实 HIS / CRM / OTA 接入、OAuth、Webhook、支付、合同、发票或外部系统同步。
- 后续建议：
  - Phase 18 收尾时建议进入 Phase 19 Plan Mode，优先评估治疗摘要作废能力 v1、HIS 标准治疗事件 mapper 增强、业务事件埋点体系 spec 和随访路径运营分析 v1；后续已选择并完成 Phase 19 治疗摘要作废能力 v1。

## Phase 19 PR 1：治疗摘要作废 spec / plan

- 分支：`docs/phase19-treatment-summary-void-plan`
- 目标：固化 Phase 19 治疗摘要作废能力 v1 的产品边界、技术设计和 PR 拆分。
- 完成：
  - 新增 `docs/superpowers/specs/2026-06-02-phase19-treatment-summary-void-v1-design.md`。
  - 新增 `docs/superpowers/plans/2026-06-02-phase19-treatment-summary-void-v1.md`。
  - 明确作废不是删除，不做硬删除，不删除历史治疗摘要、不删除客户时间线、不删除来源随访任务。
  - 明确作废后不允许继续生成新的随访建议，不允许继续人工确认创建新的来源随访任务。
  - 明确已存在来源随访任务不自动取消、不自动修改状态，仍保留来源追溯。
  - 明确作废字段、作废 API、parser、RBAC、audit、UI 展示、租户隔离、PII / 医疗隐私边界和 PR 1-5 拆分。
- 边界：
  - 只做文档。
  - 未改代码、API、数据库、权限、认证或租户隔离。
  - 未进入 AI / RAG / HIS / 企微 / 外部系统。

## Phase 19 PR 2：治疗摘要作废地基

- 分支：`codex/phase19-treatment-summary-void-core`
- 目标：为治疗摘要软作废增加最小数据底座和后端 repository 能力。
- 完成：
  - `treatment_summaries` 新增 nullable 作废字段：`voidedAt`、`voidedBy`、`voidReasonCode`、`voidReason`。
  - 新增 Drizzle migration / meta，migration 只新增字段和必要索引，不 drop 表、不删除字段、不修改已有字段类型，历史数据默认 active。
  - 治疗摘要 domain / DTO 派生 `status: "active" | "voided"`，有 `voidedAt` 即视为 voided。
  - 新增作废原因 parser，支持稳定 reason code 和安全短说明。
  - parser 拒绝完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文、手机号原文、身份证号、病历号原文、图片 / 文件原文、AI 生成内容、外部系统同步原文、请求体、SQL、stack、token、secret、`DATABASE_URL` 和连接串。
  - 新增 `voidTreatmentSummaryByTenant`，按服务端确认的 `tenantId + summaryId` 作废，重复作废返回稳定结果，返回安全 DTO。
  - 新增作废 audit reason 预留和 schema / migration / parser / repository 测试。
- 边界：
  - 未新增 API route。
  - 未新增 UI。
  - 未硬删除治疗摘要。
  - 未自动取消或修改既有来源随访任务。
  - 未改权限、认证或租户隔离。

## Phase 19 PR 3：治疗摘要作废 API 与随访阻断

- 分支：`codex/phase19-treatment-summary-void-api`
- 目标：新增作废 API，并阻断作废摘要继续生成随访建议或创建来源随访任务。
- 完成：
  - 新增 `POST /api/institution/treatment-summaries/[summaryId]/void`。
  - 服务端从 access context 推导 `tenantId`，不接受前端传入 `tenantId`。
  - API 使用 Phase 19 PR 2 的作废 parser 和 `voidTreatmentSummaryByTenant`。
  - 作废成功返回安全 DTO，并写 `treatment_summary/update allowed` audit，reason 为 `treatment_summary_voided`。
  - 重复作废、非法 payload、无权限、not_found 等场景返回稳定中文错误并写稳定 audit。
  - 作废后 `GET follow-up-suggestions` 返回稳定 `409`，不再生成新的随访建议。
  - 作废后 `POST follow-up-tasks` 返回稳定 `409`，不再创建新的来源随访任务。
- 边界：
  - 未新增 UI。
  - 未改数据库 schema / migration。
  - 未硬删除治疗摘要。
  - 未批量作废。
  - 未自动取消或修改既有随访任务。
  - 未进入 AI / RAG / HIS / 企微 / 外部系统。

## Phase 19 PR 4：机构端作废状态展示

- 分支：`codex/phase19-treatment-summary-void-ui`
- 目标：在机构端列表、详情、客户 timeline 和来源任务提示中展示治疗摘要作废状态。
- 完成：
  - 治疗摘要管理列表展示 active / voided 状态，作废摘要不隐藏，仍可查看安全详情。
  - 治疗摘要详情展示作废状态、作废时间、作废人、作废原因，以及“仅保留历史追溯”和“不再生成新的随访建议或来源随访任务”提示。
  - 机构端详情提供单条“作废治疗摘要”入口，调用既有 `POST /api/institution/treatment-summaries/[summaryId]/void`，请求 body 只包含 `reasonCode` 和 `reasonText`，不包含 `tenantId` 或敏感字段。
  - 客户 timeline 中标记作废治疗摘要节点，显示历史追溯提示，不删除历史节点。
  - 来源随访任务提示“来源治疗摘要已作废”，并明确既有任务不会自动取消或修改状态。
  - 已作废摘要在 UI 中阻断随访建议入口和人工确认创建来源随访任务，不调用建议 API 或任务创建 API。
  - workspace / institution 测试覆盖列表、详情、作废入口、客户 timeline、来源任务提示、建议阻断、任务创建阻断和敏感字段不展示。
- 边界：
  - 使用既有作废 API，未新增 API。
  - 未改数据库 schema / migration。
  - 未硬删除治疗摘要。
  - 未批量作废。
  - 未自动取消或修改既有随访任务。
  - 未自动触达客户。
  - 未进入 AI / RAG / HIS / 企微 / 外部系统。

## Phase 19 PR 5：smoke / 文档收尾

- 分支：`chore/phase19-treatment-summary-void-closeout`
- 目标：强化治疗摘要作废 smoke / entry 测试，更新 README、roadmap、devlog 和 Phase 19 spec / plan，标记 Phase 19 完成。
- 完成：
  - workspace smoke 覆盖机构端进入治疗摘要管理页面。
  - smoke 覆盖 active 摘要正常展示、voided 摘要显示“已作废”。
  - smoke 覆盖作废摘要详情显示作废时间、作废人、作废原因和历史追溯提示。
  - smoke 覆盖客户 timeline 显示作废治疗摘要节点，作废节点仍保留历史追溯。
  - smoke 覆盖来源随访任务提示“来源治疗摘要已作废”。
  - smoke 覆盖已作废摘要不能继续生成新的随访建议，不能继续创建新的来源随访任务。
  - smoke 覆盖既有来源随访任务不被自动取消、不被自动修改状态。
  - smoke 覆盖不硬删除治疗摘要、不批量作废、不展示手机号原文、身份证号、病历号原文、完整治疗记录正文、完整病历正文、诊疗原文、咨询对话全文、图片 / 文件原文、AI 生成内容、外部系统同步原文、SQL、stack、token、secret、`DATABASE_URL` 或连接串。
  - README、roadmap、Phase 19 spec / plan 更新为 Phase 19 完成状态。
- Phase 19 最终状态：
  - Phase 19 spec / plan 已完成。
  - 作废字段、schema / migration、domain、parser、repository 已完成。
  - 作废 API 已完成。
  - 作废后阻断随访建议和来源随访任务创建已完成。
  - 列表、详情、客户 timeline、来源任务提示中的作废状态展示已完成。
  - smoke / 文档收尾已完成。
  - 不硬删除治疗摘要。
  - 不批量作废。
  - 不做版本历史或 diff 展示。
  - 不自动取消既有随访任务。
  - 不自动触达客户。
  - 未进入完整治疗记录正文、完整病历正文、咨询对话全文、图片 / 文件上传、AI provider、Agent、RAG、企业微信、HIS / CRM / OTA 真实接入、OAuth、Webhook、支付或外部系统同步。
- 后续建议：
  - 先进入产品可演示性验收，完整走查 Phase 5-19 的机构端和平台端闭环。
  - 演示验收后再决定 Phase 20 Plan Mode；当前不进入 Phase 20 实现。

## Phase 20 Plan Mode：治疗项目路径模板 / 随访路径模板 v1 spec / plan

- 分支：`docs/phase20-treatment-path-template-plan`
- 目标：规划如何把现有治疗摘要和确定性随访建议升级为更清晰的治疗项目路径模板 / 随访路径模板 v1。
- 完成：
  - 新增 Phase 20 设计文档，明确当前只是 Plan Mode，不是功能实现。
  - 新增 Phase 20 计划文档，给出后续 PR 拆分建议。
  - 明确路径模板 v1 与当前治疗摘要、确定性随访建议、来源随访任务、作废阻断和重复来源任务治理的关系。
  - 首批规划覆盖光子 / 光电治疗、水光 / 注射护理、术后修复和皮肤管理。
  - 明确路径模板建议包含项目类型、恢复阶段、风险等级、建议随访节点、建议任务标题、建议处理角色、是否需要人工确认和禁止自动触达。
  - 明确当前只规划、不落库；后续 schema / API、租户自定义模板、路径实例、效果分析、HIS / 企微 / AI 都必须单独评估。
- 边界：
  - 不写代码。
  - 不改 UI。
  - 不改测试。
  - 不新增 API。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不接 HIS。
  - 不接企微。
  - 不接 AI / RAG / Agent。
  - 不做自动触达。
  - 不修改 demo seed 数据。
  - 不导入真实客户数据。
  - 不保存完整治疗记录正文、完整病历正文、咨询对话全文、图片 / 文件原文。

## Phase 20 PR #96：路径模板 domain-only catalog

- 分支：`codex/phase20-treatment-path-template-catalog`
- 目标：根据 Phase 20 spec / plan 新增 domain-only 静态治疗项目路径模板 catalog 和保守 matcher。
- 完成：
  - 新增 `src/modules/institution/domain/treatment-path-templates.ts`。
  - 首批 catalog 覆盖 `photoelectric_care`、`hydro_injection_care`、`post_surgery_repair` 和 `skin_management`。
  - 模板节点包含恢复阶段、风险等级、offset days、建议任务标题、建议处理角色、`requiresHumanConfirmation: true` 和 `forbidAutoReachOut: true`。
  - matcher 优先使用 `treatmentCategory` 白名单，`treatmentProject`、`treatmentStage`、`recoveryStage` 和 `tags` 只作为安全辅助匹配。
  - ambiguous 输入不猜测项目，敏感字段、完整正文、图片 / 文件原文、SQL、stack、token、secret、`DATABASE_URL` 和连接串不进入输出。
  - 单元测试覆盖四类模板、urgent 节点优先、人工确认、禁止自动触达和不调用 AI / RAG / Agent / 外部系统。
- 边界：
  - domain-only catalog，未接入运行时随访建议流程。
  - 未新增 API route，未改 DTO，未改数据库 schema / migration。
  - 未改权限、认证或租户隔离。
  - 未接 HIS、企微、AI、RAG、Agent。
  - 未做自动触达，未修改 demo seed 数据。

## Phase 20 PR #97：确定性随访建议接入路径模板

- 分支：`codex/phase20-template-driven-followup-suggestions`
- 目标：把 domain-only 路径模板 catalog 接入现有 `buildTreatmentFollowUpSuggestions`，输出模板驱动的内部随访建议。
- 完成：
  - `template_path_followup` 建议在模板匹配成功时生成，并保留现有高风险、恢复早期、nextCareAction、category 和 fallback 规则。
  - 新模板建议 key 稳定为 `<summaryId>:template_path_followup:<offsetDays>d:<templateKey>:<nodeKey>`。
  - 模板建议 tags 包含 `路径模板`、模板 key、恢复阶段和建议处理角色，供 UI 安全识别。
  - 人工确认创建任务 API 可使用模板建议 key 创建来源任务。
  - 重复来源任务治理、作废摘要阻断和建议 API 只读边界保持稳定。
  - 测试覆盖四类模板建议、urgent 高优先级、ambiguous fallback、旧规则 key 兼容、敏感字段不泄露和不调用 AI / RAG / Agent / 外部系统。
- 边界：
  - 未新增 API route，未改现有 API 路径。
  - 未改 DTO 字段，未改数据库 schema / migration。
  - 未改权限、认证或租户隔离。
  - 未接 HIS、企微、AI、RAG、Agent。
  - 未做自动触达，未修改 demo seed 数据。

## Phase 20 PR #98：机构端模板建议轻量展示

- 分支：`codex/phase20-template-suggestion-ui`
- 目标：在机构端治疗摘要管理页的随访建议区域轻量展示模板驱动建议信息。
- 完成：
  - 治疗摘要管理详情识别 `ruleKey === "template_path_followup"`。
  - 模板建议展示“路径模板建议”、“来源：治疗项目路径模板”、路径类型、建议处理角色、系统只生成内部随访建议、人工确认后创建内部随访任务、禁止自动触达客户、不自动回复客户和不接 AI。
  - 路径类型和建议处理角色只从既有 `suggestion.tags` 中安全识别，不新增 DTO 字段。
  - 模板建议继续通过现有“确认创建随访任务”人工确认操作创建来源任务。
  - 组件测试覆盖模板展示、旧规则建议仍正常展示、创建任务 suggestionKey、边界文案和敏感字段不展示。
- 边界：
  - 未新增 API route，未改现有 API 路径。
  - 未改 DTO 字段，未改数据库 schema / migration。
  - 未改权限、认证或租户隔离。
  - 未接 HIS、企微、AI、RAG、Agent。
  - 未做自动触达，未修改 demo seed 数据。
  - 未新增路径模板配置后台、路径编辑器或平台端模板管理。

## Phase 20 PR 5：smoke / 文档收尾

- 分支：`codex/phase20-template-smoke-docs`
- 目标：补充 Phase 20 治疗项目路径模板 v1 的 smoke / 回归覆盖，并同步 README、roadmap、devlog、Phase 20 spec / plan 最终状态。
- 完成：
  - workspace smoke 覆盖治疗摘要管理页展示模板驱动建议。
  - smoke 覆盖四类路径类型展示：光子 / 光电治疗、水光 / 注射护理、术后修复、皮肤管理。
  - smoke 覆盖建议处理角色、人工确认边界、禁止自动触达客户、不自动回复客户和不接 AI。
  - smoke 覆盖模板建议仍通过现有人工确认操作创建来源任务，请求 body 只包含 `suggestionKey`，不包含 `tenantId`、`customerId` 或完整建议内容。
  - smoke 覆盖模板建议重复确认时继续走来源任务去重治理。
  - 已有 institution / workspace 测试继续覆盖作废摘要阻断、旧规则建议展示、来源任务提示、敏感字段不展示和不调用外部能力。
  - README、roadmap、Phase 20 spec / plan 更新为 Phase 20 v1 最小闭环完成状态。
- Phase 20 v1 最终状态：
  - spec / plan 已完成。
  - domain-only 静态模板 catalog 和保守 matcher 已完成。
  - 确定性随访建议已接入路径模板。
  - 机构端治疗摘要管理页已轻量展示模板建议信息。
  - 人工确认创建来源任务、重复来源任务治理和作废摘要阻断已形成闭环。
  - 不新增 API route，不改 DTO，不改 schema / migration，不改权限、认证或租户隔离。
  - 不接 HIS / 企微 / AI / RAG / Agent。
  - 不做自动触达，不自动发微信 / 短信 / 电话 / 企业微信，不自动回复客户。
  - 不修改 demo seed 数据。
  - 不新增路径模板配置后台、路径编辑器或平台端模板管理。
- 后续建议：
  - 如需路径模板 schema / API、租户自定义 SOP、平台端模板管理、路径编辑器、HIS 输入、企微触达、AI 生成建议、自动触达或路径效果分析，必须单独进入 Plan Mode。

## Phase 21 Plan Mode：随访路径运营分析 v1 spec / plan

- 分支：`docs/phase21-followup-path-analysis-plan`
- 目标：规划如何基于现有治疗摘要、路径模板建议、来源随访任务、任务状态和审计记录，形成最小可用的随访路径运营分析 v1。
- 完成：
  - 新增 Phase 21 设计文档，明确当前只是 Plan Mode，不是功能实现。
  - 新增 Phase 21 计划文档，给出后续 PR 拆分建议。
  - 明确随访路径运营分析 v1 承接 Phase 20 路径模板闭环：治疗摘要结构化字段、`template_path_followup` 模板建议、人工确认来源任务、任务状态、作废阻断、重复来源任务治理和审计记录。
  - 明确最小指标建议：模板建议数、人工确认任务数、任务完成数、任务超时数、作废摘要阻断数和重复来源任务冲突数。
  - 明确作废摘要阻断数和重复来源任务冲突数优先依赖审计记录；如果现有审计无法稳定支撑，后续必须单独评估审计口径补强，不能在分析实现中顺手扩大范围。
  - 明确当前不做复杂经营智能中心、不做 AI 分析、不做自动触达、不新增 API / schema。
  - 明确后续如需落库、报表 API、图表 UI、导出或经营归因，必须单独评估。
- 边界：
  - 不写代码。
  - 不改 UI。
  - 不改测试。
  - 不新增 API。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不接 HIS。
  - 不接企微。
  - 不接 AI / RAG / Agent。
  - 不做自动触达。
  - 不做经营智能中心实现。
  - 不做图表 UI。
  - 不做报表导出。
  - 不修改 demo seed 数据。

## Phase 21 PR 2：随访路径运营分析 domain-only 口径

- 分支：`codex/phase21-followup-path-analysis-domain`
- 目标：基于现有治疗摘要、模板驱动随访建议、来源随访任务、任务状态和审计记录，新增 domain-only 纯函数，计算随访路径运营分析 v1 的最小指标。
- 完成：
  - 新增 `src/modules/institution/domain/followup-path-analysis.ts`。
  - 新增 `src/modules/institution/tests/FollowUpPathAnalysis.test.ts`。
  - 定义最小安全输入类型：治疗摘要、模板建议、来源随访任务、审计事件和固定 `analysisAt`。
  - 定义最小输出：`templateSuggestionCount`、`confirmedSourceTaskCount`、`completedTaskCount`、`overdueTaskCount`、`voidedSummaryBlockedCount`、`duplicateSourceTaskConflictCount`，以及安全 notes / warnings。
  - 模板建议数只统计 active 治疗摘要对应的 `template_path_followup`。
  - 人工确认任务数、完成数和超时数只统计模板路径治疗摘要来源任务；超时使用传入的固定 `analysisAt`，不读取本地时间。
  - 作废摘要阻断数和重复来源任务冲突数只从可识别审计事件统计；审计不足时返回 0 和 warning，不用 voided 摘要数量或任务重复行猜测。
  - 单元测试覆盖最小指标、voided 摘要排除、固定超时口径、completed / cancelled 排除、审计不足不猜测、敏感字段不输出、不调用 AI / RAG / Agent / 外部系统、不写数据库和不创建任务。
- 边界：
  - 不新增 API route。
  - 不改现有 API 路径。
  - 不改 DTO 字段。
  - 不改 UI。
  - 不新增图表。
  - 不做报表导出。
  - 不改数据库 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不接 HIS。
  - 不接企微。
  - 不接 AI / RAG / Agent。
  - 不做自动触达。
  - 不做经营智能中心实现。
  - 不做收入、复购、转化归因。
  - 不修改 demo seed 数据。
