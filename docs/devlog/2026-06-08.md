# 2026-06-08 开发日志

## Backlog-Review-01：Phase 23 后续 backlog 优先级冻结

本次任务目标：

- 在 Phase 23 closeout 与 AI-GOV-01 合并后，冻结 Phase 23 后续 backlog 优先级。
- 明确当前不是 Phase 24 runtime。
- 明确 backlog、devlog 下一步建议和 Plan 文档中的能力名称都不是开发许可。
- 将真实 credential provider、真实 HIS adapter、candidate query、scheduled audit、system actor、runner / scheduler、lock / lease / backoff、schema / migration、外部网络、真实凭证、生产配置、监控 / 告警 / 失败重试策略纳入单独审批边界。

新增 plan 文件：

- `docs/superpowers/plans/2026-06-08-phase23-backlog-priority-freeze.md`

本次未做：

- 未开始 Phase 24。
- 未修改 `src/**`。
- 未修改 `drizzle/**`。
- 未修改 `AGENTS.md`。
- 未修改 `docs/ai-agent-governance.md`。
- 未修改 `docs/superpowers/specs/**`。
- 未修改 `docs/roadmap/**` 或 `README.md`。
- 未修改 package 或 lockfile。
- 未新增 schema / migration。
- 未实现真实 credential provider。
- 未实现真实 HIS adapter。
- 未实现 candidate query、scheduled audit、system actor、runner、scheduler、cron、queue 或 worker。
- 未读取真实凭证。
- 未发起外部网络请求。

验证命令：

- `git status --short`
- `git diff --name-only`
- `git diff --stat`
- `git diff -- docs/superpowers/plans/2026-06-08-phase23-backlog-priority-freeze.md docs/devlog/2026-06-08.md`
- 本轮新增 / 修改文档中文化残留检查。
- 全目录固定中文化残留检查。
- 禁止范围检查。

下一步建议：

- 不自动执行。
- 建议由人工确认后，再选择 `Phase24-PLAN` 或单独 backlog Plan 任务。
- 下一步建议不自动执行，必须人工确认后单独开任务。

## Phase24-PLAN：生产可用性优先级梳理

本次任务目标：

- 在 Phase 23 closeout、AI-GOV-01 与 Backlog-Review-01 均已合并后，进入 Phase24-PLAN。
- 梳理生产可用性优先级，明确 Phase 24 不是 runtime。
- 明确真实 credential provider、真实 HIS adapter、外部网络、真实凭证、schema / migration、runner / scheduler、candidate query、scheduled audit、system actor、lock / lease / backoff、监控 / 告警 / 失败重试、生产配置和回滚策略的审批边界。
- 将后续候选任务拆成单独 Plan Mode，不把任何 P0 / P1 主题直接转成 runtime。

新增 plan 文件：

- `docs/superpowers/plans/2026-06-08-phase24-production-readiness-plan.md`

本次未做：

- 未开始 Phase 24 runtime。
- 未修改 `src/**`。
- 未修改 `drizzle/**`。
- 未修改 `AGENTS.md`。
- 未修改 `docs/ai-agent-governance.md`。
- 未修改 `docs/superpowers/specs/**`。
- 未修改 `docs/roadmap/**` 或 `README.md`。
- 未修改 package 或 lockfile。
- 未新增 schema / migration。
- 未实现真实 credential provider。
- 未实现真实 HIS adapter。
- 未实现 candidate query、scheduled audit、system actor、runner、scheduler、cron、queue 或 worker。
- 未实现 lock / lease / backoff。
- 未读取真实凭证。
- 未发起外部网络请求。

验证命令：

- `git status --short`
- `git diff --name-only`
- `git diff --stat`
- `git diff -- docs/superpowers/plans/2026-06-08-phase24-production-readiness-plan.md docs/devlog/2026-06-08.md`
- 本次新增 / 修改文档中文化残留检查。
- 全目录固定中文化残留检查。
- 禁止范围检查。

下一步建议：

- 不自动执行。
- 建议下一步由人工确认后，再选择一个 P0 / P1 单独 Plan 任务。
- 下一步建议不自动执行，必须人工确认后单独开任务。

## CRED-PLAN-01：真实 credential provider runtime 前置边界复核

本次任务目标：

- 在 Phase24-PLAN 已完成后，单独执行 CRED-PLAN-01。
- 保持 docs-only Plan Mode，只复核真实 credential provider runtime 前置边界。
- 明确凭证来源、生命周期、脱敏、审计、配置开关、环境隔离、provider / HIS adapter 依赖边界和后续拆分候选。
- 明确后续候选任务仅作为候选，不自动执行。

新增 plan 文件：

- `docs/superpowers/plans/2026-06-08-cred-plan-01.md`

本次未做：

- 未进入 runtime。
- 未读取真实凭证。
- 未修改 `src/**`。
- 未修改 `drizzle/**`。
- 未修改 package / lockfile。
- 未修改 `.codex/**`。
- 未修改 `AGENTS.md` 或 `docs/ai-agent-governance.md`。
- 未新增 schema / migration。
- 未接入真实 HIS。
- 未实现 HIS adapter。
- 未实现 runner、scheduler、cron、queue 或 worker。
- 未发起外部网络请求。
- 未自动执行后续候选任务。

验证命令：

- `git status --short`
- `git diff --stat`
- `git diff --check`
- 全目录固定中文化残留检查。
- 本次新增 / 修改文档中文化残留检查。
- 禁止范围检查。

下一步建议：

- 不自动执行。
- 建议后续由人工确认后，再从 credential provider contract plan、secret redaction plan、configuration gate plan、audit semantics plan、credential lifecycle plan、provider / HIS adapter dependency boundary plan 中选择单独任务。
- 任何 runtime 任务都必须后续明确批准后才能进入。

## HIS-ADAPTER-PLAN-01：真实 HIS adapter 接入边界复核

本次任务目标：

- 在 Phase24-PLAN 与 CRED-PLAN-01 均已完成后，单独执行 HIS-ADAPTER-PLAN-01。
- 保持 docs-only Plan Mode，只复核真实 HIS adapter 接入前置边界。
- 明确 adapter 与 credential provider、测试连接、健康检查、audit、错误归一、外部网络、配置开关、runner / scheduler 和 system actor / service actor 的边界。
- 明确后续候选任务仅作为候选，不自动执行。

新增 plan 文件：

- `docs/superpowers/plans/2026-06-08-his-adapter-plan-01.md`

本次未做：

- 未进入 runtime。
- 未实现 HIS adapter。
- 未接入真实 HIS。
- 未读取真实凭证。
- 未发起外部网络请求。
- 未修改 `src/**`。
- 未修改 `drizzle/**`。
- 未修改 package / lockfile。
- 未修改 `.codex/**`。
- 未修改 `AGENTS.md` 或 `docs/ai-agent-governance.md`。
- 未新增 schema / migration。
- 未实现测试连接 runtime。
- 未实现健康检查 runtime。
- 未实现 runner、scheduler、cron、queue 或 worker。
- 未自动执行后续候选任务。

验证命令：

- `git status --short`
- `git diff --stat`
- `git diff --check`
- 全目录固定中文化残留检查。
- 本次新增 / 修改文档中文化残留检查。
- 禁止范围检查。

下一步建议：

- 不自动执行。
- 建议后续由人工确认后，再从 HIS adapter contract plan、adapter / credential provider dependency boundary plan、external network safety plan、HIS error normalization plan、adapter audit semantics plan、manual test connection integration plan、scheduled health check integration plan、adapter environment isolation plan 中选择单独任务。
- 任何 runtime、真实 HIS 接入、真实凭证读取、外部网络请求、schema / migration 或 runner / scheduler 任务都必须后续明确批准后才能进入。

## NETWORK-PLAN-01：外部网络与真实 HIS 连接安全边界

本次任务目标：

- 在 Phase24-PLAN、CRED-PLAN-01 与 HIS-ADAPTER-PLAN-01 均已完成后，单独执行 NETWORK-PLAN-01。
- 保持 docs-only Plan Mode，只复核外部网络与真实 HIS 连接安全边界。
- 明确出站网络 allowlist / denylist、SSRF、DNS rebinding、重定向、TLS、超时、重试、backoff、熔断、响应大小、环境隔离、audit、配置开关和监控告警的前置边界。
- 明确后续候选任务仅作为候选，不自动执行。

新增 plan 文件：

- `docs/superpowers/plans/2026-06-08-network-plan-01.md`

本次未做：

- 未进入 runtime。
- 未实现 HTTP client。
- 未实现外部网络安全 runtime。
- 未实现 allowlist / denylist runtime。
- 未实现 SSRF 防护 runtime。
- 未接入真实 HIS。
- 未读取真实凭证。
- 未发起真实 HIS / 第三方业务外部网络请求。
- 未实现 HIS adapter。
- 未实现测试连接 runtime。
- 未实现健康检查 runtime。
- 未实现 runner、scheduler、cron、queue 或 worker。
- 未修改 `src/**`。
- 未修改 `drizzle/**`。
- 未修改 package / lockfile。
- 未修改 `.codex/**`。
- 未修改 `AGENTS.md` 或 `docs/ai-agent-governance.md`。
- 未新增 schema / migration。
- 未自动执行后续候选任务。

验证命令：

- `git status --short`
- `git diff --stat`
- `git diff --check`
- 全目录固定中文化残留检查。
- 本次新增 / 修改文档中文化残留检查。
- 禁止范围检查。

下一步建议：

- 不自动执行。
- 建议后续由人工确认后，再从 external network contract plan、allowlist / denylist policy plan、SSRF and redirect safety plan、timeout / retry / backoff plan、network error normalization plan、network audit semantics plan、environment isolation and gate plan、real HIS connectivity approval plan、monitoring and alerting integration plan 中选择单独任务。
- 任何 runtime、真实 HIS 接入、真实凭证读取、外部网络请求、schema / migration 或 runner / scheduler 任务都必须后续明确批准后才能进入。
## PRODUCT-REBASE-01：智美天工产品事实源收口

- 当前日期：2026-06-08。
- 当前阶段 / 编号：PRODUCT-REBASE-01。
- 本次性质：docs-only 产品事实源收口。
- 本次不是：Phase 24 HIS 风险治理、runtime 实现、schema / migration、真实凭证、真实外网、真实第三方系统接入。

### 本次整理

- 新增 `docs/product/zhimeitiangong-product-source-of-truth.md`，收口智美天工产品定位、服务对象、业务价值、HIS 位置、AI 位置和客户运营主线。
- 新增 `docs/product/zhimeitiangong-module-map.md`，整理客户档案、治疗记录、预约到院、随访、复诊、复购、沉睡客户、标签分层、SOP、AI、微信触达、HIS 数据接入、看板、权限租户审计等模块地图。
- 新增 `docs/product/zhimeitiangong-v1-scope.md`，把 1.0 范围分为必须做、可以简化、不做、后续增强。
- 新增 `docs/product/zhimeitiangong-feature-addendum.md`，恢复 OneID、SceneID、Event、标签画像、SOP、API / Webhook、插件化、AI Agent、多来源数据接入、企业微信触达、HIS / 第三方系统接入等后期补充方向。
- 新增 `docs/product/zhimeitiangong-decision-log.md`，记录已收口判断、待人工确认问题、后续候选事项和暂停事项。

### 产品主线恢复

- 智美天工恢复为面向医美 / 美业机构的 AI 客户运营中台。
- 产品主线回到客户档案、预约到院、项目 / 治疗记录、治疗后摘要、随访任务、复诊复购、沉睡客户唤醒、客户时间线、运营看板和审计追踪。
- SCRM、客户运营、随访、复诊、复购、标签分层、SOP 和触达能力属于产品主线。
- HIS 恢复为多来源数据接入的一类，不是系统主线，也不是 1.0 闭环验证的阻塞项。
- AI 恢复为运营辅助能力，不宣称自动医疗建议、自动触达或无人工确认的决策。

### 本次边界

- 未修改 `src/**`。
- 未修改 `drizzle/**`。
- 未修改 package 或 lockfile。
- 未修改 `.codex/**`。
- 未修改 `AGENTS.md`。
- 未修改 `docs/ai-agent-governance.md`。
- 未修改 README。
- 未修改 roadmap。
- 未修改 superpowers plans / specs。
- 未创建 schema 或 migration。
- 未进入 runtime。
- 未继续推进 Phase 24 HIS 后续任务。

### 后续候选

- V1-SCOPE-LOCK-01：锁定智美天工 1.0 范围。
- PROD-GAP-REVIEW-01：基于产品事实源审查当前系统偏离点。
- 1.0 试运行修复：只处理与客户运营闭环直接相关的问题。

以上只是候选，不是自动开发许可。

## V1-SCOPE-LOCK-01：锁定智美天工 1.0 范围

- 当前日期：2026-06-08。
- 当前阶段 / 编号：V1-SCOPE-LOCK-01。
- 本次性质：docs-only 产品 1.0 范围锁定。
- 本次不是：Phase 24 HIS 风险治理、PROD-GAP-REVIEW-01、runtime 实现、schema / migration、真实凭证、真实外网、真实第三方系统接入。

### 本次整理

- 更新 `docs/product/zhimeitiangong-v1-scope.md`，将 1.0 范围从 PRODUCT-REBASE-01 初版推进为 V1-SCOPE-LOCK-01 锁定版。
- 更新 `docs/product/zhimeitiangong-product-source-of-truth.md`，补充 1.0 范围来源、AI 客户运营中台定位、HIS 定位、AI 定位、SCRM 术语定位和企业微信 / 微信触达边界。
- 更新 `docs/product/zhimeitiangong-module-map.md`，把模块状态调整为必须、简化、后置、待确认。
- 更新 `docs/product/zhimeitiangong-feature-addendum.md`，把 OneID、SceneID、Event、标签画像、SOP 编排器、API / Webhook、插件化、AI Agent、多来源数据接入、企业微信触达、HIS / 第三方系统接入标记为不阻塞 1.0 的后续增强或待确认方向。
- 更新 `docs/product/zhimeitiangong-decision-log.md`，追加 V1-SCOPE-LOCK-01 决策记录、影响范围、待人工确认问题和后续候选边界。

### 1.0 锁定结论

- 智美天工 1.0 锁定为面向医美 / 美业机构的 AI 客户运营中台。
- 1.0 范围锁定为治疗后客户运营闭环。
- 1.0 主线包括客户档案 / 患者信息、预约 / 到院、项目 / 治疗记录、治疗后摘要、随访任务、复诊提醒、复购机会、沉睡客户机会、人工确认、基础运营看板和审计追踪。
- HIS 是数据来源之一，不是 1.0 主线，不阻塞 1.0。
- AI 是辅助能力，不做自动医疗决策、自动客服或 AI Agent 自动执行。
- 企业微信 / 微信自动触达后置，1.0 不做自动发送。

### 本次边界

- 未修改 `src/**`。
- 未修改 `drizzle/**`。
- 未修改 package 或 lockfile。
- 未修改 `.codex/**`。
- 未修改 `AGENTS.md`。
- 未修改 `docs/ai-agent-governance.md`。
- 未修改 README。
- 未修改 roadmap。
- 未修改 superpowers plans / specs。
- 未创建 schema 或 migration。
- 未进入 runtime。
- 未继续推进 Phase 24 HIS 风险治理线。
- 未执行 CONFIG-PLAN-01 / SCHEDULER-PLAN-01 / AUDIT-PLAN-01 / OBS-PLAN-01 / SCHEMA-REVIEW-01。
- 未执行后续候选任务。

### 后续候选

- PROD-GAP-REVIEW-01：基于产品事实源审查当前系统偏离点。
- 1.0 试运行修复：只处理与客户运营闭环直接相关的问题。

以上只是候选，不是自动开发许可。
