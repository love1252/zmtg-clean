## Phase 23 HIS 连接配置凭证 compensation job queue repository 最小边界

- 分支：`feat/phase23-his-credential-compensation-job-queue-repository-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 compensation job queue repository 最小边界，承接 job queue schema / migration 最小边界，为后续 worker claim / lock / stale recovery 和 compensation audit integration 提供 repository 基础。
- 当前 main commit：`bba59265c93e20402de829d79bb9c9a2e96ad885`
- 只读盘点结论：
  - 当前 `his_connection_credential_compensation_jobs` 表字段、job state enum、dead letter reason enum、tenant / connection / operationId 复合 FK、operationId unique、调度索引、claim / lock 字段和 retry / nextAttemptAt 字段足以支撑 repository 最小实现。
  - compensation operation repository 可复用可信输入 normalize、tenant + connection + operationId scope、稳定 result、read model 映射、状态流转先查后改、唯一冲突转 conflict 和 repository_error 脱敏模式。
  - 本轮可以只新增 repository，不改 schema / migration，不修改 `drizzle/**`。
  - 本轮可以不接 service、不实现 worker runtime、不写 audit、不调用 provider、不处理真实凭证。
  - worker claim / lock / stale recovery runtime、compensation audit integration、service 接入、dead letter / manual review 业务闭环和真实 provider / secret manager / HIS adapter 仍需另开 Plan Mode 或独立 PR。
- 完成：
  - 新增 `src/modules/institution/server/his-connection-credential-compensation-job-queue-repository.ts`。
  - 新增 `src/modules/institution/tests/HisConnectionCredentialCompensationJobQueueRepository.test.ts`。
  - repository 方法覆盖 create、get by operation、get by connection、list due、claim due、mark running、mark succeeded、mark failed、requeue、mark dead letter、mark manual review 和 list expired locked jobs。
  - 稳定 result 覆盖 `ok`、`not_found`、`conflict`、`invalid_state_transition`、`validation_failed` 和 `repository_error`。
  - read model 只返回 job queue 表的安全字段，不返回 provider path、secret path、`credentialRef`、idempotencyKey、raw credential、raw HIS payload、request body、response body、provider raw error、SQL、stack 或 `DATABASE_URL`。
  - claim due 只允许到期 queued job 或锁已过期的 claimed / running job，claim 成功写入 `claimed`、`claimId`、`claimVersion + 1`、`claimedBy`、`claimedAt`、`lockedUntil` 和 `updatedAt`。
  - mark running / succeeded / failed / dead letter / manual review / requeue 均校验 `claimId` 和 `claimVersion`，旧 claim 写回返回 `conflict`。
  - 状态流转允许 `queued -> claimed`、`claimed -> running`、`running -> succeeded`、`running -> failed`、`failed -> queued`、`claimed/running/failed -> dead_lettered` 和 `claimed/running/failed -> manual_review_required`。
  - 禁止 `succeeded`、`dead_lettered`、`cancelled` 自动回退。
  - README 和 roadmap 同步本轮 repository 最小边界。
- 边界：
  - 不新增 schema / migration。
  - 不修改 `drizzle/**`。
  - 不实现 worker runtime。
  - 不实现 worker 主循环。
  - 不接 service。
  - 不新增 API route。
  - 不修改 parser / DTO / provider / storage。
  - 不修改权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不实现 compensation audit。
  - 不调用 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
  - 不接 KMS / Vault / secret manager。
  - 不修改 package、lockfile、`.env` 或 `.codex`。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationJobQueueRepository.test.ts` 先失败，失败点为 job queue repository module 尚未存在。
  - GREEN：同一命令通过，1 个测试文件，16 个测试。
  - 类型与 lint：`./node_modules/.bin/tsc --noEmit` 通过；`./node_modules/.bin/eslint src/modules/institution/server/his-connection-credential-compensation-job-queue-repository.ts src/modules/institution/tests/HisConnectionCredentialCompensationJobQueueRepository.test.ts` 通过。
- 后续建议：
  - worker claim / lock / stale recovery Plan Mode。
  - compensation audit integration Plan Mode。
  - service 接入 compensation operation + job queue 写入。
  - dead letter / manual review 最小闭环。
  - 后续真实 provider / secret manager / HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证 compensation worker claim / lock / stale recovery Plan Mode

- 分支：`plan/phase23-his-credential-compensation-worker-claim-lock-stale-recovery`
- 目标：只做 Phase 23 HIS 连接配置凭证 compensation worker claim / lock / stale recovery Plan Mode，承接 job queue schema / migration 与 job queue repository 最小边界，为后续 worker runtime 和 audit integration 提供中文边界。
- 当前 main commit：`3cfcdd035687ed03db179ee04a599cca21f120ac`
- 只读盘点结论：
  - operation repository 足以支撑 worker 推进 pending / failed / manual review 到 running，以及 running 到 succeeded / failed / manual review，并支持 stale running operation 查询。
  - job queue repository 足以支撑 due list、claim / lock、expired lock 发现、running / succeeded / failed / requeue、dead letter 和 manual review 状态写回。
  - 当前仍缺少 worker runtime、runner / scheduler、provider execution、stale recovery runtime、retry runtime、compensation audit integration 和 service 接入。
  - 本轮可以只做 Plan Mode，不修改 `src/**`、不修改 `drizzle/**`、不新增 schema / migration、不实现任何 runtime 代码。
- 完成：
  - 新增 worker claim / lock / stale recovery spec 文档。
  - 新增后续拆分 plan 文档。
  - README 与 roadmap 同步本轮 Plan Mode 状态。
  - 文档明确 worker、operation repository、job queue repository、service、provider 和 audit 的职责边界。
  - 文档明确 claim / lock、stale locked job recovery、stale running operation recovery、retry / requeue、dead letter、manual review、tenant isolation、transaction / consistency、observability 和敏感信息禁区。
- 边界：
  - 不修改 `src/**`。
  - 不修改 `drizzle/**`。
  - 不新增 schema / migration。
  - 不实现 worker runtime。
  - 不实现 worker 主循环。
  - 不实现 provider 调用。
  - 不实现 stale recovery runtime。
  - 不实现 retry runtime。
  - 不实现 dead letter / manual review runtime。
  - 不实现 compensation audit。
  - 不接 service。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
- 后续建议：
  - worker claim / lock / stale recovery runtime 最小实现。
  - worker test-only provider / no-op execution 最小实现。
  - compensation audit integration。
  - service 接入 operation + job queue 创建。
  - retry / dead letter / manual review runtime 最小闭环。
  - observability / safe logging。
  - 真实 provider / secret manager / HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证 compensation worker claim / lock / stale recovery runtime 最小边界

- 分支：`feat/phase23-his-credential-compensation-worker-claim-lock-stale-recovery-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 compensation worker claim / lock / stale recovery runtime 最小边界，只新增 worker 编排层和 worker tests，承接 operation repository 与 job queue repository，不实现常驻 worker。
- 当前 main commit：`77b6fbeb26f2a077cbb2e66e330d02c10933b51d`
- 只读盘点结论：
  - operation repository 足以支撑 worker 最小状态推进，已有 running / manual review / stale running 查询并绑定 `tenantId + connectionId + operationId`。
  - job queue repository 足以支撑 due list、claim / lock、expired lock 查询和 `claimId + claimVersion` 写回校验。
  - 本轮可以不改 schema / migration，不接 service，不接 provider，不写 audit，不新增 API route，不实现 runner / scheduler。
  - worker test-only provider / no-op execution、compensation audit integration、service 接入 operation + job queue 创建、retry / dead letter runtime、runner / scheduler、observability 和真实 provider / secret manager / HIS adapter 必须另开 PR。
- 完成：
  - 新增 `src/modules/institution/server/his-connection-credential-compensation-worker.ts`。
  - 新增 `src/modules/institution/tests/HisConnectionCredentialCompensationWorker.test.ts`。
  - 新增 `createHisConnectionCredentialCompensationWorker` factory，构造参数包含 `operationRepository`、`jobQueueRepository`、`nowProvider`、`claimIdFactory`、`workerId`、`lockDurationMs` 和 `maxBatchSize`。
  - `claimDueCredentialCompensationJobs` 以可信 `tenantId` 读取 due jobs，逐条 claim，claim 成功后使用 repository 返回的 `claimId + claimVersion` 写回 job running，job running 成功后推进 operation running。
  - `recoverExpiredLockedCredentialCompensationJobs` 对 expired claimed job 保守返回 `skipped`，不自动重新 claim；对 expired running job 标记 job manual review，不重复 provider 动作。
  - `recoverStaleRunningCredentialCompensationOperations` 查询 stale running operation 后读取对应 job；找不到 job 或状态无法安全判断时推进 operation manual review，不创建新 job。
  - worker result 只返回稳定状态，不暴露 SQL、stack 或 `DATABASE_URL`。
- 边界：
  - 不新增 schema / migration。
  - 不修改 `drizzle/**`。
  - 不新增 API route。
  - 不修改 service。
  - 不修改 parser / DTO。
  - 不修改 provider / storage。
  - 不修改权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不实现真实 provider 调用。
  - 不实现真实 HIS adapter。
  - 不实现测试连接。
  - 不处理真实凭证。
  - 不接 KMS / Vault / secret manager。
  - 不实现 compensation audit。
  - 不新增 runner / scheduler / cron。
  - 不新增后台常驻进程。
  - 不修改 package、lockfile、`.env` 或 `.codex`。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationWorker.test.ts` 先失败，失败点为 worker module 尚未存在。
  - GREEN：同一命令通过，1 个测试文件，19 个测试。
  - 回归：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationWorker.test.ts src/modules/institution/tests/HisConnectionCredentialCompensationJobQueueRepository.test.ts src/modules/institution/tests/HisConnectionCredentialCompensationOperationRepository.test.ts` 通过，3 个测试文件，50 个测试。
  - 类型与 lint：`./node_modules/.bin/tsc --noEmit` 通过；`./node_modules/.bin/eslint src/modules/institution/server/his-connection-credential-compensation-worker.ts src/modules/institution/tests/HisConnectionCredentialCompensationWorker.test.ts src/modules/institution/server/his-connection-credential-compensation-job-queue-repository.ts src/modules/institution/tests/HisConnectionCredentialCompensationJobQueueRepository.test.ts src/modules/institution/server/his-connection-credential-compensation-operation-repository.ts src/modules/institution/tests/HisConnectionCredentialCompensationOperationRepository.test.ts` 通过。
- 后续建议：
  - worker test-only provider / no-op execution Plan Mode。
  - compensation audit integration Plan Mode。
  - service 接入 compensation operation + job queue 创建。
  - retry / dead letter / manual review runtime 最小闭环。
  - runner / scheduler / cron 独立实现。
  - observability / safe logging。
  - 真实 provider / secret manager / HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证 compensation worker test-only provider / no-op execution Plan Mode

- 分支：`plan/phase23-his-credential-compensation-worker-test-provider-noop-execution`
- 目标：只做 Phase 23 HIS 连接配置凭证 compensation worker test-only provider / no-op execution Plan Mode，承接 worker claim / lock / stale recovery runtime 最小边界，为后续 provider execution completion 编排提供中文边界。
- 当前 main commit：`c68f528c1360cb4eff8f6bac4b40f3361ae0435e`
- 只读盘点结论：
  - 当前 worker 已足够支撑后续 provider execution 编排入口，已能 claim due job、写入 job running、推进 operation running，并提供保守 recovery 入口。
  - operation repository 足够支撑 success / failure / manual review 收口。
  - job queue repository 足够支撑 success / failure / manual review 收口，并通过 `claimId + claimVersion` 拒绝旧 claim 写回。
  - 本轮可以只做 Plan Mode，不实现 provider runtime，不接真实 provider，不处理真实凭证，不写 audit，不新增 runner / scheduler。
- 完成：
  - 新增 test-only provider / no-op execution 边界规划文档。
  - 新增后续拆分计划文档。
  - README 与 roadmap 同步本轮 Plan Mode 状态。
  - 文档明确 provider result 稳定分类：`success`、`retryable_failure`、`unsafe_unknown`、`validation_failed`、`provider_unavailable`、`timeout` 和 `repository_error`。
  - 文档明确 success、retryable failure、unsafe unknown、validation failed、provider unavailable、timeout 和 repository error 的收口策略。
  - 文档明确 worker 与 provider execution 边界、job 与 operation 对齐、claim 写回、事务一致性、audit 边界、租户隔离、敏感信息禁区、safe logging、测试拆分和后续 PR 拆分。
- 边界：
  - 不修改 `src/**`。
  - 不修改 `drizzle/**`。
  - 不新增 schema / migration。
  - 不新增 API route。
  - 不修改 service。
  - 不修改 parser / DTO。
  - 不修改 provider / storage。
  - 不修改权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不实现 provider runtime。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
  - 不实现 compensation audit。
  - 不新增 runner / scheduler / cron。
  - 不修改 package、lockfile、`.env` 或 `.codex`。
- 后续建议：
  - test-only provider / no-op execution runtime 最小实现。
  - success completion runtime。
  - retryable failure completion runtime。
  - unsafe unknown manual review runtime。
  - retry / requeue / backoff runtime。
  - compensation audit integration。
  - service 接入 operation + job queue 创建。
  - runner / scheduler / cron 独立实现。
  - 真实 provider / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证 compensation worker no-op provider execution 最小边界

- 分支：`feat/phase23-his-credential-compensation-worker-test-provider-noop-execution-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 compensation worker test-only / no-op provider execution runtime 最小边界，承接 worker claim / lock / stale recovery runtime 与 no-op execution Plan Mode。
- 当前 main commit：`9f661ed756086f81654451762c003dd6f665280e`
- 只读盘点结论：
  - 当前 worker 可在 dependency factory 中新增可选 provider executor 注入点。
  - worker completion 可只依赖 operation repository 与 job queue repository。
  - job queue repository 已支持 succeeded / failed / manual review 写回，并校验 `claimId + claimVersion`。
  - operation repository 已支持 succeeded / failed / manual review 写回。
  - 本轮可以不改 schema / migration，不接真实 provider，不处理真实凭证，不写 audit，不新增 runner / scheduler。
- 完成：
  - worker 新增 provider execution 稳定分类：`success`、`retryable_failure`、`unsafe_unknown`、`validation_failed`、`provider_unavailable`、`timeout` 和 `repository_error`。
  - worker 新增可选 `providerExecutor` 注入点，输入只包含 tenant / connection / operation、claim、worker 与时间安全上下文。
  - worker 新增 `executeClaimedCredentialCompensationJob`，只执行已 claimed/running 的 job completion 编排，不改 claim / recovery 方法行为。
  - `success` 收口为 job succeeded 后 operation succeeded。
  - `retryable_failure` 与 `provider_unavailable` 收口为 job failed 后 operation failed，不 requeue、不递增 retryCount、不自动 dead letter。
  - `unsafe_unknown` 与 `timeout` 收口为 job manual review 后 operation manual review，不重试、不自动 dead letter。
  - `validation_failed` 在输入不合法或 executor 缺失时不调用 executor，不推进 job / operation。
  - executor 抛错固定收口为 `provider_unavailable`，不暴露底层错误。
  - worker tests 覆盖 provider result 收口、claim 写回、敏感字段禁区、无 fetch / HTTP、无 audit、无 request / header / query / localStorage。
- 边界：
  - 不新增 schema / migration。
  - 不修改 `drizzle/**`。
  - 不新增 API route。
  - 不修改 service。
  - 不修改 parser / DTO。
  - 不修改真实 provider / storage。
  - 不修改权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不实现真实 provider 调用。
  - 不实现真实 HIS adapter。
  - 不实现测试连接。
  - 不处理真实凭证。
  - 不接 KMS / Vault / secret manager。
  - 不实现 compensation audit。
  - 不新增 runner / scheduler / cron。
  - 不新增后台常驻进程。
  - 不修改 package、lockfile、`.env` 或 `.codex`。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationWorker.test.ts` 先失败，失败点为 `executeClaimedCredentialCompensationJob is not a function`。
  - GREEN：同一命令通过，1 个测试文件，36 个测试。
- 后续建议：
  - retry / requeue / backoff runtime Plan Mode。
  - compensation audit integration Plan Mode。
  - service 接入 operation + job queue 创建。
  - runner / scheduler / cron 独立实现。
  - 真实 provider / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证 compensation retry / requeue / backoff runtime Plan Mode

- 分支：`plan/phase23-his-credential-compensation-retry-requeue-backoff-runtime`
- 目标：只做 Phase 23 HIS 连接配置凭证 compensation retry / requeue / backoff runtime Plan Mode，承接 worker no-op provider execution 最小边界，为后续 retry runtime、requeue runtime、backoff、dead letter 和 manual review 实现提供中文边界。
- 当前 main commit：`8f547f8ae1f6b4f30d389a0fd415982581b6860d`
- 只读盘点结论：
  - 当前 worker 已具备 retry / requeue 规划入口，`retryable_failure` 与 `provider_unavailable` 已稳定收口为 failed，但尚未实现 retry policy、backoff 或自动 requeue。
  - 当前 job queue repository 已足够支撑 retry / requeue，包含 requeue、`retryCount`、`maxRetryCount`、`nextAttemptAt` 和 `claimId + claimVersion` 写回保护。
  - 当前 operation repository 已足够支撑 retry count、failed 与 running 状态推进，但不承担调度判断。
  - 可进入 retry 的 provider result 应先限于 `retryable_failure` 与受上限保护的 `provider_unavailable`。
  - `timeout` 默认不进入 retry，除非后续能证明 provider 调用无副作用。
  - `unsafe_unknown` 与 `validation_failed` 不进入 retry。
  - 本轮可以只做 Plan Mode，不新增 schema / migration，不接真实 provider，不处理真实凭证，不写 audit，不新增 runner / scheduler。
- 完成：
  - 新增 retry / requeue / backoff runtime 边界规划文档。
  - 新增后续拆分计划文档。
  - README 与 roadmap 同步本轮 Plan Mode 状态。
  - 文档明确 worker 与 retry policy 职责边界、provider result 到 retry 决策映射、`retryCount` / `maxRetryCount`、`nextAttemptAt`、backoff、抖动、retry exhausted 分流、job 与 operation 对齐、claim 写回、事务一致性、audit 边界、租户隔离、敏感信息禁区、安全日志、测试拆分和后续 PR 拆分。
- 边界：
  - 不修改 `src/**`。
  - 不修改 `drizzle/**`。
  - 不新增 schema / migration。
  - 不新增 API route。
  - 不修改 service。
  - 不修改 parser / DTO。
  - 不修改 provider / storage。
  - 不修改权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不实现 retry runtime。
  - 不实现 requeue runtime。
  - 不实现 backoff 代码。
  - 不实现 dead letter runtime。
  - 不实现 manual review runtime。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
  - 不实现 compensation audit。
  - 不新增 runner / scheduler / cron。
  - 不新增后台常驻进程。
  - 不修改 package、lockfile、`.env` 或 `.codex`。
- 后续建议：
  - retry policy 纯决策 helper 最小实现。
  - worker retry / requeue runtime 最小实现。
  - operation retry count 对齐最小实现。
  - retry exhausted dead letter runtime。
  - retry exhausted manual review runtime。
  - compensation audit integration。
  - runner / scheduler / cron 独立规划与实现。
  - 真实 provider / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证 compensation retry policy 纯决策 helper 最小边界

- 分支：`feat/phase23-his-credential-compensation-retry-policy-helper-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 compensation retry policy 纯决策 helper 最小边界，承接 retry / requeue / backoff runtime Plan Mode，为后续 worker retry / requeue runtime 提供稳定决策输入。
- 当前 main commit：`94ee6509e51f5ee0cad5d93988c87b0b23025dbf`
- 只读盘点结论：
  - 当前 worker provider result 分类已足够支撑 retry policy，包含 `success`、`retryable_failure`、`unsafe_unknown`、`validation_failed`、`provider_unavailable`、`timeout` 和 `repository_error`。
  - 当前 job queue repository 的 `retryCount`、`maxRetryCount` 与 `nextAttemptAt` 边界清楚，`retryCount >= maxRetryCount` 不得 requeue。
  - 当前 operation retry count 只做业务状态对齐，不参与调度判断。
  - 本轮可以只新增纯 helper，不改 worker runtime，不实现 requeue runtime、dead letter runtime 或 manual review runtime。
  - 本轮可以不新增 schema / migration，不接真实 provider，不处理真实凭证。
- 完成：
  - 新增 `src/modules/institution/server/his-connection-credential-compensation-retry-policy.ts`。
  - 新增 `src/modules/institution/tests/HisConnectionCredentialCompensationRetryPolicy.test.ts`。
  - 固定 provider result 白名单、decision 白名单与 reason 白名单。
  - `retryable_failure` 与 `provider_unavailable` 在 `retryCount < maxRetryCount` 时返回 requeue，并计算 fixed backoff 的 `nextAttemptAt`。
  - `retryable_failure` 与 `provider_unavailable` 在 `retryCount >= maxRetryCount` 时返回 dead_letter。
  - `timeout` 与 `unsafe_unknown` 返回 manual_review。
  - `validation_failed` 与 `repository_error` 不 requeue。
  - unknown provider result、非法 jobState、非法 retry counter 和非法 backoff config 返回 validation_failed。
  - jitter 只影响 `nextAttemptAt`，不影响 retry 上限判断。
- 边界：
  - 不修改 `drizzle/**`。
  - 不新增 schema / migration。
  - 不新增 API route。
  - 不修改 service。
  - 不修改 parser / DTO。
  - 不修改 provider / storage。
  - 不修改权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 worker runtime。
  - 不实现 requeue runtime。
  - 不实现 dead letter runtime。
  - 不实现 manual review runtime。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
  - 不实现 compensation audit。
  - 不新增 runner / scheduler / cron。
  - 不新增后台常驻进程。
  - 不修改 package、lockfile、`.env` 或 `.codex`。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationRetryPolicy.test.ts` 先失败，失败点为 retry policy helper module 尚未存在。
  - GREEN：同一命令通过，1 个测试文件，16 个测试。
  - 回归：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationRetryPolicy.test.ts src/modules/institution/tests/HisConnectionCredentialCompensationWorker.test.ts src/modules/institution/tests/HisConnectionCredentialCompensationJobQueueRepository.test.ts src/modules/institution/tests/HisConnectionCredentialCompensationOperationRepository.test.ts src/server/db/tests/Schema.test.ts` 通过，5 个测试文件，115 个测试。
- 后续建议：
  - worker retry / requeue runtime 最小实现。
  - operation retry count 对齐 Plan Mode 或最小实现。
  - retry exhausted dead letter runtime。
  - retry exhausted manual review runtime。
  - compensation audit integration。
  - runner / scheduler / cron 独立规划与实现。
  - 真实 provider / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证 compensation worker retry / requeue runtime 最小边界

- 分支：`feat/phase23-his-credential-compensation-worker-retry-requeue-runtime-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 compensation worker retry / requeue runtime 最小边界，承接 retry policy 纯决策 helper，让 `retryable_failure` 与 `provider_unavailable` 在未达上限时重新排队。
- 当前 main commit：`358ef16e77e2dafefe4422f43891e118ed81a09c`
- 只读盘点结论：
  - 当前 worker no-op provider execution 适合接入 retry policy，`retryable_failure` 与 `provider_unavailable` 已有 failed completion 入口。
  - 当前 retry policy helper 可复用，已覆盖 provider result、jobState、retryCount、maxRetryCount 与 fixed backoff `nextAttemptAt`。
  - 当前 job queue repository requeue 足够，已校验 `claimId + claimVersion`，并由 repository 递增 job retryCount、清理 claim 元数据和更新 `nextAttemptAt`。
  - 当前 operation repository 已有 retry count 递增能力，但 requeue 成功后再对齐 operation retry count 会引入一致性语义，本轮不接入，建议另开最小实现或 Plan Mode。
  - 本轮可以不新增 schema / migration，不实现 dead letter runtime，不实现 manual review runtime，不写 audit，不接真实 provider，不处理真实凭证，不新增 runner / scheduler。
- 完成：
  - worker 在 `retryable_failure` 与 `provider_unavailable` 的 job failed + operation failed 成功后调用 `decideHisConnectionCredentialCompensationRetry`。
  - worker 读取 failed job record 的 `jobState`、`retryCount` 与 `maxRetryCount` 作为 retry policy 输入。
  - retry policy 返回 `requeue` 时，worker 使用 helper 返回的 `nextAttemptAt` 调用 `requeueCredentialCompensationJob`。
  - requeue 写回携带 `tenantId + connectionId + operationId + claimId + claimVersion + now`。
  - `retryCount >= maxRetryCount`、`timeout`、`unsafe_unknown`、`validation_failed` 与 retry policy `validation_failed` 均不 requeue。
  - requeue `conflict` 与 `repository_error` 收敛为稳定 worker result。
- 边界：
  - 不直接修改 job retryCount。
  - 不调用 `incrementCredentialCompensationOperationRetryCount`。
  - 不实现 dead letter runtime。
  - 不实现 manual review runtime。
  - 不写 compensation audit。
  - 不接真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
  - 不新增 runner / scheduler / cron。
  - 不新增 schema / migration。
  - 不修改 `drizzle/**`。
  - 不新增 API route。
  - 不修改 service、parser / DTO、provider / storage、权限、audit domain / reason / query whitelist 或 audit repository。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationWorker.test.ts` 先失败，失败点为 worker 尚未调用 requeue。
  - GREEN：同一命令通过，1 个测试文件，42 个测试。
- 后续建议：
  - operation retry count 对齐 Plan Mode 或最小实现。
  - retry exhausted dead letter runtime。
  - retry exhausted manual review runtime。
  - compensation audit integration。
  - runner / scheduler / cron 独立规划与实现。
  - 真实 provider / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证 compensation operation retry count 对齐规划

- 分支：`docs/phase23-his-credential-compensation-operation-retry-count-alignment-plan`
- 目标：只做 Phase 23 HIS 连接配置凭证 compensation operation retry count 对齐规划，承接 worker retry / requeue 运行时最小边界，为后续 operation retry count 运行时 PR 明确职责、失败收口和一致性边界。
- 当前 main commit：`d1e5a87f74a0a8f6093ae1fe1132bb635945031b`
- 只读盘点结论：
  - 当前 worker 已在 `retryable_failure` 与 `provider_unavailable` 的 failed completion 后接入 retry policy helper，并在未达上限时调用 `requeueCredentialCompensationJob`。
  - 当前 job queue repository requeue 已负责递增 job `retryCount`、更新 `nextAttemptAt`、清理 claim / lock 元数据，并受 `claimId + claimVersion` 保护。
  - 当前 operation repository 已有 `incrementCredentialCompensationOperationRetryCount`，且只允许 `compensation_failed` 或 `manual_review_required` 状态递增。
  - 当前 worker 未接入 operation retry count 对齐。
  - 本轮满足 docs-only 规划条件，不需要 schema / migration，不需要修改 job queue repository，不需要修改 operation repository。
- 完成：
  - 新增 `docs/superpowers/plans/2026-06-07-phase23-his-connection-credential-compensation-operation-retry-count-alignment.md`。
  - 文档明确 job `retryCount` 是调度事实，operation `retryCount` 是业务状态展示与补偿事实对齐。
  - 文档明确 operation retry count 不参与 retry policy、不参与 `nextAttemptAt` 计算、不参与 job queue 调度。
  - 文档推荐后续运行时在 requeue 成功后再递增 operation retry count。
  - 文档明确 operation retry count 递增失败时不回滚 job requeue，允许短暂不一致。
  - 文档明确短暂不一致后续由恢复流程、人工复核和可观测性兜底。
  - README 与 roadmap 已同步本阶段状态。
- 边界：
  - 不修改 `src/**`。
  - 不修改 `drizzle/**`。
  - 不新增 schema / migration。
  - 不新增 API route。
  - 不修改 service、parser / DTO、provider / storage、权限、audit domain / reason / query whitelist 或 audit repository。
  - 不实现 operation retry count 运行时。
  - 不实现 retry exhausted dead letter 运行时。
  - 不实现 retry exhausted manual review 运行时。
  - 不接 compensation audit。
  - 不接 service / route。
  - 不接 runner / scheduler / cron。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
- 后续建议：
  - operation retry count 对齐运行时最小实现。
  - operation retry count 对齐失败恢复流程 / 人工复核规划。
  - retry exhausted dead letter 运行时。
  - retry exhausted manual review 运行时。
  - compensation audit integration。
  - runner / scheduler / cron 独立规划与实现。
  - service 创建 operation + job queue 接入。
  - 真实 provider / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证 compensation operation retry count 对齐 runtime 最小边界

- 分支：`feat/phase23-his-credential-compensation-operation-retry-count-runtime-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 compensation operation retry count 对齐 runtime 最小边界，承接 operation retry count 对齐规划和 worker retry / requeue runtime。
- 当前 main commit：`425cd826f4a806206dfce96111e05d1a16f09ad3`
- 只读盘点结论：
  - 当前 worker requeue 成功分支适合接入 operation retry count 对齐。
  - 当前 operation repository 的 `incrementCredentialCompensationOperationRetryCount` 可复用，输入已绑定 `tenantId + connectionId + operationId`。
  - 当前 operation retry count 返回状态可映射到现有 worker 稳定结果，不需要新增 worker result status。
  - requeue 失败时应跳过 operation retry count。
  - requeue 成功但 operation retry count 失败时可以不回滚 job requeue，允许短暂不一致。
  - 本轮不需要 schema / migration，不需要修改 job queue repository 或 operation repository，不需要 service / route / audit / runner / scheduler，也不需要真实 provider、真实凭证、测试连接或真实 HIS adapter。
- 完成：
  - worker 仅在 `requeueCredentialCompensationJob` 返回 `ok` 后调用 `incrementCredentialCompensationOperationRetryCount`。
  - operation retry count 调用输入固定为 `tenantId + connectionId + operationId`。
  - operation retry count `ok` 时 worker 返回 `ok`。
  - operation retry count `repository_error`、`invalid_state_transition`、`not_found`、`conflict` 或 `validation_failed` 时返回现有稳定 worker result。
  - requeue 失败、retry exhausted、timeout、unsafe unknown、validation failed 和 retry policy validation failed 均不递增 operation retry count。
  - 补充 worker tests 覆盖 requeue 后调用顺序、失败不回滚、敏感信息不暴露、dead letter / manual review 不触发，以及真实 provider / audit / request 读取禁区。
  - README 与 roadmap 已同步本阶段状态。
- 边界：
  - 不新增 schema / migration。
  - 不修改 `drizzle/**`。
  - 不修改 job queue repository 或 operation repository。
  - 不新增 API route。
  - 不修改 service、parser / DTO、provider / storage、权限、audit domain / reason / query whitelist 或 audit repository。
  - 不实现 retry exhausted dead letter runtime。
  - 不实现 retry exhausted manual review runtime。
  - 不写 compensation audit。
  - 不接 service / route。
  - 不接 runner / scheduler / cron。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
- 验证：
  - 先更新 worker tests 并确认缺失 operation retry count 调用时失败。
  - 补齐 worker runtime 后，单独运行 `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationWorker.test.ts` 通过。

## Phase 23 HIS 连接配置凭证 compensation retry exhausted dead letter runtime Plan Mode

- 分支：`docs/phase23-his-credential-compensation-retry-exhausted-dead-letter-plan`
- 目标：只做 Phase 23 HIS 连接配置凭证 compensation retry exhausted dead letter runtime Plan Mode，承接 worker retry / requeue runtime 和 operation retry count 对齐 runtime，为后续 dead letter runtime PR 明确最小边界。
- 当前 main commit：`f8f969854bbfdb91328b83a5ff6ffaf73a2b50f2`
- 只读盘点结论：
  - retry policy helper 仅在 failed job 且 `retryCount >= maxRetryCount` 时返回 `dead_letter`，reason 为 `retry_exhausted`；在当前 worker 可达路径里，这只对应 `retryable_failure` 或 `provider_unavailable` 达到上限。
  - 当前 worker 对 retry policy 非 `requeue`、非 `validation_failed` 的结果返回 `null`，因此 retry exhausted 目前只停留在 job failed + operation failed 后返回稳定 `ok`。
  - job queue repository 已有 `markCredentialCompensationJobDeadLettered`，输入已绑定 `tenantId + connectionId + operationId + claimId + claimVersion + now + deadLetterReason`。
  - job queue repository 已有 `retry_exhausted` dead letter reason，不需要新增 enum、schema 或 migration。
  - operation repository 不需要改；retry exhausted dead letter 最小边界建议只 dead letter job，operation 保持 `compensation_failed`。
  - retry exhausted manual review、compensation audit、service / route、runner / scheduler、真实 provider、真实凭证、测试连接和真实 HIS adapter 均需后置到独立 PR。
- 完成：
  - 新增 `docs/superpowers/plans/2026-06-07-phase23-his-connection-credential-compensation-retry-exhausted-dead-letter.md`。
  - 文档明确 dead letter runtime 只处理 retry policy 返回 `dead_letter` 的场景。
  - 文档明确 `timeout` 与 `unsafe_unknown` 继续后置 manual review，`validation_failed` 不进入 dead letter。
  - 文档明确 dead letter 写回必须携带 `claimId + claimVersion`，reason 固定使用 `retry_exhausted`。
  - 文档明确 dead letter 后不递增 operation retry count，不回滚 operation failed，允许 job failed + operation failed 但 dead letter 写回失败的短暂状态。
  - 文档明确现有 repository 会清理 `lockedUntil` 并写 `completedAt`，`claimId`、`claimedBy`、`claimedAt` 和 `claimVersion` 作为终态上下文保留。
  - README 与 roadmap 已同步本阶段状态。
- 边界：
  - 不修改 `src/**`。
  - 不修改 `drizzle/**`。
  - 不新增 schema / migration。
  - 不修改 job queue repository 或 operation repository。
  - 不新增 API route。
  - 不修改 service、parser / DTO、provider / storage、权限、audit domain / reason / query whitelist 或 audit repository。
  - 不实现 retry exhausted dead letter runtime。
  - 不实现 retry exhausted manual review runtime。
  - 不写 compensation audit。
  - 不接 service / route。
  - 不接 runner / scheduler / cron。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
- 后续建议：
  - retry exhausted dead letter runtime 最小实现。
  - retry exhausted manual review runtime 规划。
  - retry exhausted manual review runtime 最小实现。
  - dead letter / manual review 恢复流程规划。
  - compensation audit integration。
  - runner / scheduler / cron 独立规划与实现。
  - service 创建 operation + job queue 接入。
  - 真实 provider / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证 compensation retry exhausted dead letter runtime 最小边界

- 分支：`feat/phase23-his-credential-compensation-retry-exhausted-dead-letter-runtime-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 compensation retry exhausted dead letter runtime 最小边界，承接 retry exhausted dead letter Plan Mode、worker retry / requeue runtime 和 operation retry count 对齐 runtime。
- 当前 main commit：`c8226a1139548978073b0477e08e6dc733785ddf`
- 只读盘点结论：
  - 当前 worker 已能在 `maybeRequeueFailedCredentialCompensationJob` 中接收 retry policy `dead_letter` decision。
  - 当前 retry policy `dead_letter` 在 worker 可达路径中只对应 `retryable_failure` 或 `provider_unavailable` 达到 `retryCount >= maxRetryCount`。
  - 当前 job queue repository 的 `markCredentialCompensationJobDeadLettered` 可复用，输入必须携带 `tenantId + connectionId + operationId + claimId + claimVersion + now + deadLetterReason`。
  - 当前 dead letter reason 已包含 `retry_exhausted`。
  - dead letter 写回失败可映射为现有 worker result，不需要新增 worker result status。
  - 本轮不需要 schema / migration，不需要修改 job queue repository 或 operation repository，不需要 service / route / audit / runner / scheduler，也不需要真实 provider、真实凭证、测试连接或真实 HIS adapter。
- 完成：
  - worker 在 retry policy 返回 `dead_letter` 且 reason 为 `retry_exhausted` 时调用 `markCredentialCompensationJobDeadLettered`。
  - dead letter 写回参数固定包含 `tenantId + connectionId + operationId + claimId + claimVersion + now + deadLetterReason: retry_exhausted`。
  - dead letter 成功时 worker 返回稳定 `ok`。
  - dead letter `repository_error`、`conflict`、`invalid_state_transition`、`validation_failed` 或 `not_found` 时返回现有稳定 worker result。
  - dead letter 后不调用 requeue，不递增 operation retry count，不调用 job manual review 或 operation manual review。
  - dead letter 写回失败时不回滚 job failed 或 operation failed。
  - 补充 worker tests 覆盖 retryable failure / provider unavailable 达到上限、写回参数、调用顺序、失败状态、低于上限继续 requeue、timeout / unsafe unknown / validation failed 不进入 dead letter，以及真实 provider / audit / request 读取禁区。
  - README 与 roadmap 已同步本阶段状态。
- 边界：
  - 不新增 schema / migration。
  - 不修改 `drizzle/**`。
  - 不修改 job queue repository 或 operation repository。
  - 不新增 API route。
  - 不修改 service、parser / DTO、provider / storage、权限、audit domain / reason / query whitelist 或 audit repository。
  - 不实现 retry exhausted manual review runtime。
  - 不写 compensation audit。
  - 不接 service / route。
  - 不接 runner / scheduler / cron。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
- 验证：
  - 先更新 worker tests 并确认缺失 dead letter 调用时失败。
  - 补齐 worker runtime 后，单独运行 `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationWorker.test.ts` 通过。

## Phase 23 HIS 连接配置凭证 compensation retry exhausted manual review Plan Mode

- 分支：`docs/phase23-his-credential-compensation-retry-exhausted-manual-review-plan`
- 目标：只做 Phase 23 HIS 连接配置凭证 compensation retry exhausted manual review 运行时边界规划，承接 retry exhausted dead letter runtime 最小边界。
- 当前 main commit：`1ac26d4617b97a4fbdf6352dd8f3f09829cf812a`
- 只读盘点结论：
  - dead letter 成功后的业务触发点可以作为 operation manual review 的后续入口，但工程落地应作为单独 recovery / review 流程或独立运行时 PR。
  - job 已进入 `dead_lettered` 后不应再调用 job manual review；现有 job manual review repository 也不接受 `dead_lettered` 状态。
  - 当前 retry exhausted dead letter 路径已经先把 operation 写为 `compensation_failed`。
  - 现有 operation manual review repository 只允许 `compensation_running` 到 `manual_review_required`，不能直接复用来完成 `compensation_failed` 到 `manual_review_required`。
  - 如果后续要求 dead letter 成功后推进 operation manual review，必须先单独规划 operation repository 状态转换边界。
  - 本轮不需要 schema / migration，不需要修改 job queue repository，不需要扩展 worker result status；如果新增 manual review reason，则需要另开 schema / audit Plan Mode。
- 完成：
  - 新增 `docs/superpowers/plans/2026-06-07-phase23-his-connection-credential-compensation-retry-exhausted-manual-review.md`。
  - 文档明确 retry exhausted manual review 的触发条件仅限 `dead_letter` / `retry_exhausted` 成功后的后续处理。
  - 文档明确 job dead letter 是调度终态，operation manual review 是未来业务终态。
  - 文档明确不调用 job manual review，不 requeue，不递增 operation retry count，不回滚 job dead letter。
  - 文档明确 operation manual review 写回输入只绑定 `tenantId + connectionId + operationId`，`claimId + claimVersion` 只属于 job dead letter 写回保护。
  - 文档明确 manual review 写回失败时允许短暂不一致，并映射为既有稳定 worker result。
  - README 与 roadmap 已同步本阶段状态。
- 边界：
  - 不修改 `src/**`。
  - 不修改 `drizzle/**`。
  - 不新增 schema / migration。
  - 不修改 job queue repository 或 operation repository。
  - 不新增 API route。
  - 不修改 service、parser / DTO、provider / storage、权限、audit domain / reason / query whitelist 或 audit repository。
  - 不实现 retry exhausted manual review runtime。
  - 不写 compensation audit。
  - 不接 service / route。
  - 不接 runner / scheduler / cron。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
- 后续建议：
  - operation repository 状态转换边界 Plan Mode 或最小实现。
  - retry exhausted manual review runtime 最小实现。
  - dead letter / manual review recovery 或人工复核视图规划。
  - compensation audit integration。
  - service 创建 operation + job queue 接入。
  - runner / scheduler / cron 独立规划与实现。
  - 真实 provider / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证补偿 operation failed manual review 状态转换边界 Plan Mode

- 分支：`docs/phase23-his-credential-compensation-operation-failed-manual-review-transition-plan`
- 目标：只做 Phase 23 HIS 凭证补偿 operation repository failed 到 manual review 状态转换边界规划，承接 retry exhausted manual review Plan Mode，为后续 repository runtime 和 worker runtime 拆分明确边界。
- 当前 main commit：`d62ef647a7465db60ec4860b1b3bd40d032a22d2`
- 只读盘点结论：
  - 当前 `markCredentialCompensationOperationManualReviewRequired` 复用 running completion 判断，只允许 `compensation_running -> manual_review_required`。
  - 当前实现会明确拒绝 `compensation_failed -> manual_review_required`，返回稳定 `invalid_state_transition`，不会写库。
  - retry exhausted dead letter 路径先写 job failed，再写 operation failed；dead letter 成功后只返回稳定 `ok`，不 requeue、不递增 operation retry count、不推进 manual review，因此 operation 停在 `compensation_failed`。
  - 应该允许 `compensation_failed -> manual_review_required` 作为 retry exhausted 后的业务复核终态推进能力，但不应扩大现有 running manual review 方法。
  - 推荐后续新增 `markFailedCredentialCompensationOperationManualReviewRequired`，只允许 failed 到 manual review；现有 `markCredentialCompensationOperationManualReviewRequired` 继续服务 unsafe unknown、timeout 和 stale running recovery。
  - 本轮不需要 schema / migration，不需要新增 manual review reason，不需要修改 operation repository、worker 或 job queue repository，不需要接 audit、service、route、runner / scheduler，也不需要真实 provider、真实凭证、测试连接或真实 HIS adapter。
- 完成：
  - 新增 `docs/superpowers/plans/2026-06-07-phase23-his-connection-credential-compensation-operation-failed-manual-review-transition.md`。
  - 文档明确当前 operation 状态机现状和 failed 到 manual review 的业务理由。
  - 文档明确复用现有方法会影响 stale running recovery 语义边界，因此推荐新增窄语义方法。
  - 文档明确专用方法只绑定 `tenantId + connectionId + operationId`，不使用 `claimId + claimVersion`。
  - 文档明确状态转换允许范围、拒绝范围、租户 / connectionId / operationId 隔离、并发边界和敏感信息禁区。
  - 文档明确 repository、worker、job queue repository、audit、service / route、runner / scheduler 和真实外部系统的后续拆分。
  - README 与 roadmap 已同步本阶段状态。
- 边界：
  - 不修改 `src/**`。
  - 不修改 `drizzle/**`。
  - 不新增 schema / migration。
  - 不修改 operation repository。
  - 不修改 worker。
  - 不修改 job queue repository。
  - 不新增 API route。
  - 不修改 service、parser / DTO、provider / storage、权限、audit domain / reason / query whitelist 或 audit repository。
  - 不新增 manual review reason。
  - 不实现 operation repository 状态转换 runtime。
  - 不实现 retry exhausted manual review runtime。
  - 不写 compensation audit。
  - 不接 service / route。
  - 不接 runner / scheduler / cron。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
- 后续建议：
  - operation repository failed manual review 状态转换 runtime。
  - retry exhausted manual review worker runtime。
  - dead letter / manual review recovery 或人工复核视图规划。
  - compensation audit integration。
  - service 创建 operation + job queue 接入。
  - runner / scheduler / cron 独立规划与实现。
  - 真实 provider / 真实凭证 / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证补偿 retry exhausted manual review worker runtime 最小边界

- 分支：`feat/phase23-his-credential-compensation-retry-exhausted-manual-review-worker-runtime-min`
- 目标：实现 Phase 23 HIS 凭证补偿 retry exhausted manual review worker runtime 最小边界，承接 retry exhausted manual review Plan Mode、retry exhausted dead letter runtime 和 operation failed manual review repository runtime。
- 当前 main commit：`aa2d57787702cd645b20216a948bbfa450b74385`
- 只读盘点结论：
  - 当前 worker dead letter 成功分支位于 `maybeRequeueFailedCredentialCompensationJob`。
  - 当前 worker 已在 retry policy 返回 `dead_letter` 且 reason 为 `retry_exhausted` 时调用 `markCredentialCompensationJobDeadLettered`。
  - 当前 operation repository 已有 `markFailedCredentialCompensationOperationManualReviewRequired`，输入只需要 `tenantId + connectionId + operationId`。
  - dead letter 写回失败时应跳过 operation manual review。
  - dead letter 成功但 operation manual review 失败时可以不回滚 job dead letter，并映射为现有 worker result。
  - 本轮不需要新增 worker result status，不需要 schema / migration，不需要修改 operation repository 或 job queue repository，不需要接 audit、service / route、runner / scheduler，也不需要真实 provider、真实凭证、测试连接或真实 HIS adapter。
- 完成：
  - worker 在 job dead letter 成功后调用 `markFailedCredentialCompensationOperationManualReviewRequired`。
  - operation manual review 调用参数只包含 `tenantId + connectionId + operationId`。
  - operation manual review 成功后 worker 返回稳定 `ok`。
  - operation manual review 失败后 worker 返回现有稳定 result，不回滚 job dead letter。
  - dead letter 失败时不调用 operation manual review。
  - retryCount 未达上限的 requeue 路径不调用 operation manual review。
  - `timeout`、`unsafe_unknown`、`validation_failed` 和 retry policy `validation_failed` 不进入 retry exhausted manual review。
  - 不调用 job manual review，不调用 running manual review 方法，不 requeue，不递增 operation retry count。
  - 补充 worker tests 覆盖 retryable_failure / provider_unavailable 达到上限、dead letter 成功后调用顺序、operation manual review 失败状态、dead letter 失败状态、非 retry exhausted 分支和敏感信息禁区。
  - README 与 roadmap 已同步本阶段状态。
- 边界：
  - 不新增 schema / migration。
  - 不修改 `drizzle/**`。
  - 不修改 operation repository。
  - 不修改 job queue repository。
  - 不新增 API route。
  - 不修改 service、parser / DTO、provider / storage、权限、audit domain / reason / query whitelist 或 audit repository。
  - 不新增 manual review reason。
  - 不实现 compensation audit。
  - 不接 service / route。
  - 不接 runner / scheduler / cron。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
- 验证：
  - 先写 worker tests 并确认 worker 未调用 failed operation manual review 时失败。
  - 补齐 worker runtime 后，单独运行 `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationWorker.test.ts` 通过。
- 后续建议：
  - dead letter / manual review recovery 或人工复核视图规划。
  - compensation audit integration。
  - service 创建 operation + job queue 接入。
  - runner / scheduler / cron 独立规划与实现。
  - 真实 provider / 真实凭证 / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证补偿 dead letter manual review recovery 边界规划

- 分支：`docs/phase23-his-credential-compensation-dead-letter-manual-review-recovery-plan`
- 目标：只做 Phase 23 HIS 凭证补偿 dead letter / manual review recovery docs-only 规划，承接 retry exhausted manual review worker runtime 最小边界，为后续 recovery / review flow 拆分明确安全状态组合、查询能力、幂等写回和后置集成。
- 当前 main commit：`b2bacb61591e9dfef1c7cf44a83e9694ad793685`
- 只读盘点结论：
  - 当前 job queue repository 能单条读取并识别 `dead_lettered` job，但没有批量列出 retry exhausted dead letter job 的专用入口。
  - 当前 operation repository 能单条读取并识别 `compensation_failed` operation，但没有批量列出 failed operation 的专用入口。
  - 当前没有直接根据 job + operation 组合批量识别 job `dead_lettered` + operation `compensation_failed` 短暂不一致的能力。
  - 后续 runtime 若要自动 recovery，建议新增 repository 查询方法；本次只规划，不实现。
  - 本轮不需要 schema / migration，不需要修改 worker、job queue repository、operation repository、audit、service / route、runner / scheduler，也不需要真实 provider、真实凭证、测试连接或真实 HIS adapter。
- 完成：
  - 新增 `docs/superpowers/plans/2026-06-07-phase23-his-connection-credential-compensation-dead-letter-manual-review-recovery.md`。
  - 文档明确 job `dead_lettered` + reason `retry_exhausted` + operation `compensation_failed` 的安全不一致识别。
  - 文档明确 recovery / review flow 独立于 worker 主路径。
  - 文档明确恢复动作只调用 `markFailedCredentialCompensationOperationManualReviewRequired({ tenantId, connectionId, operationId })`。
  - 文档明确不回滚 job dead letter、不 requeue、不重新调用 provider、不递增 operation retry count。
  - 文档明确 repository 查询能力、人工复核视图、audit integration、service / route、runner / scheduler 和真实外部系统均后置拆分。
- 边界：
  - 不修改 `src/**`。
  - 不修改 `drizzle/**`。
  - 不新增 schema / migration。
  - 不新增 API route。
  - 不修改 service、audit runtime、worker runtime、job queue runtime、operation repository runtime、runner / scheduler / cron。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
  - 不修改 package / lockfile、`.env` 或 `.codex/**`。
- 后续建议：
  - repository 安全 candidate 查询能力最小 PR。
  - recovery service 最小 PR。
  - 人工复核视图规划或实现 PR。
  - compensation audit integration。
  - runner / scheduler / cron 独立规划与实现。
  - service / route 权限与输入白名单接入。
  - 真实 provider / 真实凭证 / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置凭证补偿 operation failed manual review 状态转换 runtime 最小边界

- 分支：`feat/phase23-his-credential-compensation-operation-failed-manual-review-repository-min`
- 目标：实现 Phase 23 HIS 凭证补偿 operation repository failed 到 manual review 状态转换 runtime 最小边界，承接 operation failed manual review 状态转换边界 Plan Mode，为 retry exhausted manual review worker runtime 提供 repository 前置能力。
- 当前 main commit：`d654caf57973a44bf1fb22c219ac2ab83c593b2a`
- 只读盘点结论：
  - 当前 operation repository 文件为 `src/modules/institution/server/his-connection-credential-compensation-operation-repository.ts`。
  - 当前 `markCredentialCompensationOperationManualReviewRequired` 只允许 `compensation_running -> manual_review_required`。
  - 当前已有 `normalizeConnectionLookupInput`、`findCompensationOperationByConnection`、`rowMatchesConnectionLookup`、稳定 mutation result 和 `repository_error` 脱敏收口，可复用实现 failed 到 manual review 的专用方法。
  - 当前 operation repository test 文件为 `src/modules/institution/tests/HisConnectionCredentialCompensationOperationRepository.test.ts`。
  - 本轮不需要 schema / migration，不需要修改 `drizzle/**`、worker、job queue repository、service / route、audit、runner / scheduler，也不需要真实 provider、真实凭证、测试连接或真实 HIS adapter。
- 完成：
  - 新增 `markFailedCredentialCompensationOperationManualReviewRequired`。
  - 该方法只允许 `compensation_failed -> manual_review_required`。
  - 写回继续绑定 `tenantId + connectionId + operationId`，不使用 `claimId + claimVersion`。
  - 成功时只写入 state、manualReviewRequired、updatedAt 和 completedAt。
  - 成功后不修改 retryCount、lastAttemptAt、operationId、connectionId 或 tenantId。
  - 保持现有 `markCredentialCompensationOperationManualReviewRequired` 行为不变，继续只服务 `compensation_running -> manual_review_required`。
  - 补充 operation repository tests 覆盖 failed 正向、写回字段、retryCount / lastAttemptAt 不变、scope 隔离、非法状态拒绝、非法输入 validation_failed、repository_error 脱敏和现有 running 方法不变。
  - README 与 roadmap 已同步本阶段状态。
- 边界：
  - 不新增 schema / migration。
  - 不修改 `drizzle/**`。
  - 不修改 worker。
  - 不修改 job queue repository。
  - 不新增 API route。
  - 不修改 service、parser / DTO、provider / storage、权限、audit domain / reason / query whitelist 或 audit repository。
  - 不新增 manual review reason。
  - 不实现 retry exhausted manual review worker runtime。
  - 不实现 compensation audit。
  - 不接 service / route。
  - 不接 runner / scheduler / cron。
  - 不调用真实 provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS adapter。
- 验证：
  - 先写 operation repository tests 并确认新方法缺失时失败。
  - 补齐 repository runtime 后，单独运行 `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationOperationRepository.test.ts` 通过。
- 后续建议：
  - retry exhausted manual review worker runtime。
  - dead letter / manual review recovery 或人工复核视图规划。
  - compensation audit integration。
  - service 创建 operation + job queue 接入。
  - runner / scheduler / cron 独立规划与实现。
  - 真实 provider / 真实凭证 / 测试连接 / 真实 HIS adapter Plan Mode。

## Phase 23 HIS 连接配置测试连接与健康检查边界规划

- 分支：`docs/phase23-his-connection-test-connection-health-check-plan`
- 目标：只做 Phase 23 HIS 连接配置测试连接 / 健康检查 docs-only 边界规划，停止继续扩张 compensation recovery 链路，把下一主线收敛回测试连接与健康状态。
- 当前 main commit：`97d87a2c1639bfc4407fb5824999469ee844defa`
- 只读盘点结论：
  - 本地 `main` 已同步 `origin/main`，两者都位于 `97d87a2c1639bfc4407fb5824999469ee844defa`。
  - PR #192 已合并，且只完成 dead letter / manual review recovery Plan Mode。
  - README、roadmap、2026-06-07 devlog 和 Phase 23 连接配置 / 凭证 / provider / compensation 相关计划均显示：测试连接、真实 provider、真实凭证、真实 secret manager 和真实 HIS adapter 仍未开始。
  - 最新 recovery Plan 已把 candidate 查询、recovery service、人工复核视图、audit integration、service / route 和 runner / scheduler 全部后置，本轮不继续推进。
- 完成：
  - 新增 `docs/superpowers/plans/2026-06-07-phase23-his-connection-test-connection-health-check.md`。
  - 文档明确测试连接 / 健康检查应优先于继续实现 compensation recovery runtime。
  - 文档规划连接配置详情页作为主入口，凭证管理页作为次入口，状态操作后不自动测试连接。
  - 文档明确测试连接必须通过受控 credential provider，不从前端、query、header、localStorage 或普通 DTO 读取凭证明文。
  - 文档明确不得返回凭证明文、token、secret、连接串、外部完整请求 / 响应体、raw HIS payload、SQL、stack 或 `DATABASE_URL`。
  - 文档明确健康状态复用 `unknown / healthy / degraded / failed`，并规划 `lastCheckedAt`、`healthStatus`、`lastErrorCode` 写回边界。
  - 文档规划稳定 reason code、安全前端文案、audit 边界、service / route / permission / parser / DTO / repository 后续拆分和 fake provider 优先顺序。
- 边界：
  - 不修改 `src/**`。
  - 不修改 `drizzle/**`。
  - 不新增 schema / migration。
  - 不新增 API route。
  - 不新增 service、repository runtime、credential provider runtime、test connection runtime 或 health check runtime。
  - 不实现 real provider。
  - 不处理真实凭证。
  - 不接真实 secret manager / KMS / Vault。
  - 不接真实 HIS adapter。
  - 不实现 Webhook / 同步任务。
  - 不实现 recovery runtime。
  - 不实现 runner / scheduler。
  - 不修改 package / lockfile、`.env` 或 `.codex/**`。
- 后续建议：
  - 测试连接权限与 route Plan Mode。
  - 健康状态 repository 写回 Plan Mode。
  - fake provider 测试连接 runtime 最小实现。
  - 测试连接 audit integration Plan Mode。
  - fake provider route runtime。
  - 真实 credential provider 读取边界 Plan Mode。
  - 真实 HIS adapter 测试连接 Plan Mode。
  - 周期健康检查 runner / scheduler Plan Mode。

## Phase 23 HIS 测试连接权限与 route 边界规划

- 分支：`docs/phase23-his-test-connection-permission-route-plan`
- 目标：只做 Phase 23 HIS 测试连接权限与 route docs-only 边界规划，承接测试连接 / 健康检查 Plan Mode，不进入任何 runtime。
- 当前 main commit：`534a45bbc52289e8ad61ef948affeb4eb8fd1f7e`
- 只读盘点结论：
  - 本地 `main` 已同步 `origin/main`，两者均位于 `534a45bbc52289e8ad61ef948affeb4eb8fd1f7e`。
  - 建分支前 working tree 为 clean。
  - README、roadmap、2026-06-07 devlog、上一轮测试连接 / 健康检查 Plan，以及现有权限、route、audit、HIS connection 相关文档均确认：本轮只需规划权限与 route 边界，不需要实现 runtime。
  - 既有状态权限和凭证权限文档均强调高风险动作不得复用 `open_connection:read_own_tenant`。
  - 既有 route denied audit 文档已形成 route 层负责权限拒绝和 parser 失败审计、service 层负责业务审计的分层口径。
- 完成：
  - 新增 `docs/superpowers/plans/2026-06-07-phase23-his-test-connection-permission-route.md`。
  - 文档明确测试连接应使用单独权限，候选为 `open_connection:test_connection`。
  - 文档明确不复用 `open_connection:read_own_tenant`、`open_connection:manage_credentials`、`open_connection:update`、`open_connection:manage_status` 或 `open_connection:delete`。
  - 文档明确 v1 默认仅 `tenant_admin` 可触发，普通机构角色、平台角色和审计角色默认拒绝，平台代管不进入 v1。
  - 文档规划 route 路径为 `POST /api/institution/his-connections/[connectionId]/test-connection`。
  - 文档明确 v1 推荐无 body；如后续需要 body，只允许极薄白名单。
  - 文档明确 route 不接受外部 `tenantId`，不从 body / query / header / localStorage 读取租户或凭证。
  - 文档明确 DTO 只返回 `ok`、`code`、`error`、`healthStatus`、`checkedAt` 等安全字段。
  - 文档明确 denied audit、parser failure audit、test success / failure audit 应规划，但 audit runtime 后置。
  - 文档明确权限实现、route 实现、parser、DTO、service、repository 写回、fake provider runtime 全部后置。
- 边界：
  - 不修改 `src/**`。
  - 不修改 `drizzle/**`。
  - 不新增 schema / migration。
  - 不实现 permission runtime。
  - 不实现 route runtime。
  - 不实现 parser / DTO。
  - 不实现 service。
  - 不实现 repository 写回。
  - 不实现 audit runtime。
  - 不实现 test connection runtime。
  - 不实现 fake provider runtime。
  - 不实现 real provider。
  - 不处理真实凭证。
  - 不接真实 secret manager / KMS / Vault。
  - 不接真实 HIS adapter。
  - 不实现 Webhook / 同步任务。
  - 不实现 recovery runtime。
  - 不实现 runner / scheduler。
  - 不修改 package / lockfile、`.env` 或 `.codex/**`。
- 后续建议：
  - 测试连接权限 action Plan Mode 或最小实现。
  - 测试连接 audit action / reason / query whitelist Plan Mode。
  - 测试连接 route parser / DTO Plan Mode。
  - 健康状态 repository 写回 Plan Mode。
  - fake provider service Plan Mode。
  - fake provider route runtime 最小实现。
  - 测试连接 audit runtime。
  - 真实 credential provider 读取边界 Plan Mode。
  - 真实 HIS adapter 测试连接 Plan Mode。
  - 周期健康检查 runner / scheduler Plan Mode。
