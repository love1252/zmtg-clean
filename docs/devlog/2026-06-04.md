# 2026-06-04 开发日志

## Phase 23 HIS 连接配置写入 payload parser

- 分支：`feat/phase23-his-connection-write-parser`
- 目标：为后续 HIS 连接配置 create / update API 提供可复用的 HTTP payload parser 和最小 DTO helper。
- 完成：
  - 新增 `parseCreateHisConnectionInput`，create payload 只接受 `connectionName`、`sourceSystem`、`vendorType`、`systemType` 四个安全元数据字段，全部必填。
  - 新增 `parseUpdateHisConnectionInput`，update payload 只接受四个安全元数据字段的非空子集。
  - parser 只接受普通 JSON object，拒绝 null、数组、Date、函数、class instance、空原型对象、未知字段、禁止字段、敏感字段别名、非字符串、空字符串、超长字符串和敏感内容。
  - 字符串 trim 后返回，长度限制对齐 repository：`connectionName` 不超过 160，`sourceSystem`、`vendorType`、`systemType` 不超过 64。
  - 新增最小 DTO helper，只返回 create / update v1 允许的四个安全元数据字段。
  - 新增 `HisConnectionWriteInput.test.ts`，覆盖 create / update 合法输入、字段白名单、禁止字段、敏感别名、DTO 边界、错误不回显敏感原值和无外部调用边界。
- 边界：
  - 不新增 API route。
  - 不改现有 route。
  - 不新增或修改 service。
  - 不新增或修改 repository。
  - 不改 schema / migration。
  - 不改权限、认证或租户隔离。
  - 不写审计实现。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存或返回真实凭证。
  - 不返回 `credentialRef` 给前端 DTO。
  - 不展示凭证明文。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteInput.test.ts`
  - `npm run typecheck`
  - 敏感字段 grep 检查

## Phase 23 HIS 连接配置写入权限 Plan Mode

- 分支：`docs/phase23-his-connection-write-permission-plan`
- 目标：规划后续 HIS 连接配置 create / update API 所需写入权限模型，明确不能复用只读权限放行写入。
- 完成：
  - 新增写入权限设计文档，记录当前 `open_connection` 权限现状。
  - 新增写入权限计划文档，记录只读检查、执行清单、验证命令和停止条件。
  - 明确当前 `tenant_admin` 只有 `open_connection:read_own_tenant`，没有 `open_connection:create` 或 `open_connection:update`。
  - 明确 create / update 必须使用独立写入权限，不得复用 `read_own_tenant`。
  - 规划未来权限动作：`open_connection:create` 和 `open_connection:update`。
  - 规划角色边界：v1 仅评估 `tenant_admin` 写入，普通机构人员、顾问、客服、平台代管写入默认不进入 v1。
  - 规划 API 权限判断顺序、稳定错误态、权限模型测试和 API 权限测试。
- 边界：
  - 不写代码。
  - 不改测试。
  - 不修改 `src/**`。
  - 不新增 API route。
  - 不新增或修改 service。
  - 不新增或修改 repository。
  - 不改 schema / migration。
  - 不真正修改权限、认证或租户隔离实现。
  - 不写审计实现。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存或返回真实凭证。
  - 不返回 `credentialRef` 给前端 DTO。
  - 不展示凭证明文。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - 中文化残留检查

## Phase 23 HIS 连接配置写入权限最小实现

- 分支：`feat/phase23-his-connection-write-permission`
- 目标：在现有权限模型中最小授予 `tenant_admin` 对 HIS 连接配置的 create / update 写入权限，为后续 create / update API 接入保留统一授权入口。
- 完成：
  - 在 `tenant_admin` 的 `open_connection` 权限策略中保留 `read_own_tenant`，并新增 `create` 和 `update`。
  - 继续通过统一 `canAccessResource` 判断权限，不新增 route 内硬编码角色判断。
  - 补充权限模型测试，覆盖 `tenant_admin` 对 `open_connection:create`、`open_connection:update` 和 `open_connection:read_own_tenant` 的允许结果。
  - 补充默认拒绝测试，覆盖 `tenant_operator`、`consultant`、`customer_service`、`platform_admin`、`platform_operator` 和 `security_auditor` 对 create / update 的 `role_denied`。
  - 补充租户边界测试，覆盖缺失 `tenantId` 时 `missing_tenant`，以及跨租户 targetTenantId 时 `cross_tenant_denied`。
  - 补充只读动作不能替代未授权写入动作的回归测试，确认未授权动作仍返回 `role_denied`。
- 边界：
  - 不新增 API route。
  - 不修改现有 API route。
  - 不新增或修改 service。
  - 不调用或修改 repository。
  - 不修改 parser。
  - 不改 schema / migration。
  - 不写审计实现。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存或返回真实凭证。
  - 不返回 `credentialRef` 给前端 DTO。
  - 不展示凭证明文。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - `node scripts/run-vitest.mjs run src/modules/security/tests/AccessControlDomain.test.ts`
  - `./node_modules/.bin/tsc --noEmit`

## Phase 23 HIS 连接配置写入 service Plan Mode

- 分支：`docs/phase23-his-connection-write-service-plan`
- 目标：规划后续 HIS 连接配置 create / update API 的 service 层事务边界、repository 结果映射、审计写入边界、DTO 边界和 API 错误映射。
- 完成：
  - 新增写入 service 设计文档，记录当前 parser、repository、权限、审计模型和邻近写入 API 风格。
  - 新增写入 service 计划文档，记录只读检查、文件职责、执行清单、验证命令和停止条件。
  - 明确后续 service 输入只能来自 `accessContext.tenantId`、`accessContext.userId`、route path `connectionId` 和 parser 输出的四个安全元数据字段。
  - 明确 service 不接受 body / query / header / localStorage `tenantId`，不接受 `credentialRef`、凭证、raw HIS payload、完整请求 / 响应体、SQL、stack 或 `DATABASE_URL`。
  - 规划 create / update 事务边界：repository 写入和 allowed audit 建议在同一事务内完成，审计失败时 v1 建议回滚。
  - 规划 repository result 映射：`ok` -> 201 / 200，`validation_failed` -> 400，`conflict` -> 409，`not_found` -> 404，thrown error -> 503。
  - 规划审计写入边界，明确成功 allowed audit、拒绝 denied audit、审计 reason 补强缺口和敏感内容禁止记录。
  - 规划 DTO 边界，create / update 成功响应只返回四个安全元数据字段或 `{ ok: true }`。
  - 规划后续 service tests 和 API route tests 拆分。
- 边界：
  - 只写文档。
  - 不写 service 代码。
  - 不新增 API route。
  - 不修改现有 API route。
  - 不修改 parser。
  - 不修改 repository。
  - 不修改权限实现或权限测试。
  - 不改 schema / migration。
  - 不写审计实现。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存或返回真实凭证。
  - 不返回 `credentialRef` 给前端 DTO。
  - 不展示凭证明文。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.codex`、Superpowers 缓存目录或技能文件。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - 中文化残留检查
  - 禁止范围检查

## Phase 23 HIS 连接配置写入 service 最小实现

- 分支：`feat/phase23-his-connection-write-service`
- 目标：实现 HIS 连接配置 create / update service 层最小编排，为后续 API route 接入提供稳定 service 入口。
- 完成：
  - 新增 `his-connection-write-service.ts`，导出 `createHisConnectionForTenantService` 和 `updateHisConnectionForTenantService`。
  - create service 只接收服务端 access context、parser 输出的四个安全元数据字段、数据库事务入口和 repository factory。
  - update service 只接收服务端 access context、path `connectionId`、parser 输出的四个安全元数据字段非空子集、数据库事务入口和 repository factory。
  - create 调用既有 `createHisConnectionForTenant`，只传入可信 `tenantId`、actorUserId 和四个安全元数据字段。
  - update 调用既有 `updateHisConnectionForTenant`，只传入可信 `tenantId`、path `connectionId`、actorUserId 和 parser 输出的非空子集。
  - 成功路径在同一事务内写 allowed audit：`open_connection:create` / `open_connection:update`，`result=allowed`，`reason=allowed_by_policy`。
  - repository `validation_failed`、`conflict`、`not_found` 和 thrown error 映射为稳定 service result。
  - 成功 DTO 采用 `{ ok: true }`，不返回 repository read model。
  - 新增 `HisConnectionWriteService.test.ts`，覆盖 create / update 成功、可信租户、命令白名单、结果映射、异常脱敏、audit 失败、DTO 边界和无外部调用边界。
- denied audit：
  - 本 PR 不实现 denied audit。
  - HIS 连接配置 payload 非法、连接名冲突、repository validation_failed 等 denied reason 仍留给后续审计 reason 补强 PR。
- 边界：
  - 不新增 API route。
  - 不修改现有 API route。
  - 不修改 parser。
  - 不修改 repository。
  - 不修改权限实现或权限测试。
  - 不改 schema / migration。
  - 不修改 audit domain 或 audit reason enum。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存或返回真实凭证。
  - 不返回 `credentialRef` 给前端 DTO。
  - 不展示凭证明文。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不做经营智能中心、图表或导出。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.codex`、Superpowers 缓存目录或技能文件。
  - 不引入新的 npm 依赖。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteService.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteInput.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionRepository.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/security/tests/AccessControlDomain.test.ts`
  - `./node_modules/.bin/tsc --noEmit`
  - 禁止范围检查

## Phase 23 HIS 连接配置审计 reason 补强 Plan Mode

- 分支：`docs/phase23-his-connection-audit-reason-plan`
- 目标：规划后续 HIS 连接配置 create / update API 失败路径所需的审计 reason，先明确命名、复用策略、敏感信息禁区和后续拆分。
- 完成：
  - 新增审计 reason 设计文档，记录当前 audit domain、audit query whitelist、audit repository、权限模型和 HIS 写入 service 审计现状。
  - 新增审计 reason 计划文档，记录只读检查、文件职责、执行清单、验证命令和停止条件。
  - 明确当前 audit domain 已支持 `open_connection` resource 和 `create` / `update` action。
  - 明确 service 已实现成功路径 allowed audit，denied audit 尚未实现。
  - 规划候选 reason：`invalid_his_connection_payload`、`his_connection_name_conflict`、`invalid_his_connection_repository_result`。
  - 明确权限拒绝复用 `role_denied`、`missing_tenant`、`cross_tenant_denied`。
  - 明确 update 不存在、跨租户或已删除优先复用 `not_found_or_not_owned`。
  - 规划 denied audit 禁止记录 payload、request / response body、body / query / header tenantId、凭证、raw HIS payload、SQL、stack、`DATABASE_URL`、完整病历、完整治疗正文、咨询全文和图片 / 文件原文。
  - 规划后续拆分：audit reason 类型补强、service denied audit 接入、API route denied audit 接入和 API route tests。
- 边界：
  - 只写文档。
  - 不修改 `src/**`。
  - 不修改 audit domain。
  - 不修改 audit reason union / type。
  - 不修改 audit repository。
  - 不修改 service。
  - 不新增或修改 API route。
  - 不修改 parser。
  - 不修改 repository。
  - 不修改权限实现或权限测试。
  - 不改 schema / migration。
  - 不实现 denied audit。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.codex`、Superpowers 缓存目录或技能文件。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - 中文化残留检查
  - 禁止范围检查

## Phase 23 HIS 连接配置审计 reason 最小实现

- 分支：`feat/phase23-his-connection-audit-reasons`
- 目标：补强 HIS 连接配置写入失败路径后续 denied audit 可复用的最小 audit reason 类型和查询白名单。
- 完成：
  - 在 audit reason 类型中新增 `invalid_his_connection_payload`。
  - 在 audit reason 类型中新增 `his_connection_name_conflict`。
  - 同步 `AUDIT_REASON_VALUES` 查询白名单，允许审计查询按上述两个 reason 过滤。
  - 补充 audit domain 测试，覆盖 `open_connection:create` / `open_connection:update` 可使用新增 reason。
  - 补充 audit domain 测试，确认 `not_found_or_not_owned` 继续用于 `open_connection:update`，`role_denied`、`missing_tenant`、`cross_tenant_denied` 不受影响。
  - 补充 audit domain 测试，确认事件不携带 request / response body、凭证、raw HIS payload、SQL、stack、`DATABASE_URL` 或数据库约束细节。
  - 补充 audit query parser 测试，覆盖两个新增 reason 可查询，未知 HIS 专用 reason 仍被拒绝。
- 未进入范围：
  - 未新增 `invalid_his_connection_repository_result`，保留到 service denied audit 接入前再评审。
  - 未新增 `his_connection_not_found_or_not_owned`，not found / not owned / deleted 场景继续复用 `not_found_or_not_owned`。
  - 不接入 service denied audit。
  - 不新增或修改 API route。
  - 不修改 HIS connection service、repository、parser 或权限。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.codex`、Superpowers 缓存目录或技能文件。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventsDomain.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventQueryParser.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteService.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/security/tests/AccessControlDomain.test.ts`
  - `./node_modules/.bin/tsc --noEmit`
  - 禁止范围检查

## Phase 23 HIS 连接配置写入 service denied audit 最小接入

- 分支：`feat/phase23-his-connection-service-denied-audit`
- 目标：在 HIS 连接配置写入 service 现有事务内接入 repository 失败路径的 denied audit，保持 API route、parser、repository、权限、schema、audit reason 和 audit repository 不变。
- 完成：
  - `createHisConnectionForTenantService` 在 repository `validation_failed` 时写 denied audit，reason 为 `invalid_his_connection_payload`，不写 resourceId。
  - `createHisConnectionForTenantService` 在 repository `conflict` 时写 denied audit，reason 为 `his_connection_name_conflict`，不写 resourceId。
  - `updateHisConnectionForTenantService` 在 repository `validation_failed` 时写 denied audit，reason 为 `invalid_his_connection_payload`，resourceId 使用 path `connectionId`。
  - `updateHisConnectionForTenantService` 在 repository `conflict` 时写 denied audit，reason 为 `his_connection_name_conflict`，resourceId 使用 path `connectionId`。
  - `updateHisConnectionForTenantService` 在 repository `not_found` 时写 denied audit，reason 为 `not_found_or_not_owned`，resourceId 使用 path `connectionId`。
  - denied audit 写入成功后，service 继续返回原有稳定结果：`validation_failed`、`conflict`、`not_found`。
  - denied audit 写入失败时，service 返回 `service_unavailable`，不返回业务成功或原失败结果。
  - repository thrown error 仍直接映射为 `service_unavailable`，不写 denied audit，不回显内部异常。
  - allowed audit 成功路径保持不变。
  - 补充 `HisConnectionWriteService.test.ts`，覆盖 reason 映射、resourceId 边界、audit 失败处理、敏感信息禁区和成功路径回归。
- 未进入范围：
  - 不新增 API route。
  - 不修改现有 API route。
  - 不修改 HIS connection parser。
  - 不修改 HIS connection repository。
  - 不修改权限实现或权限测试。
  - 不修改 audit domain / reason。
  - 不修改 `AUDIT_REASON_VALUES`。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不新增 `invalid_his_connection_repository_result`。
  - 不新增 `his_connection_not_found_or_not_owned`。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存或返回真实凭证。
  - 不返回 `credentialRef` 给前端 DTO。
  - 不展示凭证明文。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.codex`、Superpowers 缓存目录或技能文件。
  - 不引入新的 npm 依赖。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteService.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventsDomain.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventQueryParser.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteInput.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionRepository.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/security/tests/AccessControlDomain.test.ts`
  - `./node_modules/.bin/tsc --noEmit`
  - 禁止范围检查

## Phase 23 HIS 连接配置创建更新 API route Plan Mode

- 分支：`docs/phase23-his-connection-create-update-api-route-plan`
- 目标：规划后续 `POST /api/institution/his-connections` 与 `PATCH /api/institution/his-connections/[connectionId]` 的 route 接入顺序、权限判断、parser 边界、service result 映射、route denied audit、DTO 边界和测试拆分。
- 完成：
  - 新增 route 设计文档，记录当前 read route、写入 parser、写入 service、权限模型、审计 reason 和 service denied audit 现状。
  - 明确 POST 接入顺序：access context、401、`open_connection:create` 权限判断、403 denied audit、读取 JSON、`parseCreateHisConnectionInput`、400 parser denied audit、调用 create service、映射 HTTP。
  - 明确 PATCH 接入顺序：path `connectionId`、access context、401、`open_connection:update` 权限判断、403 denied audit、读取 JSON、`parseUpdateHisConnectionInput`、400 parser denied audit、调用 update service、映射 HTTP。
  - 明确 `tenantId` 只能来自 `accessContext.tenantId`，不得信任 body / query / header / localStorage tenantId。
  - 明确 route 不接受或传递凭证、raw HIS payload、完整请求体 / 响应体、SQL、stack 或 `DATABASE_URL`。
  - 明确 service result 到 HTTP 的稳定映射：created -> 201，updated -> 200，validation_failed -> 400，conflict -> 409，not_found -> 404，service_unavailable -> 503。
  - 明确 route 层只写权限拒绝和 parser 失败 denied audit，不重复 service repository failure audit。
  - 明确成功 DTO 优先 `{ ok: true }`，不返回 id、tenantId、status、credentialRef、credentialConfigured、healthStatus、时间字段、actor 字段、deletedAt、revokedAt、repository read model 或原始 request body。
  - 规划后续拆分：route 最小实现、route 测试、route permission / parser denied audit、smoke / 文档收尾。
- 边界：
  - docs-only。
  - 不修改 `src/**`。
  - 不新增 API route。
  - 不修改现有 API route。
  - 不修改 service、parser、repository 或权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.codex`、Superpowers 缓存目录或技能文件。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - 中文化残留检查
  - 禁止范围检查

## Phase 23 HIS 连接配置创建更新 API route 最小实现

- 分支：`feat/phase23-his-connection-create-update-api-route`
- 目标：最小接入机构端 HIS 连接配置创建与更新 route：`POST /api/institution/his-connections` 和 `PATCH /api/institution/his-connections/[connectionId]`。
- 完成：
  - `POST /api/institution/his-connections` 已接入服务端 access context、`open_connection:create` 权限判断、JSON 读取、`parseCreateHisConnectionInput`、`createHisConnectionForTenantService` 和稳定 HTTP 映射。
  - `PATCH /api/institution/his-connections/[connectionId]` 已接入 path `connectionId` trim、空 ID `not_found`、服务端 access context、`open_connection:update` 权限判断、JSON 读取、`parseUpdateHisConnectionInput`、`updateHisConnectionForTenantService` 和稳定 HTTP 映射。
  - 成功响应保持最小 DTO：`{ ok: true }`。
  - `validation_failed`、`conflict`、`not_found` 和 `service_unavailable` 统一映射为稳定错误响应，不回显原始 payload、parser 细节、异常信息、SQL、stack、`DATABASE_URL`、凭证或连接串。
  - route 只把 `accessContext`、parser 输出、PATCH path `connectionId` 和数据库依赖传给 service，不传 raw request body、body / query / header tenantId、凭证、raw HIS payload 或内部错误细节。
  - 轻量同步 `HisConnectionApiRoutes.test.ts`，覆盖写入成功、401 / 403 顺序、空 ID、malformed JSON、parser 失败和 service result 映射。
- 未进入范围：
  - 不实现 route 层 denied audit。
  - 不实现 parser 失败 audit。
  - 不新增 route denied audit helper。
  - 不修改 service、parser、repository、权限实现或权限测试。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不新增 pause / resume / revoke / delete API。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存或返回真实凭证。
  - 不返回 `credentialRef` 给前端 DTO。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.codex`、Superpowers 缓存目录或技能文件。
  - 不引入新的 npm 依赖。
- 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionApiRoutes.test.ts` 先因 `POST is not a function` / `PATCH is not a function` 失败。
  - GREEN：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionApiRoutes.test.ts` 通过，18 tests。
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteInput.test.ts` 通过，15 tests。
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteService.test.ts` 通过，7 tests。
  - `node scripts/run-vitest.mjs run src/modules/security/tests/AccessControlDomain.test.ts` 通过，20 tests。
  - `./node_modules/.bin/tsc --noEmit` 通过。
  - 提交前继续执行范围检查、空白检查和禁止范围检查。

## Phase 23 HIS 连接配置创建更新 API route 测试补强

- 分支：`test/phase23-his-connection-create-update-api-route`
- 目标：只补强 `POST /api/institution/his-connections` 与 `PATCH /api/institution/his-connections/[connectionId]` 的 route tests，保持 route、service、parser、repository、权限、audit 和 schema 不变。
- 完成：
  - 补强 POST 成功路径测试，确认返回 `201` 和 `{ ok: true }`，create service input 只包含 `accessContext`、`database`、`metadata`，metadata 只包含四个安全元数据字段。
  - 补强 POST tenantId 注入、凭证字段、token / secret / apiKey、raw payload、request / response body、SQL、stack、`DATABASE_URL`、malformed JSON 和 service result 映射测试。
  - 补强 PATCH 成功路径测试，确认返回 `200` 和 `{ ok: true }`，update service input 只包含 `accessContext`、`connectionId`、`database`、`metadata`，metadata 只包含 parser 输出的非空子集。
  - 补强 PATCH path `connectionId` trim、空 ID、tenantId 注入、凭证字段、token / secret / apiKey、raw payload、request / response body、SQL、stack、`DATABASE_URL`、空更新、malformed JSON 和 service result 映射测试。
  - 补强 create / update route 外部系统禁区测试，确认不调用 fetch / localStorage、不直接写数据库、不混入真实 HIS、测试连接、治疗摘要、随访任务或自动触达。
  - 保留既有 GET list / detail、只读 DTO、安全响应、demo seed 和无外部调用回归测试。
- 未进入范围：
  - 不修改 API route 代码。
  - 不新增 API。
  - 不新增 pause / resume / revoke / delete API。
  - 不修改 service、parser、repository、权限实现或权限测试。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不实现 route denied audit。
  - 不测试 route 权限拒绝 audit 或 parser 失败 audit。
  - 不修改 schema / migration。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存或返回真实凭证。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.codex`、Superpowers 缓存目录或技能文件。
  - 不引入新的 npm 依赖。
- 验证：
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionApiRoutes.test.ts` 通过，21 tests。
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteInput.test.ts` 通过，15 tests。
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteService.test.ts` 通过，7 tests。
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionRepository.test.ts` 通过，31 tests。
  - `node scripts/run-vitest.mjs run src/modules/security/tests/AccessControlDomain.test.ts` 通过，20 tests。
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventsDomain.test.ts` 通过，11 tests。
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventQueryParser.test.ts` 通过，8 tests。
  - `./node_modules/.bin/tsc --noEmit` 通过。
  - 提交后继续执行范围检查、空白检查和禁止范围检查。

## Phase 23 HIS 连接配置 route denied audit 最小接入

- 分支：`feat/phase23-his-connection-route-denied-audit`
- 目标：只在机构端 HIS 连接配置 create / update route 层接入权限拒绝、malformed JSON 和 parser 失败的 denied audit，保持 service、parser、repository、权限、audit domain、audit repository 和 schema 不变。
- 完成：
  - `POST /api/institution/his-connections` 在存在 access context 但 `open_connection:create` 权限拒绝时写 route denied audit，返回稳定 `403 forbidden`。
  - `PATCH /api/institution/his-connections/[connectionId]` 在 path `connectionId` trim 后、存在 access context 但 `open_connection:update` 权限拒绝时写 route denied audit，resourceId 使用 path `connectionId`。
  - POST / PATCH malformed JSON 和 parser 失败写 `invalid_his_connection_payload` denied audit，POST 不写 resourceId，PATCH 使用 path `connectionId`。
  - route denied audit 写入失败统一返回 `503 service_unavailable`，不回显 audit repository 异常、SQL、stack、`DATABASE_URL`、凭证或请求内容。
  - route denied audit 只写结构化审计字段，不记录 payload、request body、response body、body / query / header tenantId、凭证、raw HIS payload、连接串、SQL、stack 或医疗正文。
  - 保留 POST / PATCH 成功 DTO、service result HTTP 映射、未登录 401、PATCH 空 ID 404、GET list / detail 回归和无外部调用边界。
  - route 不为 service `validation_failed`、`conflict`、`not_found` 或 `service_unavailable` 结果重复写 audit。
- 未进入范围：
  - 不新增 API。
  - 不新增 pause / resume / revoke / delete API。
  - 不修改 service、parser、repository、权限实现或权限测试。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI / RAG / Agent 或自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存或返回真实凭证。
  - 不返回 `credentialRef` 给前端 DTO。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.codex`、Superpowers 缓存目录或技能文件。
  - 不引入新的 npm 依赖。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionApiRoutes.test.ts` 先因 route 未写 denied audit、audit 写失败未返回 503 失败，28 tests 中 7 failed。
  - GREEN：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionApiRoutes.test.ts` 通过，28 tests。

## Phase 23 HIS 连接配置状态 API Plan Mode

- 分支：`docs/phase23-his-connection-status-api-plan`
- 目标：规划后续 HIS 连接配置 pause / resume / revoke / delete 状态 API 的 API 边界、权限边界、service 边界、审计边界、DTO 边界和测试拆分。
- 完成：
  - 新增状态 API 设计文档，记录当前 route、repository、parser、service、权限和 audit 现状。
  - 明确状态 API 范围：pause、resume、revoke、delete / softDelete。
  - 规划 API 路径：`POST /api/institution/his-connections/[connectionId]/pause`、`POST /api/institution/his-connections/[connectionId]/resume`、`POST /api/institution/his-connections/[connectionId]/revoke`、`DELETE /api/institution/his-connections/[connectionId]`。
  - 明确 `DELETE` 与现有 detail route 组织适配；如后续因可选 `reasonCode` body 选择 `POST /delete`，必须在 route Plan Mode 或实现 PR 中说明原因。
  - 明确可信输入只能来自 `accessContext.tenantId`、`accessContext.userId`、path `connectionId` 和安全可选 `reasonCode`。
  - 明确不接受 body / query / header / localStorage `tenantId`。
  - 明确不接受或透传凭证、raw HIS payload、完整请求 / 响应体、SQL、stack、`DATABASE_URL`、完整治疗正文、完整病历正文、咨询全文或图片 / 文件原文。
  - 规划权限边界：不得复用 `read_own_tenant`；评估 `open_connection:update`、`manage_status + delete` 和细分 `pause / resume / revoke / delete` action；v1 默认仅 `tenant_admin` 可写，平台代管写入不进入 v1。
  - 记录当前 repository 状态流转：pause 允许 `active / error -> paused`，resume 允许 `paused -> active`，revoke 允许 `draft / active / paused / error -> revoked`，softDelete 允许未删除状态进入 `deleted`。
  - 规划 HTTP 映射：成功优先 `{ ok: true }`，401 / 403 / 400 / 404 / 409 / 503 使用稳定 code。
  - 规划 service 边界：建议后续先做状态 service Plan Mode 或最小实现，route 不直接调用 repository 状态方法。
  - 规划审计边界：成功语义为 open_connection pause / resume / revoke / delete allowed；当前 audit action 类型尚不能直接表达这些 action，需后续权限 / audit Plan Mode 评审。
  - 规划测试覆盖：四个成功路径、401、403、tenantId 注入无效、空 `connectionId`、not_found 不暴露存在性、invalid transition、service unavailable、route denied audit、allowed audit、DTO 最小化和无外部调用边界。
- 边界：
  - docs-only。
  - 不修改 `src/**`。
  - 不新增 API route。
  - 不修改现有 GET / POST / PATCH。
  - 不修改 service、parser、repository 或权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI、RAG、Agent 或自动触达。
  - 不导入真实客户数据。
  - 不保存 raw HIS payload。
  - 不保存或返回真实凭证。
  - 不返回 `credentialRef` 给前端 DTO。
  - 不展示凭证明文。
  - 不保存完整治疗正文、完整病历正文、咨询全文、图片 / 文件原文。
  - 不做患者身份匹配。
  - 不自动创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.codex`、Superpowers 缓存目录或技能文件。
  - 不引入新的 npm 依赖。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - 中文化残留检查
  - 禁止范围检查

## Phase 23 HIS 连接配置状态权限 Plan Mode

- 分支：`docs/phase23-his-connection-status-permission-plan`
- 目标：规划后续 HIS 连接配置 pause / resume / revoke / delete / softDelete 状态 API 所需权限边界，明确候选方案、v1 推荐策略、角色授权、tenant scope、audit action、DTO 和权限测试拆分。
- 完成：
  - 新增状态权限设计文档，记录当前权限模型、audit action、状态 repository 和状态 API Plan Mode 现状。
  - 明确状态 API 权限范围仅包含 pause、resume、revoke、delete / softDelete。
  - 确认推荐路径仍为 `POST /api/institution/his-connections/[connectionId]/pause`、`POST /api/institution/his-connections/[connectionId]/resume`、`POST /api/institution/his-connections/[connectionId]/revoke` 和 `DELETE /api/institution/his-connections/[connectionId]`。
  - 明确 `POST /delete` 仅作为后续需要说明原因的备选路径。
  - 明确不得复用 `open_connection:read_own_tenant`，不得用只读权限放行状态写入，不得在 route PR 中顺手扩大权限模型。
  - 评估三种权限方案：复用 `open_connection:update`、复用 `open_connection:manage_status` 与 `open_connection:delete`、新增细分 pause / resume / revoke / delete action。
  - 推荐 v1 使用 `open_connection:manage_status` 承载 pause / resume / revoke，使用 `open_connection:delete` 承载 delete / softDelete。
  - 明确 v1 默认仅 `tenant_admin` 可写；`tenant_operator`、`consultant`、`customer_service`、`platform_admin`、`platform_operator`、`security_auditor` 和其他只读或非管理员角色默认拒绝。
  - 明确平台代管写入不进入 v1。
  - 明确 `targetTenantId` 只能来自 `accessContext.tenantId`，不接受 body / query / header / localStorage `tenantId`，跨租户目标由 service / repository 统一映射为 `not_found`。
  - 明确 pause / resume / revoke 的 audit action 可使用 `manage_status`，delete / softDelete 使用 `delete`；当前 PR 不新增 audit action、reason、query whitelist 或 audit repository。
  - 明确 route 权限拒绝时不读取 body、不调用 service、不调用 repository、不调用真实 HIS、不调用 fetch、不做测试连接、不处理凭证。
  - 明确成功响应优先 `{ ok: true }`，不返回 `connectionId`、`tenantId`、`status`、`credentialRef`、`credentialConfigured`、`healthStatus`、时间字段、actor 字段或 read model。
  - 规划权限模型测试和 route 权限测试，覆盖 `tenant_admin` allowed、其他角色 denied、missing tenant、cross tenant、`read_own_tenant` 不可替代、`update` 不可替代状态管理或删除。
- 边界：
  - docs-only。
  - 不修改 `src/**`。
  - 不新增 API route。
  - 不修改现有 GET / POST / PATCH。
  - 不修改 service、parser、repository。
  - 不修改权限实现或权限测试。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI、RAG、Agent 或自动触达。
  - 不创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.env*`、`.codex`、Superpowers 缓存目录或技能文件。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - 中文化残留检查
  - 禁止范围检查

## Phase 23 HIS 连接配置状态权限最小实现

- 分支：`feat/phase23-his-connection-status-permission`
- 目标：为后续 HIS 连接配置 pause / resume / revoke / delete / softDelete 状态 API 提供最小权限模型，只给 `tenant_admin` 增加既有 `open_connection:manage_status` 与 `open_connection:delete`。
- 完成：
  - 在 `tenant_admin` 的 `open_connection` policy 中保留 `read_own_tenant`、`create`、`update`，并新增 `manage_status` 与 `delete`。
  - 不新增 `pause`、`resume`、`revoke` 或 `soft_delete` action，不修改 `ACCESS_ACTIONS`。
  - 不修改 `ACCESS_RESOURCES`，继续复用既有 `open_connection`。
  - 补充权限模型测试，覆盖 `tenant_admin` 对 `manage_status` / `delete` allowed，既有 read / create / update 不回退。
  - 补充默认拒绝测试，覆盖 `tenant_operator`、`consultant`、`customer_service`、`platform_admin`、`platform_operator`、`security_auditor` 对状态权限 denied。
  - 补充 tenant scope 测试，覆盖缺失 `tenantId` 返回 `missing_tenant`，跨租户 `targetTenantId` 返回 `cross_tenant_denied`。
  - 补充只读和 update 动作不替代状态管理或删除的隔离测试。
- 未进入范围：
  - 不新增 API route。
  - 不修改现有 GET / POST / PATCH route。
  - 不修改 service、parser、repository。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI、RAG、Agent 或自动触达。
  - 不创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.codex`、Superpowers 缓存目录或技能文件。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/security/tests/AccessControlDomain.test.ts` 先因 `open_connection:manage_status` / `open_connection:delete` 未授权失败，24 tests 中 1 failed。
  - GREEN：`node scripts/run-vitest.mjs run src/modules/security/tests/AccessControlDomain.test.ts` 通过，24 tests。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - `node scripts/run-vitest.mjs run src/modules/security/tests/AccessControlDomain.test.ts` 通过，24 tests。
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionApiRoutes.test.ts` 通过，28 tests。
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteService.test.ts` 通过，7 tests。
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventsDomain.test.ts` 通过，11 tests。
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventQueryParser.test.ts` 通过，8 tests。
  - `./node_modules/.bin/tsc --noEmit` 通过，无输出。

## Phase 23 HIS 连接配置 pause / resume API route 最小实现

- 分支：`feat/phase23-his-connection-pause-resume-api-route`
- 目标：实现 `POST /api/institution/his-connections/[connectionId]/pause` 与 `POST /api/institution/his-connections/[connectionId]/resume` 两个状态 API route，保持 status service、parser、repository、权限、audit domain、schema 和 migration 不变。
- 完成：
  - 新增 pause route，只调用 `pauseHisConnectionForTenantService`。
  - 新增 resume route，只调用 `resumeHisConnectionForTenantService`。
  - 接入顺序为 path `connectionId` trim、空 ID 返回 `404 not_found`、读取服务端 access context、未登录返回 `401 unauthorized`、`open_connection:manage_status` 权限判断、无权限返回 `403 forbidden`、读取安全 JSON body、调用 status service、映射稳定 HTTP 响应。
  - body 可为空或 `{}`，仅允许可选 `reasonCode`；`reasonCode` 必须是 string，trim 后非空才传给 service；未知字段、body tenantId 注入、非 string `reasonCode` 和 malformed JSON 统一返回 `400 validation_failed`。
  - service input 只包含 `accessContext`、path `connectionId`、`database` 和可选安全 `reasonCode`，不传 raw body、body / query / header tenantId、凭证字段、raw HIS payload、SQL、stack 或 `DATABASE_URL`。
  - 成功响应固定 `{ ok: true }`；错误响应使用稳定 code / error，不回显非法字段值、JSON parse error、异常信息、凭证、连接串、SQL、stack 或数据库细节。
  - route 层不实现 denied audit；权限拒绝、parser 失败、service repository failure 的 allowed / denied audit 继续留给 status service 或后续状态 API 审计补强 PR。
  - 补强 `HisConnectionApiRoutes.test.ts`，覆盖 pause / resume 成功、service 调用边界、reasonCode trim、空 body、tenantId 注入、query/header tenantId 不可信、malformed JSON、401 / 403 / 404 短路、service result HTTP 映射、DTO 最小化、敏感信息禁区和无外部副作用。
- 未进入范围：
  - 不实现 revoke route。
  - 不实现 delete / softDelete route。
  - 不修改 status service。
  - 不修改通用 parser。
  - 不修改 repository。
  - 不修改权限实现或权限测试。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不新增 audit reason。
  - 不新增 audit action。
  - 不新增 pause / resume / revoke / soft_delete action。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI、RAG、Agent 或自动触达。
  - 不创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.env*`、`.codex`、Superpowers 缓存目录或技能文件。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionApiRoutes.test.ts` 先因缺少 pause / resume route module 失败。
  - GREEN：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionApiRoutes.test.ts` 通过，36 tests。

## Phase 23 HIS 连接配置状态 service Plan Mode

- 分支：`docs/phase23-his-connection-status-service-plan`
- 目标：规划后续 HIS 连接配置 pause / resume / revoke / delete / softDelete 状态 service，明确 service 导出函数、可信输入、repository 调用、事务边界、allowed audit、denied audit、稳定 result、DTO 和测试拆分。
- 完成：
  - 新增状态 service 设计文档，记录当前 repository 状态方法、create / update service 模式、权限模型和 audit action 现状。
  - 明确推荐导出 `pauseHisConnectionForTenantService`、`resumeHisConnectionForTenantService`、`revokeHisConnectionForTenantService` 和 `softDeleteHisConnectionForTenantService`。
  - 明确 service 只接收服务端 access context、path `connectionId`、database / transaction、repository / audit repository 和安全解析后的可选 `reasonCode`。
  - 明确不接受 body / query / header / localStorage `tenantId`，不接收 raw request / response body，不接收凭证、raw HIS payload、SQL、stack、`DATABASE_URL` 或完整医疗正文。
  - 明确 pause / resume / revoke / softDelete service 分别调用 `pauseHisConnectionForTenant`、`resumeHisConnectionForTenant`、`revokeHisConnectionForTenant` 和 `softDeleteHisConnectionForTenant`。
  - 明确 repository command 只包含 `tenantId`、`connectionId`、`actorUserId` 和可选 `reasonCode`。
  - 明确 route 不应直接调用 repository 状态方法，优先通过状态 service 统一处理事务、审计和稳定 result。
  - 明确 repository 写入与 allowed / denied audit 在同一事务内；audit 失败或 repository thrown error 均返回 `service_unavailable`，不记录异常细节。
  - 明确 allowed audit：pause / resume / revoke 使用 `open_connection` + `manage_status` + `allowed_by_policy`，delete / softDelete 使用 `open_connection` + `delete` + `allowed_by_policy`。
  - 明确 denied audit：`not_found` 复用 `not_found_or_not_owned`，`invalid_state_transition` 映射为 service `invalid_transition` 并可复用 `invalid_transition` reason，`conflict` 与 `validation_failed` reason 后续评估。
  - 明确成功响应优先 `{ ok: true }`，不返回 connectionId、tenantId、status、凭证字段、健康字段、时间字段、actor 字段、read model、repository command 或 raw request body。
  - 明确 revoke 不处理凭证撤销，delete 只 softDelete，不物理删除，不删除外部系统数据。
  - 规划状态 service tests，覆盖成功、not_found、conflict、validation_failed、invalid_transition、audit failure、repository thrown error、missing tenant、command 最小化和无外部副作用。
- 边界：
  - docs-only。
  - 不修改 `src/**`。
  - 不新增 service。
  - 不新增 API route。
  - 不修改现有 GET / POST / PATCH。
  - 不修改 parser、repository。
  - 不修改权限实现或权限测试。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI、RAG、Agent 或自动触达。
  - 不创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.env*`、`.codex`、Superpowers 缓存目录或技能文件。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - 中文化残留检查
  - 禁止范围检查

## Phase 23 HIS 连接配置状态 service audit reason 映射 Plan Mode

- 分支：`docs/phase23-his-connection-status-service-audit-reason-plan`
- 目标：规划后续 HIS 连接配置状态 service 的 repository 非 ok 结果到 denied audit reason 的映射，收敛 `conflict`、`validation_failed`、`invalid_state_transition`、`not_found` 和 thrown error 的审计边界。
- 完成：
  - 新增状态 service audit reason 设计文档，记录当前 audit reason / query whitelist、状态 repository result、create / update service denied audit 模式和前序状态 service Plan Mode 现状。
  - 新增当前计划文档，记录只读检查、文件职责、执行清单、验证命令和停止条件。
  - 确认当前 main commit 为 `5bd226807f304950e6c7b04ef83644f80101486c`，建分支前工作区干净。
  - 明确状态 repository 稳定结果为 `ok`、`not_found`、`conflict`、`invalid_state_transition`、`validation_failed`，内部异常以 thrown error 进入 service `service_unavailable`。
  - 明确状态 service 稳定结果为 `paused`、`resumed`、`revoked`、`deleted`、`not_found`、`conflict`、`invalid_transition`、`validation_failed` 和 `service_unavailable`。
  - 明确 denied audit reason 映射：`not_found -> not_found_or_not_owned`，`invalid_state_transition -> invalid_transition`，`validation_failed -> invalid_his_connection_payload`，`conflict -> invalid_transition`，thrown error 不写 denied audit。
  - 明确不新增 `his_connection_status_conflict`、`invalid_his_connection_status_transition`、`invalid_his_connection_status_payload`、`his_connection_status_validation_failed` 或任何 audit reason / action。
  - 明确后续状态 service 实现只能使用既有 `allowed_by_policy`、`not_found_or_not_owned`、`invalid_transition` 和 `invalid_his_connection_payload`。
  - 明确 allowed audit 不受影响：pause / resume / revoke 成功使用 `open_connection + manage_status + allowed_by_policy`，delete / softDelete 成功使用 `open_connection + delete + allowed_by_policy`。
  - 明确 denied audit 不记录 payload、request / response body、body / query / header tenantId、凭证、raw HIS payload、SQL、stack、`DATABASE_URL`、完整医疗正文、图片 / 文件原文、数据库 constraint、索引名或冲突行详情。
  - 明确后续 service tests 必须覆盖 reason 映射、thrown error 不写 denied audit、audit 失败返回 `service_unavailable` 和 audit event 敏感信息禁区。
  - 轻量同步前序状态 service spec、README 和 roadmap，标注状态 service 实现前置 reason 已收敛。
- 边界：
  - docs-only。
  - 不写代码。
  - 不修改 `src/**`。
  - 不新增 service。
  - 不新增 API route。
  - 不修改现有 API route。
  - 不修改 parser。
  - 不修改 repository。
  - 不修改权限实现或权限测试。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不新增 audit reason。
  - 不新增 audit action。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI、RAG、Agent 或自动触达。
  - 不创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.env*`、`.codex`、Superpowers 缓存目录或技能文件。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - 禁止范围检查

## Phase 23 HIS 连接配置状态 service 最小实现

- 分支：`feat/phase23-his-connection-status-service`
- 目标：实现 HIS 连接配置 pause / resume / revoke / delete / softDelete 状态 service，接入事务内 repository 状态写入、allowed / denied audit、稳定 service result 和最小 DTO。
- 完成：
  - 新增 `src/modules/institution/server/his-connection-status-service.ts`。
  - 导出 `pauseHisConnectionForTenantService`、`resumeHisConnectionForTenantService`、`revokeHisConnectionForTenantService` 和 `softDeleteHisConnectionForTenantService`。
  - service 只接收 `accessContext`、path `connectionId`、database、repository / audit repository factory 或 instance、可选安全 `reasonCode`。
  - 防御性校验 `accessContext.tenantId`、`accessContext.userId` 和 trim 后非空 `connectionId`；缺失时返回 `validation_failed`，不调用 repository，不写 audit。
  - repository command 只包含可信 `tenantId`、path `connectionId`、`actorUserId` 和可选 `reasonCode`。
  - pause / resume / revoke 分别调用 `pauseHisConnectionForTenant`、`resumeHisConnectionForTenant`、`revokeHisConnectionForTenant`，成功返回 `paused` / `resumed` / `revoked` 和 `{ ok: true }`，并写 `open_connection + manage_status + allowed_by_policy`。
  - softDelete 调用 `softDeleteHisConnectionForTenant`，成功返回 `deleted` 和 `{ ok: true }`，并写 `open_connection + delete + allowed_by_policy`。
  - repository `not_found` 返回 `not_found` 并写 `not_found_or_not_owned` denied audit。
  - repository `invalid_state_transition` 返回 `invalid_transition` 并写 `invalid_transition` denied audit。
  - repository `validation_failed` 返回 `validation_failed` 并写 `invalid_his_connection_payload` denied audit。
  - repository `conflict` 返回 `conflict` 并写 `invalid_transition` denied audit，v1 不新增 status conflict reason。
  - repository thrown error 返回 `service_unavailable`，不写 denied audit，不回显异常信息。
  - audit 写入失败返回 `service_unavailable`，不返回业务成功或原失败结果。
  - 新增 `src/modules/institution/tests/HisConnectionStatusService.test.ts`，覆盖成功路径、repository 非 ok 映射、audit failure、repository thrown error、缺失可信输入、command 最小化、DTO 最小化和无外部副作用。
- 未进入范围：
  - 不新增 API route。
  - 不修改现有 API route。
  - 不修改 parser。
  - 不修改 repository。
  - 不修改权限实现或权限测试。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不新增 audit reason。
  - 不新增 audit action。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI、RAG、Agent 或自动触达。
  - 不创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.env*`、`.codex`、Superpowers 缓存目录或技能文件。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionStatusService.test.ts` 先因缺少 `his-connection-status-service` module 失败。
  - GREEN：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionStatusService.test.ts` 通过，7 tests。
- 回归验证：
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionRepository.test.ts` 通过，31 tests。
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionWriteService.test.ts` 通过，7 tests。
  - `node scripts/run-vitest.mjs run src/modules/security/tests/AccessControlDomain.test.ts` 通过，24 tests。
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventsDomain.test.ts` 通过，11 tests。
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventQueryParser.test.ts` 通过，8 tests。
  - `./node_modules/.bin/tsc --noEmit` 通过，无输出。

## Phase 23 HIS 连接配置 revoke / DELETE API route 最小实现

- 分支：`feat/phase23-his-connection-revoke-delete-api-route`
- 目标：实现 `POST /api/institution/his-connections/[connectionId]/revoke` 与 `DELETE /api/institution/his-connections/[connectionId]` 两个状态 API route，保持 status service、parser、repository、权限、audit domain、schema 和 migration 不变。
- 完成：
  - 新增 revoke route，只调用 `revokeHisConnectionForTenantService`。
  - 在既有 `[connectionId]/route.ts` 新增 `DELETE`，只调用 `softDeleteHisConnectionForTenantService`。
  - revoke 接入顺序为 path `connectionId` trim、空 ID 返回 `404 not_found`、读取服务端 access context、未登录返回 `401 unauthorized`、`open_connection:manage_status` 权限判断、无权限返回 `403 forbidden`、读取安全 JSON body、调用 status service、映射稳定 HTTP 响应。
  - DELETE 接入顺序为 path `connectionId` trim、空 ID 返回 `404 not_found`、读取服务端 access context、未登录返回 `401 unauthorized`、`open_connection:delete` 权限判断、无权限返回 `403 forbidden`、读取安全 JSON body、调用 status service、映射稳定 HTTP 响应。
  - body 可为空或 `{}`，仅允许可选 `reasonCode`；`reasonCode` 必须是 string，trim 后非空才传给 service；未知字段、body tenantId 注入、非 string `reasonCode` 和 malformed JSON 统一返回 `400 validation_failed`。
  - service input 只包含 `accessContext`、path `connectionId`、`database` 和可选安全 `reasonCode`，不传 raw body、body / query / header tenantId、凭证字段、raw HIS payload、SQL、stack 或 `DATABASE_URL`。
  - 成功响应固定 `{ ok: true }`；错误响应使用稳定 code / error，不回显非法字段值、JSON parse error、异常信息、凭证、连接串、SQL、stack 或数据库细节。
  - route 层不实现权限拒绝 audit 或 parser 失败 audit；权限拒绝、parser 失败、service repository failure 的 allowed / denied audit 继续留给 status service 或后续状态 API 审计补强 PR。
  - 补强 `HisConnectionApiRoutes.test.ts`，覆盖 revoke / DELETE 成功、service 调用边界、权限动作隔离、reasonCode trim、空 body、tenantId 注入、query/header tenantId 不可信、malformed JSON、401 / 403 / 404 短路、service result HTTP 映射、DTO 最小化、敏感信息禁区和无外部副作用。
- 未进入范围：
  - 不修改 pause route。
  - 不修改 resume route。
  - 不修改 status service。
  - 不修改通用 parser。
  - 不修改 repository。
  - 不修改权限实现或权限测试。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不新增 audit reason。
  - 不新增 audit action。
  - 不新增 pause / resume / revoke / soft_delete action。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI、RAG、Agent 或自动触达。
  - 不创建治疗摘要或随访任务。
  - 不修改 demo seed 数据。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.env*`、`.codex`、Superpowers 缓存目录或技能文件。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionApiRoutes.test.ts` 先因缺少 revoke route module 失败。
  - GREEN：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionApiRoutes.test.ts` 通过，45 tests。
- 验证命令：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionApiRoutes.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionStatusService.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionRepository.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/security/tests/AccessControlDomain.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventsDomain.test.ts`
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventQueryParser.test.ts`
  - `./node_modules/.bin/tsc --noEmit`

## Phase 23 HIS 连接配置状态 API route denied audit Plan Mode

- 分支：`docs/phase23-his-connection-status-route-denied-audit-plan`
- 目标：规划后续 pause / resume / revoke / DELETE 状态 API route 层 denied audit 补强，明确权限拒绝、parser 失败、audit 失败处理、禁止重复审计和敏感信息禁区。
- 当前 main commit：`358392bc48066f53a96cecbda8d040c6d469a1c5`
- 完成：
  - 新增状态 API route denied audit 设计文档，记录当前 status route、create / update route denied audit、status service、权限和 audit reason 的只读检查结论。
  - 新增状态 API route denied audit 实施计划，拆分后续权限拒绝 audit、parser 失败 audit、不重复审计和收口验证任务。
  - 明确适用 route：`POST /api/institution/his-connections/[connectionId]/pause`、`POST /api/institution/his-connections/[connectionId]/resume`、`POST /api/institution/his-connections/[connectionId]/revoke` 和 `DELETE /api/institution/his-connections/[connectionId]`。
  - 明确 route denied audit 只覆盖有 access context 的权限拒绝和权限允许后的 parser 失败。
  - 明确 `401 unauthorized`、空 path `connectionId` 的 `404 not_found`、status service repository 失败、repository thrown error 和 service unavailable 不由 route 重复审计。
  - 明确 pause / resume / revoke route audit action 使用 `manage_status`，DELETE 使用 `delete`，不新增 `pause`、`resume`、`revoke` 或 `soft_delete` action。
  - 明确权限拒绝 reason 复用 `canAccessResource` 既有 reason，parser 失败使用 `invalid_his_connection_payload`，不新增 audit reason。
  - 明确 resource 固定为 `open_connection`，resourceId 固定为 trim 后 path `connectionId`，不从 body / query / header 读取 tenant 或 resourceId。
  - 明确 route audit 写入失败时返回 `503 service_unavailable`，不得继续调用 status service。
  - 明确 route denied audit、错误响应和测试 fixture 不记录 raw body、body / query / header tenantId、凭证、raw HIS payload、SQL、stack、`DATABASE_URL`、完整医疗正文或图片 / 文件原文。
- 边界：
  - docs-only。
  - 不写代码。
  - 不修改 `src/**`。
  - 不新增 API。
  - 不修改 route、status service、parser、repository。
  - 不修改权限实现或权限测试。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不新增 audit reason。
  - 不新增 audit action。
  - 不处理凭证管理。
  - 不做测试连接。
  - 不接真实 HIS、机构系统、企微、AI、RAG、Agent 或自动触达。
  - 不修改 demo seed 数据。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - 本次新增 / 修改文档中文化残留检查
  - 禁止范围检查
