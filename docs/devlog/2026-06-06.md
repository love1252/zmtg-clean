# 2026-06-06 开发日志

## Phase 23 HIS 连接配置凭证 parser / service / DTO 最小实现

- 分支：`feat/phase23-his-connection-credential-parser-service-dto-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 parser / service / DTO 最小边界，承接凭证 parser / service / DTO Plan Mode 和 repository / storage 最小边界。
- 当前 main commit：`d8d78700d5ee1df17e29ef989889b53bdf85af27`
- devlog 日期规则执行结果：本次任务按执行当天日期新建 `docs/devlog/2026-06-06.md`，不再追加到旧日期 `docs/devlog/2026-06-04.md`。
- 只读盘点结论：
  - 当前已有 HIS connection write parser 模式可复用。
  - 当前已有 HIS connection service result / transaction 模式可复用。
  - 当前已有 `{ ok: true }` 最小 DTO 模式可复用。
  - 当前可以不新增 API route。
  - 当前可以不修改权限。
  - 当前可以不新增 audit action / reason / domain / query whitelist。
  - 当前可以不新增 schema / migration。
  - 当前可以只使用现有 fake in-memory storage。
  - 当前可以只使用现有 repository set / rotate / clear / revoke credential reference 方法。
  - 当前可以继续不处理真实凭证、不做测试连接、不接真实 HIS。
- 修改文件：
  - `src/modules/institution/server/his-connection-credential-input.ts`
  - `src/modules/institution/server/his-connection-credential-service.ts`
  - `src/modules/institution/server/his-connection-credential-dto.ts`
  - `src/modules/institution/tests/HisConnectionCredentialInput.test.ts`
  - `src/modules/institution/tests/HisConnectionCredentialService.test.ts`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增凭证 parser，覆盖 create / update / rotate / clear / revoke payload。
  - create / update / rotate 只接受 `credentialType`、`syntheticPlaceholder`、`idempotencyKey` 和可选 `reasonCode`。
  - clear / revoke 只接受可选 `reasonCode`。
  - parser 只接受普通 JSON object，拒绝数组、null、空字符串、函数、Date、class instance、空原型对象、未知字段和禁止字段。
  - parser 拒绝 body `tenantId`、`credentialRef`、`credentialConfigured`、status、healthStatus、raw HIS payload、external secret path、token、secret、API key、connection string 和真实凭证材料。
  - 新增凭证 service，覆盖 create / update / rotate / clear / revoke 最小 flow。
  - create / update 调用 fake storage 生成安全 `credentialRef` 后调用 repository set。
  - rotate 调用 fake storage 生成安全 `credentialRef` 后调用 repository rotate。
  - clear 调用 repository clear。
  - revoke 调用 repository revoke；当前不强制 storage revoke，因为现有 repository 不暴露内部 `credentialRef` 读取能力，storage revoke 留给后续单独规划。
  - 新增 DTO helper，成功 DTO 只返回 `ok` 和 `credentialConfigured`，错误 DTO 只返回稳定 code / error。
  - README 和 roadmap 同步：凭证 parser / service / DTO 最小实现已完成，下一步进入凭证 API route / permission / audit Plan Mode。
- 边界：
  - 不新增 API route。
  - 不修改现有 API 行为。
  - 不新增权限。
  - 不新增 audit action / reason / domain / query whitelist。
  - 不修改 audit repository。
  - 不新增 schema / migration。
  - 不修改 demo seed。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.env*`、`.codex`、Superpowers 缓存目录或技能文件。
  - 不接真实 KMS / Vault / secret manager。
  - 不保存真实凭证。
  - 不保存真实 token / secret / API key / connection string。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不保存 raw HIS payload。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialInput.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts` 先失败，失败点为缺少 `his-connection-credential-dto` 和 `his-connection-credential-service` 目标模块。
  - GREEN：同一命令通过，2 个测试文件，19 个测试。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialInput.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts src/modules/institution/tests/HisConnectionCredentialStorage.test.ts src/modules/institution/tests/HisConnectionRepository.test.ts`
  - `./node_modules/.bin/tsc --noEmit`
  - `./node_modules/.bin/eslint src/modules/institution/server/his-connection-credential-input.ts src/modules/institution/server/his-connection-credential-service.ts src/modules/institution/server/his-connection-credential-dto.ts src/modules/institution/tests/HisConnectionCredentialInput.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts`
  - 中文化残留检查
  - 禁止范围检查
  - route / api / migration / schema / seed 额外禁止检查

## Phase 23 HIS 连接配置凭证 API route / permission / audit Plan Mode

- 分支：`docs/phase23-his-connection-credential-api-permission-audit-plan`
- 目标：创建 docs-only Plan 文档，规划 Phase 23 HIS 连接配置凭证 API route、permission、route denied audit、route success audit、DTO / error mapping、调用顺序和测试拆分边界。
- 当前 main commit：`8cbac99e09d7f9b01fbc4c028ce8e8b63d2488bd`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 修改文件：
  - `docs/superpowers/specs/2026-06-06-phase23-his-connection-credential-api-permission-audit-plan.md`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 规划凭证 create / update / rotate / clear / revoke API route，但不新增 route。
  - 规划 `open_connection:manage_credentials` 权限候选，明确 `read_own_tenant`、普通 `update` 和 `manage_status` 不应替代凭证管理权限。
  - 规划 route denied audit、route success audit、DTO / error mapping 和 route / service 调用顺序。
  - 规划测试拆分、敏感信息禁区、后续阶段边界和下一阶段建议。
  - README 和 roadmap 轻量同步：凭证 parser / service / DTO 最小实现已完成，下一步进入凭证 API route / permission / audit Plan Mode。
- 边界：
  - docs-only。
  - 未修改 `src/**`。
  - 不实现 API route / permission / audit。
  - 不新增 API。
  - 不修改 route、service、parser、DTO、repository 或权限。
  - 不新增 audit action / reason / domain / query whitelist。
  - 不修改 schema / migration。
  - 不修改测试或 demo seed。
  - 不处理真实凭证。
  - 不保存 token、secret、API key、connection string 或 raw HIS payload。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。

- 验证：
  - `git status --short`：仅 README、roadmap、当天 devlog 和新增 Plan 文档。
  - `git diff --check`：无输出。
  - 本次新增 audit reason / query Plan 文档英文模板残留检查：无输出。
  - 禁止范围检查：无输出。

## Phase 23 HIS 连接配置凭证 provider failure / compensation domain 最小边界

- 分支：`feat/phase23-his-connection-provider-failure-compensation-domain-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 provider failure / compensation domain 最小边界，只落内部 server-side 分类、domain-only compensation summary 和稳定 service result mapping。
- 当前 main commit：`06a8c89543a6d18c9298f8cc274fd535faad3034`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 只读盘点：
  - 现有 provider abstraction、credential service、DTO、audit、repository 和测试模式足够支撑 domain-only 最小实现。
  - 可以不新增 audit reason / action。
  - 可以不修改 audit query whitelist 或 audit repository。
  - 可以不新增 schema / migration。
  - 可以不实现 job queue / outbox / inbox。
  - 可以不改 API route、parser、DTO 响应形态或权限。
  - 可以继续不处理真实凭证、不做测试连接、不接真实 HIS。
  - provider failure / compensation domain 只作为内部 server-side result / classification，不进入 route response、read model 或 audit metadata。
- 修改文件：
  - `src/modules/institution/server/his-connection-credential-provider-failure.ts`
  - `src/modules/institution/server/his-connection-credential-service.ts`
  - `src/modules/institution/tests/HisConnectionCredentialProviderFailure.test.ts`
  - `src/modules/institution/tests/HisConnectionCredentialService.test.ts`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增 provider failure 白名单分类：provider unavailable、timeout、retry exhausted、circuit open、validation failed、tenant / connection mismatch、idempotency conflict、invalid state、provider write / revoke / describe / health failed、repository after provider failed、audit after provider failed。
  - 新增 domain-only compensation summary 状态：`compensation_pending`、`compensation_running`、`compensation_succeeded`、`compensation_failed`、`manual_review_required`。
  - 新增 provider failure 到 service result 的稳定映射：provider validation failed -> `validation_failed`，invalid state -> `invalid_state_transition`，其余 provider / cross-system failure -> `service_unavailable`。
  - service 仅识别 safe provider failure 对象并映射到现有稳定 status；未知 thrown error 仍统一 `service_unavailable`。
  - README 和 roadmap 轻量同步 provider failure / compensation domain 最小边界已完成。
- 边界：
  - 修改 `src/**`：是，仅 provider failure domain helper、credential service 最小 catch mapping 和相关测试。
  - 不新增 API route。
  - 不修改 route、parser、DTO 响应形态或权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不新增 schema / migration。
  - 不实现 job queue / outbox / cleanup。
  - 不接真实 KMS / Vault / cloud provider。
  - 不处理真实凭证。
  - 不保存 token、secret、API key、connection string 或 raw HIS payload。
  - 不让 HTTP route 接收真实凭证明文。
  - 不改造 parser 放行真实凭证材料。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialProviderFailure.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts` 先失败，失败点为缺少 `his-connection-credential-provider-failure` 目标模块。
  - GREEN：同一命令通过，2 个测试文件，17 个测试。
- 验证：
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialProviderFailure.test.ts src/modules/institution/tests/HisConnectionCredentialStorage.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts src/modules/institution/tests/HisConnectionCredentialApiRoutes.test.ts`：4 个测试文件，36 个测试通过。
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialInput.test.ts src/modules/institution/tests/HisConnectionCredentialProviderFailure.test.ts src/modules/institution/tests/HisConnectionCredentialStorage.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts src/modules/institution/tests/HisConnectionRepository.test.ts src/modules/institution/tests/HisConnectionCredentialApiRoutes.test.ts`：6 个测试文件，85 个测试通过。
  - `./node_modules/.bin/tsc --noEmit`：无输出。
  - `./node_modules/.bin/eslint src/modules/institution/server/his-connection-credential-provider-failure.ts src/modules/institution/server/his-connection-credential-service.ts src/modules/institution/tests/HisConnectionCredentialProviderFailure.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts`：无输出。

## Phase 23 HIS 连接配置凭证 audit reason / query whitelist 扩展 Plan Mode

- 分支：`docs/phase23-his-connection-credential-audit-reason-query-plan`
- 目标：创建 docs-only Plan 文档，规划 Phase 23 HIS 连接配置凭证 provider failure / compensation 相关 audit reason、action、query whitelist、metadata、route / service audit、一致性和测试拆分边界。
- 当前 main commit：`46213d66eddfe377c7001043e7387fde33634b31`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 修改文件：
  - `docs/superpowers/specs/2026-06-06-phase23-his-connection-credential-audit-reason-query-plan.md`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增 audit reason / query whitelist 扩展 Plan 文档。
  - 规划 v1 继续复用 `manage_credentials` action，不新增 provider / compensation 专用 action。
  - 规划 provider failure / compensation 候选 audit reason 和 query whitelist 扩展边界。
  - 规划 audit metadata 安全摘要、禁止字段、route / service / compensation audit 职责和 consistency / migration 边界。
  - README 和 roadmap 轻量同步：provider failure / compensation domain 最小边界已完成，下一步进入 audit reason / query whitelist 扩展 Plan Mode。
- 边界：
  - docs-only。
  - 未修改 `src/**`。
  - 不新增 API route。
  - 不修改 route、service、parser、DTO、provider、storage、repository 或权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不新增 schema / migration。
  - 不修改测试或 demo seed。
  - 不实现 audit reason / query whitelist。
  - 不实现 provider failure audit。
  - 不实现 compensation audit。
  - 不实现 job queue / outbox / cleanup。
  - 不接真实 KMS / Vault / provider。
  - 不处理真实凭证。
  - 不保存 token、secret、API key、connection string 或 raw HIS payload。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。
- 验证：
  - `git status --short`：仅本次 4 个 docs / README 范围文件。
  - `git diff --name-only origin/main...HEAD`：提交后仅本次 4 个目标文件。
  - `git diff --stat origin/main...HEAD`：提交后为 `4 files changed, 548 insertions(+)`。
  - `git diff --check origin/main...HEAD`：无输出。
  - 中文化残留检查：全目录命中历史旧文档；本次新增 provider failure / compensation / audit Plan 文档无英文模板残留。
  - 禁止范围检查：无输出。
- 验证：
  - `git status --short`
  - `git diff --name-only origin/main...HEAD`
  - `git diff --stat origin/main...HEAD`
  - `git diff --check origin/main...HEAD`
  - 中文化残留检查
  - 禁止范围检查

## Phase 23 HIS 连接配置凭证 API route / permission / audit 最小实现

- 分支：`feat/phase23-his-connection-credential-api-permission-audit-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 create / update / rotate / clear / revoke API route、专用权限和最小审计闭环。
- 当前 main commit：`8baeaea233301e0dc7346fc2fd5f7f1002877128`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 只读盘点结论：
  - 当前已有 HIS connection API route 模式可复用。
  - 当前已有 route denied audit 模式可复用。
  - 当前已有 allowed audit 写入模式可复用，并已在凭证 service 中以显式 audit factory 接入。
  - 当前已有 permission guard 和 permission action 扩展模式可复用。
  - 当前已有 audit action / reason / domain / query whitelist 扩展模式可复用。
  - 当前可以不新增 schema / migration。
  - 当前可以继续不处理真实凭证、不做测试连接、不接真实 HIS、不接真实 secret manager。
- 修改文件：
  - `src/app/api/institution/his-connections/[connectionId]/credentials/credential-route.ts`
  - `src/app/api/institution/his-connections/[connectionId]/credentials/route.ts`
  - `src/app/api/institution/his-connections/[connectionId]/credentials/rotate/route.ts`
  - `src/app/api/institution/his-connections/[connectionId]/credentials/clear/route.ts`
  - `src/app/api/institution/his-connections/[connectionId]/credentials/revoke/route.ts`
  - `src/modules/security/domain/access-control.ts`
  - `src/modules/audit/tests/AuditEventQueryParser.test.ts`
  - `src/modules/audit/tests/AuditEventsDomain.test.ts`
  - `src/modules/security/tests/AccessControlDomain.test.ts`
  - `src/modules/institution/server/his-connection-credential-service.ts`
  - `src/modules/institution/tests/HisConnectionCredentialApiRoutes.test.ts`
  - `src/modules/institution/tests/HisConnectionCredentialService.test.ts`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增凭证 API route：`POST /credentials`、`PATCH /credentials`、`POST /credentials/rotate`、`POST /credentials/clear`、`POST /credentials/revoke`。
  - 新增 `open_connection:manage_credentials`，默认仅 `tenant_admin` 具备，普通机构角色、平台角色和审计角色默认拒绝。
  - route 调用顺序固定为 path `connectionId` -> access context -> permission -> body -> parser -> service -> DTO。
  - 权限拒绝和 parser failure 写 route denied audit；audit 失败 fail closed 为 `503 service_unavailable`。
  - 凭证 service 在 route 显式注入 audit factory 时写 allowed audit；allowed audit 失败 fail closed 为 `service_unavailable`。
  - 成功 DTO 仅返回 `ok` 与 `credentialConfigured`，错误响应仅返回稳定 code / error。
- 边界：
  - 新增 API route，但不新增 schema / migration。
  - 修改权限 action / policy 和 audit query action 白名单；不新增 audit reason。
  - 修改 service 仅用于凭证成功 allowed audit 注入，不改变 parser / repository 安全边界。
  - 不保存真实凭证。
  - 不保存真实 token / secret / API key / connection string。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不保存 raw HIS payload。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。
- TDD 验证：
  - RED：凭证 route 测试先失败于 route 文件缺失；权限 / audit / service 测试先失败于缺少 `manage_credentials` 与 allowed audit。
  - GREEN：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialApiRoutes.test.ts src/modules/security/tests/AccessControlDomain.test.ts src/modules/audit/tests/AuditEventQueryParser.test.ts src/modules/audit/tests/AuditEventsDomain.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts` 通过，5 个测试文件，68 个测试。

## Phase 23 HIS 连接配置凭证加密与真实 secret manager Plan Mode

- 分支：`docs/phase23-his-connection-credential-secret-manager-plan`
- 目标：创建 docs-only Plan 文档，规划 Phase 23 HIS 连接配置凭证加密、真实 secret manager、provider 抽象、密钥轮换、审计和回滚边界。
- 当前 main commit：`c3f36acf8d6cc8f515ce25dee9ba46b88462e3db`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 修改文件：
  - `docs/superpowers/specs/2026-06-06-phase23-his-connection-credential-secret-manager-plan.md`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增凭证加密与真实 secret manager Plan 文档。
  - 规划 secret manager provider 抽象、fake provider 与 real provider 边界。
  - 规划真实 one-time credential material 的进入、短暂驻留和泄露禁区。
  - 规划 `credentialRef` 生产语义、provider namespace、version summary、digest 和默认禁止对 route / DTO / read model 暴露。
  - 规划数据库加密存储、外部 secret manager、envelope encryption、KMS key rotation、secret value rotation、tenant / connection isolation、backup / restore 和 disaster recovery 边界。
  - 规划 rotate / clear / revoke 在真实 provider 下的版本、失败、补偿、审计和回滚边界。
  - 规划 audit metadata 允许记录的安全摘要和禁止记录的敏感字段。
  - 规划 DTO / error mapping、测试拆分、后续阶段边界和下一阶段建议。
  - README 和 roadmap 轻量同步：凭证 API route / permission / audit 最小实现已完成，下一步进入凭证加密与真实 secret manager Plan Mode。
- 边界：
  - docs-only。
  - 未修改 `src/**`。
  - 不新增 API。
  - 不修改 route、service、parser、DTO、repository 或权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不新增 schema / migration。
  - 不修改测试或 demo seed。
  - 不实现 secret manager。
  - 不接真实 KMS / Vault / provider。
  - 不处理真实凭证。
  - 不保存 token、secret、API key、connection string 或 raw HIS payload。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。
- 验证：
  - `git status --short`：仅本次 4 个 docs / README 范围文件。
  - `git diff --name-only origin/main...HEAD`：提交后仅本次 4 个目标文件。
  - `git diff --stat origin/main...HEAD`：提交后仅本次 docs-only diff。
  - `git diff --check origin/main...HEAD`：无输出。
  - 中文化残留检查：全目录命中历史旧文档；本次新增 secret manager Plan 文档无英文模板残留。
  - 禁止范围检查：无输出。

## Phase 23 HIS 连接配置凭证 provider 抽象接口最小边界

- 分支：`feat/phase23-his-connection-secret-provider-interface-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 provider 抽象接口最小边界，承接凭证加密与真实 secret manager Plan Mode，但不进入真实 secret manager 实现。
- 当前 main commit：`7ef328a161110bc93606ac719cd5db8e28322454`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 只读盘点结论：
  - 当前凭证 API route 只在模块级创建 fake storage 并注入 service；保留兼容 factory 后可以不修改 route。
  - 当前 parser 只接受 `syntheticPlaceholder` 和 `idempotencyKey`，并拒绝 token、secret、API key、connection string、raw credential、raw payload 和 external secret path。
  - 当前 DTO 只返回 `{ ok, credentialConfigured }` 或稳定错误 code / error，不暴露 `credentialRef`、provider 内部信息或敏感字段。
  - 当前 service create / update / rotate 已经只通过 storage 生成安全 `credentialRef` 后写 repository，适合把 storage shape 收敛为 provider interface。
  - 当前 fake in-memory storage 已有安全 `credentialRef`、tenant + connection scoped idempotency、digest metadata 和 revoke 测试基础，适合最小补充 provider health / describe。
  - 当前权限、audit action / reason / query whitelist、schema / migration 和 repository 不需要修改。
- 修改文件：
  - `src/modules/institution/server/his-connection-credential-storage.ts`
  - `src/modules/institution/server/his-connection-credential-service.ts`
  - `src/modules/institution/tests/HisConnectionCredentialStorage.test.ts`
  - `src/modules/institution/tests/HisConnectionCredentialService.test.ts`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增 `HisConnectionCredentialProvider` 抽象接口，覆盖 `storeSyntheticCredentialReference`、`revokeCredentialReference`、`describeCredentialReference` 和 `health`。
  - 新增 `createInMemoryHisConnectionCredentialProvider()`，基于现有 fake in-memory storage 能力实现 test-only provider。
  - 保留 `createInMemoryHisConnectionCredentialStorage()` 作为兼容包装，避免修改凭证 API route。
  - provider `health` 仅表达 fake provider 可用性和 test-only 边界，不代表测试连接、真实 HIS 或真实 secret manager 可用。
  - provider `describeCredentialReference` 只返回安全 summary：`tenantId`、`connectionId`、不可逆 `credentialRefDigest`、provider、`storedAt` 和 `revokedAt`。
  - service 依赖类型改为 `Pick<HisConnectionCredentialProvider, 'storeSyntheticCredentialReference'>`，保持 create / update / rotate 通过 provider 抽象生成安全引用。
  - storage / service 测试补充 provider health、describe 安全 summary、tenant / connection 隔离、敏感引用拒绝和 provider health / describe 不被 service 当作测试连接调用。
- 边界：
  - 不新增 API route。
  - 不修改凭证 API route。
  - 不修改权限实现或权限测试。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 parser / DTO / repository。
  - 不新增 schema / migration。
  - 不修改 demo seed。
  - 不修改 `package.json` 或 lockfile。
  - 不修改 `.env*`、`.codex`、Superpowers 缓存目录或技能文件。
  - 不接真实 KMS / Vault / cloud secret manager。
  - 不实现真实 secret manager provider。
  - 不处理真实凭证。
  - 不保存 token、secret、API key、connection string 或 raw HIS payload。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialStorage.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts` 先失败，失败点为 `createInMemoryHisConnectionCredentialProvider` 缺失，3 个 provider 用例失败。
  - GREEN：同一命令通过，2 个测试文件，19 个测试。
- 验证：
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialStorage.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts src/modules/institution/tests/HisConnectionCredentialApiRoutes.test.ts`：3 个测试文件，30 个测试通过。
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialInput.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts src/modules/institution/tests/HisConnectionCredentialStorage.test.ts src/modules/institution/tests/HisConnectionRepository.test.ts src/modules/institution/tests/HisConnectionCredentialApiRoutes.test.ts`：5 个测试文件，79 个测试通过。
  - `./node_modules/.bin/tsc --noEmit`：无输出。
  - `./node_modules/.bin/eslint src/modules/institution/server/his-connection-credential-storage.ts src/modules/institution/server/his-connection-credential-service.ts src/modules/institution/tests/HisConnectionCredentialStorage.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts`：无输出。
  - `./node_modules/.bin/eslint .`：被既有非本 PR 文件阻断，阻断点为 `src/modules/institution/components/HisConnectionReadOnlyPanel.tsx`、`src/modules/institution/components/TreatmentSummaryManagementShell.tsx` 的 hook 规则错误，以及 `src/modules/auth/components/LuxuryLoginShell.tsx` 的 `<img>` warning。
  - `git diff --check`：无输出。
  - 中文化残留检查：全目录命中历史旧文档；本次未新增 Superpowers plan / spec 文档。
  - 禁止范围检查：无输出。

## Phase 23 HIS 连接配置凭证 provider failure / compensation / audit Plan Mode

- 分支：`docs/phase23-his-connection-provider-failure-compensation-audit-plan`
- 目标：创建 docs-only Plan 文档，规划 Phase 23 HIS 连接配置凭证 provider 失败分类、补偿一致性、audit reason / action、audit metadata、transaction / consistency、调用顺序、DTO / error mapping 和测试拆分边界。
- 当前 main commit：`09c9e762c485a46e6017ead417c7f1b70a89c456`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 修改文件：
  - `docs/superpowers/specs/2026-06-06-phase23-his-connection-provider-failure-compensation-audit-plan.md`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增 provider failure / compensation / audit Plan 文档。
  - 规划 provider unavailable、timeout、retry exhausted、circuit open、validation failed、tenant / connection mismatch、idempotency conflict、provider / repository / audit 交错失败、revoke / describe / health failure 等分类。
  - 规划 compensation 触发条件、状态枚举、幂等 key、retry、人工处理、tenant / connection 绑定、orphan secret 检测与清理和后续 schema / migration 评估边界。
  - 规划 audit reason / action、audit metadata、transaction / consistency、route / service / provider 调用顺序、DTO / error mapping 和测试拆分建议。
  - README 和 roadmap 轻量同步：provider 抽象接口最小边界已完成，下一步进入 provider failure / compensation / audit Plan Mode。
- 边界：
  - docs-only。
  - 未修改 `src/**`。
  - 不新增 API。
  - 不修改 route、service、parser、DTO、provider、storage、repository 或权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不修改测试或 demo seed。
  - 不实现 provider failure handling。
  - 不实现 compensation job / queue。
  - 不新增 audit reason / action。
  - 不接真实 KMS / Vault / provider。
  - 不处理真实凭证。
  - 不保存 token、secret、API key、connection string 或 raw HIS payload。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。

## Phase 23 HIS 连接配置凭证 audit reason / query whitelist 最小边界

- 分支：`feat/phase23-his-connection-credential-audit-reason-query-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 provider failure / compensation 相关 audit reason 与查询白名单最小边界。
- 当前 main commit：`56cdecbd9fa7a8e2ba3f7d87ac6b5051fc77dce4`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 只读盘点结论：
  - 现有 audit domain 已集中维护 `AuditReason`。
  - 现有 audit query domain 已集中维护 `AUDIT_REASON_VALUES`。
  - 现有 query parser 已复用 `AUDIT_REASON_VALUES` 校验 `reason`，无需新增 parser 分支。
  - 现有 `open_connection:manage_credentials` action 已可用于凭证管理审计查询。
  - 现有 audit result 仍为 `allowed` / `denied` / `transitioned`，本次无需也不得新增 `failure` result。
  - 当前具备最小实现条件，可以只补 audit reason / query whitelist 与测试。
- 修改文件：
  - `src/modules/audit/domain/audit-events.ts`
  - `src/modules/audit/domain/audit-event-query.ts`
  - `src/modules/audit/tests/AuditEventQueryParser.test.ts`
  - `src/modules/audit/tests/AuditEventsDomain.test.ts`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增 provider failure 稳定 audit reason：`provider_unavailable`、`provider_timeout`、`provider_retry_exhausted`、`provider_circuit_open`、`provider_validation_failed`、`provider_write_failed`、`provider_revoke_failed`、`provider_describe_failed`、`provider_health_failed`。
  - 新增一致性 / 补偿稳定 audit reason：`repository_after_provider_failed`、`audit_after_provider_failed`、`compensation_pending`、`compensation_running`、`compensation_succeeded`、`compensation_failed`、`manual_review_required`。
  - `AUDIT_REASON_VALUES` 同步纳入以上 reason，现有 query parser 可通过 `reason` 查询这些稳定枚举。
  - 继续复用 `open_connection:manage_credentials` action，不新增 provider / compensation 专用 action。
  - 继续复用 `allowed` / `denied` / `transitioned` result，不新增 `failure` result。
  - README 和 roadmap 轻量同步：audit reason / query whitelist 最小边界已完成。
- 边界：
  - 本次修改 `src/**` 仅限 audit domain / reason / query whitelist 和对应测试。
  - 不新增 API route。
  - 不修改 route、service、parser、DTO、provider、storage 或 repository。
  - 不修改权限实现或权限测试。
  - 不修改 audit repository。
  - 不新增 audit action、result 或 metadata schema。
  - 不新增 schema / migration。
  - 不修改 demo seed。
  - 不实现 provider failure audit。
  - 不实现 compensation audit。
  - 不实现 job queue / outbox / cleanup。
  - 不接真实 KMS / Vault / provider。
  - 不处理真实凭证。
  - 不保存 token、secret、API key、connection string 或 raw HIS payload。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventQueryParser.test.ts src/modules/audit/tests/AuditEventsDomain.test.ts` 先失败，失败点为新 reason 尚未进入 `AUDIT_REASON_VALUES`，query parser 返回 `reason 不在允许范围内`。
  - GREEN：同一命令通过，2 个测试文件，24 个测试。
- 验证：
  - `git status --short`：仅本次 7 个目标文件。
  - `git diff --name-only`：仅 README、roadmap、当天 devlog、audit reason / query whitelist 文件和对应 audit 测试。
  - `git diff --stat`：`7 files changed, 227 insertions(+), 2 deletions(-)`。
  - `git diff --check`：无输出。
  - `node scripts/run-vitest.mjs run src/modules/audit/tests/AuditEventQueryParser.test.ts src/modules/audit/tests/AuditEventsDomain.test.ts`：2 个测试文件，24 个测试通过。
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialProviderFailure.test.ts src/modules/institution/tests/HisConnectionCredentialStorage.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts src/modules/institution/tests/HisConnectionCredentialApiRoutes.test.ts src/modules/audit/tests/AuditEventQueryParser.test.ts src/modules/audit/tests/AuditEventsDomain.test.ts`：6 个测试文件，60 个测试通过。
  - `./node_modules/.bin/tsc --noEmit`：无输出。
  - `./node_modules/.bin/eslint src/modules/audit/domain/audit-events.ts src/modules/audit/domain/audit-event-query.ts src/modules/audit/tests/AuditEventsDomain.test.ts src/modules/audit/tests/AuditEventQueryParser.test.ts`：无输出。
  - 中文化残留检查：全目录命中历史旧文档；本次未新增或修改 Superpowers plan / spec 文档。
  - 禁止范围检查：无输出。
  - 额外禁止检查：无输出。

## Phase 23 HIS 连接配置凭证 provider failure audit / service audit Plan Mode

- 分支：`docs/phase23-his-connection-provider-failure-service-audit-plan`
- 目标：创建 docs-only Plan 文档，规划 Phase 23 HIS 连接配置凭证 provider failure audit / service audit 边界。
- 当前 main commit：`247d117bcdd79b83c1df50ff64a0ba418061a374`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 修改文件：
  - `docs/superpowers/specs/2026-06-06-phase23-his-connection-provider-failure-service-audit-plan.md`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增 provider failure audit / service audit Plan 文档。
  - 规划 provider failure audit 写入职责、写入时机、去重、fail closed 和敏感信息禁区。
  - 规划 service audit 成功路径保持、provider failure 失败路径、audit 失败映射和事务顺序边界。
  - 规划 route audit 与 service audit 职责拆分，避免 provider failure 被 route 重复审计。
  - 规划 compensation audit 后续进入条件、职责归属、metadata / schema 前置条件和敏感信息禁区。
  - 规划已存在 audit reason 的使用时机，不新增 reason / action / result。
  - 规划无 metadata schema 时的字段使用边界、fail closed / best effort 取舍和后续测试拆分。
  - README 和 roadmap 轻量同步：audit reason / query whitelist 最小边界已完成，下一步进入 provider failure audit / service audit Plan Mode。
- 边界：
  - docs-only。
  - 未修改 `src/**`。
  - 不新增 API route。
  - 不修改 route、service、parser、DTO、provider、storage、repository 或权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不修改测试或 demo seed。
  - 不实现 provider failure audit。
  - 不实现 service audit 逻辑变更。
  - 不实现 compensation audit。
  - 不新增 metadata schema。
  - 不实现 job queue / outbox / cleanup。
  - 不接真实 KMS / Vault / provider。
  - 不处理真实凭证。
  - 不保存 token、secret、API key、connection string 或 raw HIS payload。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。
- 验证：
  - `git status --short`：仅本次 4 个 docs / README 范围文件。
  - `git diff --name-only origin/main...HEAD`：提交后仅本次 4 个目标文件。
  - `git diff --stat origin/main...HEAD`：提交后为 `4 files changed, 614 insertions(+)`。
  - `git diff --check origin/main...HEAD`：无输出。
  - 中文化残留检查：全目录命中历史旧文档；本次新增 provider failure audit / service audit Plan 文档无英文模板残留。
  - 禁止范围检查：无输出。

## Phase 23 HIS 连接配置凭证 provider failure audit / service audit 最小边界

- 分支：`feat/phase23-his-connection-provider-failure-service-audit-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 provider failure audit / service audit 最小边界。
- 当前 main commit：`d451585cc817a8c7e52da79d3217ad905607e831`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 只读盘点结论：
  - credential service 当前 allowed audit 写入位置在 repository `ok` 后的 `recordAllowedCredentialAudit`。
  - provider failure 当前在 stored credential flow 和 clear / revoke flow 的 catch 中通过 `isHisConnectionCredentialProviderFailure(error)` 识别，并映射到稳定 service status。
  - 现有 `createAuditEvent` audit factory 和 injected audit writer 可复用。
  - provider failure audit 可以由 service 层写入。
  - 可以不改 API route、parser / DTO、provider / storage、repository、权限、audit domain / reason / query whitelist、audit repository、schema / migration 或 metadata schema。
  - 可以不实现 compensation audit，继续不处理真实凭证、不做测试连接、不接真实 HIS。
- 修改文件：
  - `src/modules/institution/server/his-connection-credential-service.ts`
  - `src/modules/institution/tests/HisConnectionCredentialService.test.ts`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - credential service 对已知 safe provider failure 写 `resource: "open_connection"`、`resourceId: connectionId`、`action: "manage_credentials"`、`result: "denied"` audit。
  - provider failure category 映射到既有 audit reason：provider unavailable、timeout、retry exhausted、circuit open、validation failed、provider write / revoke / describe / health failed、repository after provider failed、audit after provider failed 均使用既有 provider failure reason。
  - `tenant_connection_mismatch` 复用 `not_found_or_not_owned`，`idempotency_conflict` 复用 `provider_validation_failed`，`invalid_state` 复用 `invalid_transition`。
  - provider failure audit 写入失败 fail closed 为 `service_unavailable`；写入成功后返回既有稳定 service status。
  - 未知 thrown error 仍只返回 `service_unavailable`，不写 provider failure audit。
  - provider failure 不写 allowed audit；route denied audit 继续只覆盖 permission denied / parser failure，不补写 service provider failure denied audit。
  - provider failure audit event 不写 provider path、secret path、`credentialRef`、idempotencyKey、synthetic placeholder、token、secret、API key、connection string、raw credential、raw HIS payload、request / response body、provider raw error、SQL、stack 或 `DATABASE_URL`。
- 边界：
  - 不新增 API route。
  - 不修改 route、parser、DTO、provider、storage、repository 或权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不新增 schema / migration。
  - 不新增 metadata schema。
  - 不实现 compensation audit。
  - 不接真实 KMS / Vault / provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不保存 raw HIS payload。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialService.test.ts src/modules/institution/tests/HisConnectionCredentialProviderFailure.test.ts` 先失败，失败点为 provider failure 未写 denied audit、provider revoke failure 未写 audit、provider failure audit 失败未 fail closed。
  - GREEN：同一命令通过，2 个测试文件，20 个测试。
- 验证：
  - `node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialInput.test.ts src/modules/institution/tests/HisConnectionCredentialProviderFailure.test.ts src/modules/institution/tests/HisConnectionCredentialStorage.test.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts src/modules/institution/tests/HisConnectionRepository.test.ts src/modules/institution/tests/HisConnectionCredentialApiRoutes.test.ts src/modules/audit/tests/AuditEventsDomain.test.ts src/modules/audit/tests/AuditEventQueryParser.test.ts`：8 个测试文件，112 个测试通过。
  - `./node_modules/.bin/tsc --noEmit`：无输出。
  - `./node_modules/.bin/eslint src/modules/institution/server/his-connection-credential-service.ts src/modules/institution/tests/HisConnectionCredentialService.test.ts`：无输出。
  - `git diff --check`：无输出。
  - 中文化残留检查：全目录命中历史旧 Superpowers plan / spec 文档；本次未新增或修改 Superpowers plan / spec 文档。
  - 禁止范围检查：无输出。
  - 额外 migration / schema / seed / route / api 禁止检查：无输出。

## Phase 23 HIS 连接配置凭证 compensation audit Plan Mode

- 分支：`docs/phase23-his-connection-credential-compensation-audit-plan`
- 目标：规划 Phase 23 HIS 连接配置凭证 compensation audit 边界。
- 当前 main commit：`2a66f22ab8dea7f520432bc1562462fcb4582362`
- devlog 日期规则执行结果：本次任务追加到任务执行当天 `docs/devlog/2026-06-06.md`。
- 修改文件：
  - `docs/superpowers/specs/2026-06-06-phase23-his-connection-credential-compensation-audit-plan.md`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增 compensation audit docs-only Plan 文档。
  - 规划 `compensation_pending`、`compensation_running`、`compensation_succeeded`、`compensation_failed` 和 `manual_review_required` 的触发场景、audit reason / result、可见性、metadata、持久化、schema / migration、outbox / job queue 和人工复核边界。
  - 明确 compensation audit 应由 compensation domain / job 层写入，route / provider 不写 compensation audit，service 当前只负责 provider failure audit 和稳定 service status。
  - 明确当前无 metadata schema、无 compensation state、无 outbox / job queue，因此本阶段不实现完整 compensation audit。
  - 明确后续如需 operationId、retry count、manual review、dead letter、metadata query 或正式关联，必须单独进入 metadata schema / outbox Plan Mode。
  - 明确 compensation audit 继续复用 `open_connection` resource、`manage_credentials` action、既有 compensation reason 和 `allowed` / `denied` result，不新增 audit reason / action / result。
  - 明确 provider failure audit 与 compensation audit 当前只能粗粒度人工关联，正式关联需要 safe operationId 或 compensation operation 表。
  - 规划 compensation audit 写入失败、provider cleanup 成功但 audit 失败、operator alert 和 best effort / fail closed 取舍。
  - 补充后续测试拆分建议。
- 边界：
  - docs-only。
  - 未修改 `src/**`。
  - 不新增 API route。
  - 不修改 route、service、parser、DTO、provider、storage、repository 或权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不新增 audit reason / action / result。
  - 不新增 metadata schema。
  - 不新增 schema / migration。
  - 不修改测试。
  - 不实现 compensation audit。
  - 不实现 job queue / outbox / cleanup。
  - 不接真实 KMS / Vault / provider。
  - 不处理真实凭证。
  - 不保存 token、secret、API key、connection string 或 raw HIS payload。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。
- 验证：
  - `git diff --cached --name-only`：仅本次 4 个 docs / README 范围文件。
  - `git diff --cached --stat`：`4 files changed, 464 insertions(+), 1 deletion(-)`。
  - `git diff --cached --check`：无输出。
  - 本次新增 compensation audit Plan 文档中文化残留检查：无输出。
  - 允许文件范围检查：无输出。
  - 禁止范围检查：无输出。

## Phase 23 HIS 连接配置凭证 compensation metadata / outbox Plan Mode

- 分支：`docs/phase23-his-connection-credential-compensation-metadata-outbox-plan`
- 目标：先只读盘点 compensation audit 最小实现是否可在无 metadata schema / outbox / job queue / schema migration 下完成；盘点后规划 metadata / outbox 边界。
- 当前 main commit：`342725b010cca78024d8b3829e403eae589c7f7b`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`。
- 只读盘点结论：
  - 当前没有 compensation state 持久化位置。
  - 当前没有 compensation operationId 或等价 safe operation key。
  - 当前没有 outbox / job queue。
  - 当前没有 audit metadata schema。
  - 当前 audit repository 只落标准审计字段，不落 metadata。
  - 当前 query parser 不支持 metadata filter，并拒绝 `metadata`、`compensationState`、`failureCategory` 等非白名单字段。
  - 当前只能用现有 audit 字段表达孤立 compensation reason，不能安全表达补偿链路、状态生命周期、retry、dead letter 或 manual review。
  - 当前不满足 compensation audit 最小实现条件，停止实现路径，改做 metadata / outbox docs-only Plan PR。
- 修改文件：
  - `docs/superpowers/specs/2026-06-06-phase23-his-connection-credential-compensation-metadata-outbox-plan.md`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增 compensation metadata / outbox docs-only Plan 文档。
  - 说明为什么 compensation audit 最小实现不能在无 metadata / outbox 下安全完成。
  - 规划后续 metadata 字段、compensation state、operationId、outbox 表、job queue、dead letter / manual review、schema / migration、audit repository、query parser 和测试拆分边界。
  - 明确继续复用既有 `open_connection` resource、`manage_credentials` action、compensation reason 和 `allowed` / `denied` result，不新增 audit reason / action / result。
- 边界：
  - docs-only。
  - 未修改 `src/**`。
  - 不新增 API route。
  - 不修改 route、service、parser、DTO、provider、storage、repository 或权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不新增 audit reason / action / result。
  - 不新增 metadata schema。
  - 不新增 schema / migration。
  - 不修改测试。
  - 不实现 compensation audit。
  - 不实现 job queue / outbox / cleanup。
  - 不接真实 KMS / Vault / provider。
  - 不处理真实凭证。
  - 不保存 token、secret、API key、connection string 或 raw HIS payload。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不接企微、AI / RAG / Agent。
  - 不创建自动治疗摘要、自动随访任务或自动触达。
- 验证：
  - `git diff --cached --name-only`：仅本次 4 个 docs / README 范围文件。
  - `git diff --cached --stat`：`4 files changed, 530 insertions(+), 1 deletion(-)`。
  - `git diff --cached --check`：无输出。
  - 本次新增 compensation metadata / outbox Plan 文档中文化残留检查：无输出。
  - 禁止范围检查：无输出。
  - 额外 migration / schema / seed / route / api 文件名检查：无输出。

## Phase 23 HIS 连接配置凭证 compensation metadata / operationId schema 最小边界

- 分支：`feat/phase23-his-connection-compensation-metadata-operationid-schema-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 compensation metadata / operationId schema 最小边界，为后续 compensation audit 提供安全 operationId 与状态承载。
- 当前 main commit：`c692b4bbd589f84997cedeb33927bb627cf79019`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 只读盘点结论：
  - 当前 schema / migration 模式清楚，沿用 `src/server/db/schema.ts`、`drizzle/*.sql`、`drizzle/meta/_journal.json`、`drizzle/meta/*_snapshot.json` 和 schema 测试模式。
  - 当前没有可复用的 compensation operation / outbox / audit metadata 表。
  - 本阶段应新增 compensation operation 状态承载表，而不是 audit metadata 表。
  - 可以只实现 metadata / operationId schema 和 domain-only metadata helper，不实现 outbox / job queue。
  - 可以不修改 audit repository、service、API route、parser / DTO、provider / storage、audit domain / reason / query whitelist。
  - 可以不实现 compensation audit，不处理真实凭证，不做测试连接，不接真实 HIS。
- 修改文件：
  - `src/server/db/schema.ts`
  - `src/server/db/tests/Schema.test.ts`
  - `drizzle/0007_phase23_his_connection_credential_compensation_metadata_operationid.sql`
  - `drizzle/meta/_journal.json`
  - `drizzle/meta/0007_snapshot.json`
  - `src/modules/institution/server/his-connection-credential-provider-failure.ts`
  - `src/modules/institution/tests/HisConnectionCredentialProviderFailure.test.ts`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增 `his_connection_credential_compensation_operations` 表，用于承载 `tenantId`、`connectionId`、server-generated safe `operationId`、operation type、compensation state、provider failure category、retry count、manual review 标记和时间戳。
  - 新增 operationId 唯一索引、tenant / connection / state 查询索引，以及 tenant 和 HIS connection 外键。
  - 新增 operation type、compensation state 和 provider failure category 白名单枚举。
  - 新增 domain-only compensation operation metadata helper，只输出安全 operationId、状态、failure category、retry count 和 manual review 标记。
  - operationId 只接受服务端生成的安全格式，不接受 request / query / header / localStorage、tenantId / connectionId 明文拼接、credentialRef、provider path、secret path、idempotencyKey 或 scoped idempotency key。
  - schema / type / test 均禁止 token、secret、API key、connection string、raw credential、raw HIS payload、request / response body、SQL、stack、`DATABASE_URL`、provider raw error、provider path、secret path、credentialRef 和 idempotencyKey。
- 边界：
  - 本阶段仅提供 schema / migration / type / test 准备层。
  - 不在 service、route 或 provider 中创建 compensation operation。
  - 不写 compensation audit。
  - 不写 outbox job，不实现 job queue / worker。
  - 不修改 audit repository。
  - 不修改 query parser，不暴露 read API，不修改 DTO。
  - 不接真实 KMS / Vault / provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不保存 raw HIS payload。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/server/db/tests/Schema.test.ts src/modules/institution/tests/HisConnectionCredentialProviderFailure.test.ts` 先失败，失败点为 schema exports、0007 snapshot / migration 和 operation metadata helper 尚未存在。
  - GREEN：同一命令通过，2 个测试文件，37 个测试。
- 后续建议：
  - 下一步进入 outbox / job queue Plan Mode 或 compensation operation repository Plan Mode。
  - compensation audit 最小实现仍需在 operation state / outbox / repository 边界明确后单独进入，不在本 PR 实现。

## Phase 23 HIS 连接配置凭证 compensation operation repository Plan Mode

- 分支：`docs/phase23-his-connection-compensation-operation-repository-plan`
- 目标：创建 docs-only Plan 文档，规划 Phase 23 HIS 连接配置凭证 compensation operation repository 边界。
- 当前 main commit：`f01464f1ffb1a0d1c3e85eb72e145630b70a870f`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 修改文件：
  - `docs/superpowers/specs/2026-06-06-phase23-his-connection-compensation-operation-repository-plan.md`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增 compensation operation repository docs-only Plan 文档。
  - 规划 repository 最小职责：create operation、tenant + operationId 查询、tenant + connectionId + operationId 查询、mark running / succeeded / failed / manual review required、retry count、lastAttemptAt / completedAt、pending query 和 stale running query。
  - 规划 tenant / connection 隔离、operationId 可信来源、状态流转、retry / stale running / manual review、failure category、DTO / read model、transaction / consistency、outbox / job queue 关系、audit 边界和测试拆分。
  - README 和 roadmap 轻量同步：compensation metadata / operationId schema 最小边界已完成，下一步进入 compensation operation repository Plan Mode。
- 边界：
  - docs-only。
  - 未修改 `src/**`。
  - 未修改 `drizzle/**`。
  - 不新增 API route。
  - 不修改 route、service、parser、DTO、provider、storage、repository 或权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration。
  - 不修改测试。
  - 不实现 compensation operation repository。
  - 不实现 outbox / job queue。
  - 不实现 compensation audit。
  - 不接真实 KMS / Vault / provider。
  - 不处理真实凭证。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不保存 raw HIS payload。
  - 不接 webhook / sync、患者身份匹配、企微、AI / RAG / Agent、经营智能中心、图表或导出。
- 验证：
  - `git diff --cached --name-only`：仅本次 4 个 docs / README 范围文件。
  - `git diff --cached --stat`：`4 files changed, 421 insertions(+), 1 deletion(-)`。
  - `git diff --cached --check`：无输出。
  - 本次新增 compensation operation repository Plan 文档中文化残留检查：无输出。
  - 禁止范围检查：无输出。

## Phase 23 HIS 连接配置凭证 compensation operation repository 最小边界

- 分支：`feat/phase23-his-connection-compensation-operation-repository-min`
- 目标：实现 Phase 23 HIS 连接配置凭证 compensation operation repository 最小边界，为后续 outbox / job queue 和 compensation audit integration 提供安全 repository 基础。
- 当前 main commit：`4990ed44d2045c553b4071c0e0536bce75353a48`
- devlog 日期规则执行结果：本次任务继续追加到任务执行当天 `docs/devlog/2026-06-06.md`，不追加到旧日期 `docs/devlog/2026-06-04.md`。
- 只读盘点结论：
  - 当前 `his_connection_credential_compensation_operations` 表和 schema 足够支撑最小 repository。
  - 当前 server-generated safe operationId helper 可复用，repository 只校验安全格式，不从 request / query / header / localStorage 生成或读取 operationId。
  - 当前 HIS connection repository factory 模式和 repository tests mock 模式清楚，可沿用。
  - 可以只新增 repository，不改 service、API route、parser / DTO、provider / storage、权限、audit domain / reason / query whitelist、audit repository 或 schema / migration。
  - 可以不实现 outbox / job queue，不实现 compensation audit，不处理真实凭证，不做测试连接，不接真实 HIS。
- 修改文件：
  - `src/modules/institution/server/his-connection-credential-compensation-operation-repository.ts`
  - `src/modules/institution/tests/HisConnectionCredentialCompensationOperationRepository.test.ts`
  - `README.md`
  - `docs/roadmap/2026-05-30-clean-roadmap-from-rebuild-plan.md`
  - `docs/devlog/2026-06-06.md`
- 完成：
  - 新增 compensation operation repository factory，只操作 `his_connection_credential_compensation_operations` 表。
  - 新增 create、tenant + operationId 查询、tenant + connectionId + operationId 查询、mark running / succeeded / failed / manual review required、retry count increment、pending list 和 stale running list。
  - 所有 read / write 均绑定可信 `tenantId`；connection 相关操作绑定 `tenantId + connectionId + operationId`；cross tenant / cross connection 统一 `not_found`。
  - create 校验 safe operationId 与 provider failure category 白名单，拒绝危险 operationId、自由文本 category 和敏感字段。
  - 状态流转保持最小白名单：pending / failed / manual review -> running，running -> succeeded / failed / manual review；succeeded 不允许回退。
  - retryCount 只能通过显式方法递增，lastAttemptAt 在 running / retry 时更新，completedAt 在 succeeded / failed / manual review 时更新，manualReviewRequired 仅在 manual review 状态置 true。
  - read model 只返回 operationId、tenantId、connectionId、operationType、state、failureCategory、retryCount、manualReviewRequired 和时间戳，不返回凭证、provider path、secret path、raw payload、SQL、stack 或 `DATABASE_URL`。
- 边界：
  - 不接 service runtime。
  - 不新增 route / API。
  - 不修改 parser / DTO。
  - 不修改 provider / storage。
  - 不修改权限。
  - 不修改 audit domain / reason / query whitelist。
  - 不修改 audit repository。
  - 不修改 schema / migration 或 Drizzle snapshot / journal。
  - 不实现 compensation audit。
  - 不实现 outbox / job queue / worker / claim / lock。
  - 不接真实 KMS / Vault / provider。
  - 不处理真实凭证。
  - 不保存 token、secret、API key、connection string 或 raw HIS payload。
  - 不做测试连接。
  - 不接真实 HIS。
  - 不接 webhook / sync、患者身份匹配、企微、AI / RAG / Agent、经营智能中心、图表或导出。
- TDD 验证：
  - RED：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationOperationRepository.test.ts` 先失败，失败点为 repository module 尚未存在。
  - GREEN：同一命令通过，1 个测试文件，12 个测试。
  - 相关回归：`node scripts/run-vitest.mjs run src/modules/institution/tests/HisConnectionCredentialCompensationOperationRepository.test.ts src/modules/institution/tests/HisConnectionCredentialProviderFailure.test.ts src/server/db/tests/Schema.test.ts` 通过，3 个测试文件，51 个测试。
  - `./node_modules/.bin/tsc --noEmit`：通过，无输出。
  - `./node_modules/.bin/eslint src/modules/institution/server/his-connection-credential-compensation-operation-repository.ts src/modules/institution/tests/HisConnectionCredentialCompensationOperationRepository.test.ts`：通过，无输出。
- 后续建议：
  - 下一步进入 outbox / job queue Plan Mode，明确 claim / lock / stale running 恢复 / dead letter / manual review 边界。
  - 或进入 compensation audit repository / service integration Plan Mode，明确 operation state 与 audit event 写入职责、fail closed / best effort 边界和测试拆分。
