# 第五阶段：Demo、Mock、Fixture、Seed 调用关系审计

- 日期：2026-07-26
- 分支：`refactor/demo-mock-seed-audit-20260726-002033`
- 基线：`b833f9a6fa4a3faf275cf07f0a58560576250889`
- 审计候选：44 个
- 本阶段只审计和分组，不移动任何业务文件。

## 审计结论

- 已完成 44 个候选文件的存在性、归属和引用范围检查。
- 其中 44 个候选发现了其他文件引用。
- 其中 18 个候选自身或调用方位于运行时边界。
- 本阶段允许移动的文件数：0。
- 所有候选的 `move_now` 均为 `no`。

## 分组汇总

| 分组 | 数量 | 当前处理意见 |
|---|---:|---|
| A-文档历史 | 14 | 保留历史位置，后续单独治理文档目录。 |
| B-测试归属 | 11 | 继续随所属模块保留。 |
| C-模块 Mock | 5 | 先区分运行时调用和测试调用。 |
| D-运行时 Demo 边界 | 11 | 禁止按文件名直接移入测试目录。 |
| E-Demo 脚本 | 1 | 先核对入口、数据范围和执行保护。 |
| F-受保护 Seed | 2 | 保持数据库安全边界，单独审核。 |

## 风险汇总

- 高风险：5 个。
- 中风险：14 个。
- 低风险：25 个。

## 高风险候选

### `src/app/api/institution/wecom-customer-contact-readonly-proof-mock/route.ts`

- 类型：运行时 Demo 或 Mock API
- 执行边界：API 路由
- 引用文件数：5
- 处理意见：属于运行时路由，禁止按文件名直接搬入测试目录。

### `src/app/api/v1/knowledge-base/demo-readonly/route.ts`

- 类型：运行时 Demo 或 Mock API
- 执行边界：API 路由
- 引用文件数：5
- 处理意见：属于运行时路由，禁止按文件名直接搬入测试目录。

### `src/modules/auth/server/demo-session.ts`

- 类型：运行时 Demo 认证
- 执行边界：认证服务
- 引用文件数：36
- 处理意见：属于认证运行时边界，需单独安全设计。

### `src/server/db/seed-demo-data.ts`

- 类型：数据库 Seed 入口
- 执行边界：数据库入口
- 引用文件数：23
- 处理意见：保持数据库边界，后续只能单独审核。

### `src/server/db/seed-guard.ts`

- 类型：Seed 安全守卫
- 执行边界：数据库安全边界
- 引用文件数：4
- 处理意见：保持原位，禁止与普通 Demo 文件混合移动。

## 后续拆分建议

1. 第六阶段优先处理文档历史和纯测试候选的归属说明。
2. 模块 Mock 必须先区分是否被运行时 API、服务或组件调用。
3. Demo 认证、Demo API 和运行时 Mock 不得直接迁入测试目录。
4. Seed 入口和 Seed 安全守卫保持在数据库边界，单独立项审核。
5. 完整逐文件结果以配套 CSV 清单为准。

## 安全边界

- 未修改任何 `src/`、`scripts/`、`package.json` 或锁文件。
- 未执行数据库 Migration 或 Seed。
- 未连接数据库、HIS、企业微信或测试服务器。
- 未读取或输出 `.env.local` 或真实凭证。
- 未改变认证、权限、租户隔离和运行时行为。
