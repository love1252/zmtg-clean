# 智美天工模块地图

> 文档状态：PRODUCT-REBASE-01 初版，模块优先级仍需人工确认。

## 模块总览

| 模块 | 产品定位 | 1.0 状态判断 | 本轮是否可进入 runtime | 备注 |
| --- | --- | --- | --- | --- |
| 客户档案 / 患者档案 | 客户运营闭环的基础对象，承载客户身份、联系方式、来源、标签、生命周期和服务记录 | 必须做，但命名和字段边界待人工确认 | 否，本轮不得进入 runtime | 需避免把系统改造成 HIS 病历主系统 |
| 项目 / 治疗记录 | 连接客户需求、消费项目、治疗节点和后续随访的业务事实 | 必须做，可先简化为运营所需记录 | 否，本轮不得进入 runtime | 重点是触发运营动作，不是完整电子病历 |
| 预约 / 到院 | 客户从咨询到服务履约的关键节点 | 必须做，可简化为基础状态流转 | 否，本轮不得进入 runtime | 可支撑到院率、转化和后续任务 |
| 随访任务 | 治疗后运营闭环的核心执行对象 | 必须做 | 否，本轮不得进入 runtime | 需要人工确认、任务状态和时间线沉淀 |
| 复诊提醒 | 从治疗记录和 SOP 规则生成复诊机会 | 倾向必须做，具体范围待人工确认 | 否，本轮不得进入 runtime | 可先做提醒，不做自动触达 |
| 复购提醒 | 基于项目周期、客户标签和历史记录识别复购机会 | 可简化或后续增强，待人工确认 | 否，本轮不得进入 runtime | 1.0 可先保留运营机会列表 |
| 沉睡客户唤醒 | 面向长期未到院或未互动客户的召回运营 | 可简化或后续增强，待人工确认 | 否，本轮不得进入 runtime | 需要标签、分层和触达策略配合 |
| 标签 / 分层 | 用于客户画像、运营优先级、SOP 分流和看板分析 | 倾向简化纳入，待人工确认 | 否，本轮不得进入 runtime | 1.0 可先做基础标签和人工分层 |
| SOP / 流程编排 | 把项目、阶段、客户状态转为可执行任务路径 | 倾向必须做简化版，待人工确认 | 否，本轮不得进入 runtime | 可先做固定模板，不做复杂编排器 |
| AI 辅助沟通 | 为客服、咨询师提供随访话术、关怀建议和沟通辅助 | 可简化或后续增强，待人工确认 | 否，本轮不得进入 runtime | 需人工确认，不做自动医疗建议 |
| 企业微信 / 微信触达 | 客户触达渠道和互动沉淀方向 | 1.0 可先不做真实接入，待人工确认 | 否，本轮不得进入 runtime | 可先记录触达建议或模拟触达结果 |
| HIS 数据接入 | 多来源数据接入的一类，不是产品主线 | 1.0 不应被真实 HIS 阻塞 | 否，本轮不得进入 runtime | 真实凭证、真实外网、adapter、scheduler 全部后置审批 |
| 运营看板 | 展示客户运营任务、随访完成、复诊复购机会和机构运营指标 | 必须做基础版，待人工确认指标 | 否，本轮不得进入 runtime | 1.0 先服务运营决策，不做完整 BI |
| 权限 / 租户 / 审计 | 支撑机构多租户、平台管理、操作追踪和合规边界 | 必须做基础能力 | 否，本轮不得进入 runtime | 当前可见资料已多次强调租户、配额和审计 |

## 模块关系

`客户档案 / 患者档案` 是中心对象。

`预约 / 到院` 与 `项目 / 治疗记录` 形成服务履约事实。

`SOP / 流程编排` 基于服务事实生成 `随访任务`、`复诊提醒`、`复购提醒` 和 `沉睡客户唤醒`。

`标签 / 分层`、`AI 辅助沟通` 和 `企业微信 / 微信触达` 服务于客户运营执行。

`HIS 数据接入`、`API / Webhook` 和其他第三方系统接入是数据来源和集成能力，不应替代产品主线。

`运营看板`、`权限 / 租户 / 审计` 是产品化和商业化所需的治理能力。

## 需要避免的误判

- 不应把 HIS adapter 当成智美天工的核心产品模块。
- 不应把真实外网、真实凭证、scheduler、schema 扩展作为产品主线优先事项。
- 不应因为后续补充了 OneID、SceneID、Event、Webhook、插件化，就默认 1.0 必须实现全部平台能力。
- 不应把 AI 说成已经可以自动替代客服、咨询师或医生。
