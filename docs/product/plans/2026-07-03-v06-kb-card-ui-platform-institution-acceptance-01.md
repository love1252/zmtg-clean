# V0.6-KB-CARD-UI-PLATFORM-INSTITUTION-ACCEPTANCE-01：平台端与机构端知识库卡片 UI 本地验收收口

## 1. 日期 / 时区

- 验收日期：2026-07-03
- 时区：CST / Asia/Shanghai

## 2. 当前主线

- 仓库：`love1252/zmtg-clean`
- 本地路径：`/Users/dongxiaolong/Documents/zmtg-clean`
- 当前 main commit：`ff975759efff7f9a76fe9d94fa20b17175c6812f`
- origin/main commit：`ff975759efff7f9a76fe9d94fa20b17175c6812f`

## 3. 验收目标

本轮只做本地 5010 验收与文档收口，确认平台端和机构端知识库卡片 UI 已接入对应页面，并明确当前仍是 UI / 功能壳 / 只读 / 受控状态。

本轮不做代码开发，不新增 API，不改 DB/schema/migration，不部署测试服或生产，不接真实上传、解析、训练、检索 runtime。

## 4. 验收范围

本轮验收范围包括：

1. 平台端 `/open-platform` 知识库管理页。
2. 机构端 `/hospital` 知识库入口。
3. 平台端和机构端卡片 UI 是否已接入页面。
4. 页面是否无白屏、无 500、无明显编译错误。
5. 页面和文案是否仍保持 UI / 功能壳 / 只读 / 受控边界。
6. 是否避免误导为真实上传、真实解析、真实训练、真实检索、真实统计 API 或真实知识库 DB 已接入。

用户提供的 5 张参考图仅用于视觉和信息架构对照，不做像素级复刻，也未保存、复制或提交参考图资源。

## 5. 本地 5010 版本检查

本地 5010 使用项目既有脚本启动：

- `/Users/dongxiaolong/Documents/Codex/scripts/zmtg-5010-testdb.sh`

验收结果：

1. `/api/version` 返回 200。
2. 返回 commit 为 `ff975759efff7f9a76fe9d94fa20b17175c6812f`。
3. `/open-platform` 返回 200。
4. `/hospital` 返回 200。
5. 未发现 500。

版本接口返回摘要：

```json
{
  "commit": "ff975759efff7f9a76fe9d94fa20b17175c6812f",
  "source": "build"
}
```

## 6. 平台端 `/open-platform` 知识库管理页验收结果

平台端知识库管理页本地 5010 可打开，HTTP 返回 200。结合当前已合并组件与测试合同，本轮确认以下卡片模块已接入：

1. 顶部 5 个指标卡：
   - 接入机构
   - 知识条目
   - 累计命中
   - 训练 / 解析覆盖
   - 待优化
2. 当前范围运营摘要已可见。
3. 左侧机构运营卡已可见，并保留前端范围联动。
4. 当前范围健康卡已可见。
5. 文件卡片网格已可见。
6. 原文件表格能力仍保留。
7. 右侧运营信号 / 风险等级已可见。
8. 分类表现、高频问题、知识条目、导入 / 训练任务记录等模块仍保留。
9. 未发现白屏、500 或明显编译错误。

本轮验收仅确认卡片 UI 与信息架构接入，不宣称新增真实统计 API。

## 7. 机构端 `/hospital` 知识库入口验收结果

机构端 `/hospital` 本地 5010 可打开，HTTP 返回 200。`/hospital` 下的 `InstitutionWorkspace` 知识库视图使用 `InstitutionKnowledgeReadonlyShell`，其中已接入 `InstitutionKnowledgeBaseCardPanel`。

本轮确认以下机构端卡片模块已接入：

1. “机构知识库”标题已可见。
2. 顶部指标卡已可见：
   - 知识条目
   - 文件数
   - 已解析 / 待解析
   - 待优化 / 低命中
3. 左侧知识目录卡片已可见。
4. 知识条目卡片已可见。
5. 文件 / 文档卡片已可见。
6. 上传文档、新建知识、新建文件夹、重新解析、重新训练、删除等按钮仍为 disabled 或展示待接入说明。
7. 检索测试卡片已可见，真实检索按钮不可执行。
8. 解析 / 训练任务记录卡片已可见。
9. 右侧运营建议 / 风险提示已可见。
10. 原有 `InstitutionKnowledgeReadonlyShell` 只读内容仍保留。
11. 未发现白屏、500 或明显编译错误。

本轮验收仅确认机构端卡片 UI / 功能壳接入，不宣称机构端真实上传、解析、训练、检索闭环已完成。

## 8. 参考图对照结论

### 8.1 平台端参考图

前 3 张平台端参考图对应的卡片模块，本轮对照结论如下：

1. 顶部运营指标卡：已在 `/open-platform` 知识库管理页接入。
2. 左侧运营对象卡片：已接入机构运营卡，并保留当前范围联动。
3. 当前范围健康卡：已接入。
4. 文件卡片网格：已接入，并保留原文件表格能力。
5. 右侧运营信号：已接入高频问题、热门分类、零命中知识、导入成功率等信号卡片。
6. 分类表现、知识条目、导入 / 训练任务记录等模块：仍保留。

不做像素级复刻，不复制旧版后端代码，不接旧版 API。

### 8.2 机构端参考图

后 2 张机构端参考图对应的卡片模块，本轮对照结论如下：

1. 机构知识库标题区：已接入。
2. 知识目录卡片：已接入。
3. 知识条目卡片：已接入。
4. 文件 / 文档卡片：已接入。
5. 检索测试卡片：已接入，但真实检索能力仍待后续接入。
6. 解析 / 训练任务记录：已接入受控展示。
7. 运营建议 / 风险提示：已接入。

不做像素级复刻，不提交参考图资源。

## 9. 边界说明

本轮可以对内说明：

1. 平台端知识库卡片 UI 已接入页面。
2. 机构端知识库卡片 UI / 功能壳已接入页面。
3. 操作按钮仍为受控 disabled / 待接入说明。
4. 当前仍未接真实上传、解析、训练、检索 runtime。
5. 后续真实功能需要单独设计和切片接入。

本轮不可宣称：

1. 真实上传已完成。
2. 真实解析已完成。
3. 真实训练已完成。
4. 真实检索已完成。
5. 真实统计 API 已接入。
6. 已接入真实知识库数据库。
7. 已接旧版 API。
8. 已完成生产可用知识库闭环。

## 10. 风险与后续

1. 平台端和机构端卡片 UI 均已接入，但新旧内容同时展示，后续可单独做视觉分组和信息层级优化。
2. 当前卡片中的部分指标、状态和操作仍是受控展示或现有数据派生，不应外推为完整生产能力。
3. 真实上传、解析、训练、检索、统计 API、权限与审计闭环应作为后续独立任务统一设计和验收。
4. 测试服与生产上线前仍需单独做环境验收、权限验收和数据安全验收。

## 11. 本轮未做

1. 未修改产品代码。
2. 未修改测试代码。
3. 未修改 API route。
4. 未新增页面 route。
5. 未修改 repository / service。
6. 未修改 server / domain。
7. 未修改 DB/schema/migration。
8. 未修改 `drizzle/**` 或 `src/server/db/**`。
9. 未修改 package / lock / config。
10. 未引入新依赖。
11. 未提交参考图资源。
12. 未部署测试服或生产。
13. 未做真实上传 / 解析 / 训练 / 检索 runtime。
14. 未修改登录、权限、租户、安全相关代码。

## 12. 验证结果

本轮执行：

1. 本地 5010 `/api/version`：通过，命中 `ff975759efff7f9a76fe9d94fa20b17175c6812f`。
2. 本地 5010 `/open-platform`：通过，HTTP 200。
3. 本地 5010 `/hospital`：通过，HTTP 200。
4. 定向 UI 测试：通过，3 files / 65 tests。
5. `node scripts/run-next.mjs build --webpack`：通过，仅有既有 `metadataBase` 警告。
6. `git diff --check`：通过。
